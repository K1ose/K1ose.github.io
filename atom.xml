<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
  <title>K1ose&#39;s Space</title>
  
  <subtitle>Stay hungry, stay foolish</subtitle>
  <link href="https://www.jk404.cn/atom.xml" rel="self"/>
  
  <link href="https://www.jk404.cn/"/>
  <updated>2022-12-24T10:28:15.536Z</updated>
  <id>https://www.jk404.cn/</id>
  
  <author>
    <name>K1ose</name>
    
  </author>
  
  <generator uri="https://hexo.io/">Hexo</generator>
  
  <entry>
    <title>Linux_kernel_development_04_process_scheduling</title>
    <link href="https://www.jk404.cn/study/book/Linux-Kernel-Development/Linux-kernel-development-04-process-scheduling/"/>
    <id>https://www.jk404.cn/study/book/Linux-Kernel-Development/Linux-kernel-development-04-process-scheduling/</id>
    <published>2022-12-23T19:01:43.000Z</published>
    <updated>2022-12-24T10:28:15.536Z</updated>
    
    <content type="html"><![CDATA[<h1>进程调度</h1><p>进程调度程序是确保进程能有效工作的一个内核子系统，它负责决定：</p><ul><li>哪个进程可以投入运行；</li><li>进程何时运行；</li><li>进程可以运行的时长；</li></ul><p>可以将它看作在<u>可运行态进程之间分配有限的处理器时间资源</u>的内核子系统，这是多任务操作系统的基础。对进程的合理调度使得系统资源最大限度地发挥作用，多进程才会有并发执行的效果。</p><p>只要有可以执行的进程，那么就总会有进程在执行。但只要系统中可运行的进程数目比处理器个数多，就注定在某一时刻一部分进程不能执行。可以说，调度程序的基本工作就是：在一组处于可运行状态的进程中选择一个来执行。</p><h2 id="多任务">多任务</h2><p>多任务操作系统：同时并发地<u>交互</u>执行多个进程的操作系统。</p><p>多任务操作系统可以分为两类：</p><ul><li>非抢占式多任务 cooperative multitasking：除非进程自己主动停止运行，否则它会一直执行。这个主动挂起自己的动作叫做<u>让步(yielding)</u>。当调度程序无法主导每个进程的运行时长时，麻烦就会增多。</li><li>抢占式多任务 preemptive multitasking：由调度程序决定何时停止一个进程的运行，以便其他进程能够得到执行的机会，这个强制将当前执行进程挂起的动作叫做<u>抢占(preemption)</u>。进程的<u>时间片(timeslice)</u>是进程在被抢占前能够运行的时间。有效合理地管理时间片使得调度程序从系统全局的角度做出调度决定。</li></ul><h2 id="Linux的进程调度">Linux的进程调度</h2><p>自 Linux 2.5 内核后，引入了一种叫做 O(1) 调度程序的新机制，从名字就知道这是一个效率很高的调度机制。其中使用了两个主要方法：</p><ul><li>静态时间片算法</li><li>每一处理器新建单独的运行队列</li></ul><p>该算法在调度那些对响应时间很敏感的程序有一些先天不足，例如对用户交互进程。在 2.6.23 中，引入了著名的“反转楼梯最后期限调度算法”(Rotaing Staircase Deadline scheduler, RSDL)，它汲取了队列理论，引入了公平调度的概念，所以也被称为“完全公平调度算法” (CFS)。</p><h2 id="策略">策略</h2><p>策略决定调度程序在何时让何种程序运行，它是调度进程和优化使用处理器的核心。</p><h3 id="I-O消耗型和处理器消耗型进程">I/O消耗型和处理器消耗型进程</h3><p>从设备占用进程主要执行时间的角度看，进程可以分为：</p><ul><li><p>I/O 消耗型进程</p><p>大部分时间用来提交 I/O 请求或是等待 I/O 请求，因而常处于可运行状态，但执行时间较短。比如，大多数用户图形界面程序(GUI)都属于 I/O 密集型程序，所以它们在执行时，属于 I/O 消耗型进程。</p></li><li><p>处理器消耗型进程</p><p>大部分时间用来执行代码上。从系统响应速度上考虑，不该让这样的进程常常运行，因此调度器会尽量减少调度这类程序的频率。例如，极端的有 while(true) 循死环，或者是 sshkeygen 和 MATLAB 这类需要执行大量数学计算的程序。</p></li></ul><p>一个进程不是非A即B的，许多程序即是 I/O 消耗型程序，又是处理器消耗型程序。所以，调度策略通常需要在二者之间寻找平衡，即：在<u>进程响应速度</u>和<u>最大系统利用率</u>上寻找最优平衡解。</p><h3 id="进程优先级">进程优先级</h3><p>基于优先级的调度是调度策略中最基本的一类，它根据进程的价值和对处理器时间的需求对进程进行分级。通常做法是：</p><ul><li>优先级高的进程先运行，优先级低的后运行；</li><li>相同优先级的进程按轮转方式运行；</li></ul><p>Linux 采用了两种优先级范围：</p><ul><li>nice 值：是默认为 0，范围在 -20~19 之间的数值。越低代表优先级越高，反之则反。低 nice 值的进程可以获得更多的处理器时间。可以通过 <code>ps-el</code> 来查看进程列表，其中 <code>NI</code> 列即为进程的 nice 值。基于 Unix 的不同的操作系统对 nice 值的运用方式不同，例如：<ul><li>Mac OS X 中的 nice 值表示分配给进程的时间片的绝对值；</li><li>Linux 中的 nice 值表示分配给进程的时间片的比例；</li></ul></li><li>可配置的实时优先级：默认变化范围为 0~99 ，数值越高代表优先级越高。任何实时进程的优先级都高于普通进程，实时优先级与 nice 值属于两个互不相交的范畴。使用 <code>ps-eo state,uid,pid,ppid,rtprio,time,comm</code> 来查看进程列表，实时优先级为 <code>RTPRIO</code> 列表，如果显示为 <code>-</code> ，则说明它不是实时进程。</li></ul><h3 id="时间片">时间片</h3><p>时间片(timeslice) 是一个数值，表示进程被抢占前所能持续运行的时间。调度策略必须为时间片设置默认值，但这不是一个简单的事情，正如先前提到的 I/O 消耗型进程和处理器消耗型进程所涉及的矛盾。默认的时间片是 10 ms，Linux 的 CFS 调度器没有直接给进程分配时间片数值，而是将处理器的使用比例划分给进程，这个比例还影响进程的 nice 值因此，进程所获得的处理器使用时间与系统负载时密切相关的。</p><p>在一般的操作系统里，进程是否立刻投入运行取决于：</p><ul><li>进程的优先级</li><li>是否有时间片</li></ul><p>而在 Linux 的 CSF 调度器中，抢占时机取决于：</p><ul><li>消耗的处理器使用比例（消耗使用比例更小，则立刻投入运行，否则推迟运行）</li></ul><h3 id="调度策略的活动">调度策略的活动</h3><p>假设有两个可运行的进程：</p><ul><li>文字编辑程序（I/O 消耗型）</li><li>视频编码程序（处理器消耗型）</li></ul><p>理想情况下应当这样处理：</p><ul><li>给予文本编辑程序更多处理器时间</li><li>希望文本编辑程序在被唤醒时能够抢占视频解码程序</li></ul><p>这样用户才能得到使用交互式程序时带来的好的体验。这也意味着需要给文本编辑程序分配更高的优先级和时间片。</p><p>但 Linux 分配一个给定的处理器使用比。如果仅有这两个进程在运行，且它们具有相同的 nice 值，则处理器使用比都将分配为 50% 。但由于交互式程序需要更多时间等待输入，因此肯定不会用到处理器 50% 的使用时间，而类似于视频编码程序这类处理器消耗型进程就有机会使用超过 50% 的处理器使用时间，以便更快完成任务。</p><p>CFS 调度器能够发现，文本编辑程序并没有消费掉承诺给它的50%的处理器使用比，因此在文本编辑程序需要被投入运行时，让其进行抢占动作。</p><h2 id="Linux调度算法">Linux调度算法</h2><p>进一步深入了解Linux特色的进程调度。</p><h3 id="调度器类">调度器类</h3><p>Linux调度器时以<u>模块</u>方式提供的，这种模块化结构被称为<u>调度器类 (scheduler classes)</u>，目的是<u>允许不同类型的进程可以针对性地选择调度算法</u>。</p><p>每个调度器都有一个优先级，基础的调度器代码定义在 <code>kernel/sched.c</code> 中，它按照<u>优先级顺序遍历调度类</u>，拥有可执行进程的<u>最高优先级的调度类将胜出</u>，去选择下面要执行的那一个程序。</p><p>完全公平调度 (CFS) 是针对普通进程的调度类，在 Linux 中被称为 <code>SCHED_NORMAL</code> ，CFS 算法的实现在 <code>kernel/sched_fair.c</code> 中。</p><h3 id="Unix-系统中的进程调度">Unix 系统中的进程调度</h3><p>先前说到现代进程调度其通用的两个概念：1. 进程优先级；2. 时间片。由进程的优先级影响时间片的大小，即影响进程执行的时间长短。在 Unix 系统中以 nice 值来表示进程优先级。但是由 nice 值映射到时间片的简单方式会带来很多问题：</p><ul><li><p>【问题一】nice 单位值对应到处理器绝对时间。在这里，假设默认时间片单位为 <code>100ms</code>。那么：</p><ul><li>【情况一】当存在两个 nice 值分别为 0 （默认优先级） 和 +19 （最低优先级） 的两个进程，它们将分别占用处理器时间 <code>100ms</code> 和 <code>5ms</code>。假设理想条件下，系统中仅有这两个进程待运行时，那么它们分别占用 <code>20/21 (100ms/105ms)</code> 和 <code>1/21 (5ms/105ms)</code> 的处理器执行时间。内核将在 105ms 内进行两次上下文切换。</li><li>【情况二】当存在两个 nice 值都为 +19 （最低优先级）的进程，它们都占用处理器执行时间 5ms 。在理想条件下，系统中仅有这两个进程待运行时，它们每次仅能获得 <code>5ms</code> 的处理器执行时间，且在 <code>10ms</code> 内，处理器需要进行两次上下文切换。</li><li>【情况三】当存在两个 nice 值都为 0 （默认优先级）的普通进程，它们都占用处理器执行时间 100ms 。在理想条件下，系统中仅有这两个进程待运行时，它们每次各获得 100ms 的处理器执行时间，在 200ms 内，处理器进行两次上下文切换。</li></ul><p>在事实上：</p><ul><li>给定高 nice 值（低优先级）的进程多是<u>计算密集型进程</u>，它们可能需要较长的处理器执行时间；</li><li>给定默认 nice 值（默认优先级）的进程多是前台用户任务，它们往往要求抢占（响应速度）而不要求较长的处理器执行时间（系统利用率）。</li></ul></li><li><p>【问题二】体现为相对 nice 值表现的处理器执行时间的不公平，以 <code>O(1)</code> 调度算法为例：</p><ul><li>【情况一】当存在两个 nice 值分别为 0  和 +1 的两个进程，它们分别占用的处理器执行时间为 10<code>0ms 和 </code>95ms<code>，相差仅</code>5ms`。</li><li>【情况二】当存在两个 nice 值分别为 +18  和 +19 的两个进程，它们分别占用的处理器执行时间为 <code>10ms</code> 和 <code>5ms</code> ，相差了整整一倍。</li></ul><p>可以通过几何增加/减小而非算数增加/减小来解决这个问题。</p></li><li><p>【问题三】绝对时间片必须能在内核的测试范围内，这要求绝对时间片必须是定时器节拍的整数倍，也就是 <code>10ms</code> 或者 <code>1ms</code> 的倍数（关于定时器需要参考第11章）。<u>引入 CFS 就是为了解决时间片跟随系统定时器变化而变化的问题</u>。如果绝对时间片跟随定时器变化，那相邻优先级的进程所获得的处理器执行时间相隔可多至 <code>10ms</code> ，少至 <code>1ms</code> 。可以通过引入新的度量机制来解决这个问题。</p></li><li><p>【问题四】基于优先级的调度器为了<u>优化交互任务而唤醒相关进程</u>的手段导致的不公平。为了让交互进程能更快投入运行，系统可能会提升该进程的优先级，即使它们的时间片已经用尽。这个机制会给一些特殊的交互用例玩弄调度器的后门，导致对其他进程不公平。</p></li></ul><p>即使上述问题能够通过一些非结构性的改动来解决，但是这样的调度算法没有看到根本问题：需要一个与处理器执行时间直接挂钩的调度机制。于是 CFS 应运而生：它摒弃时间片分配机制，而直接分配给进程一个<u>处理器使用比重</u>，从而确保了进程调度能由恒定的公平性。</p><h3 id="CFS-公平调度">CFS 公平调度</h3><p>CFS 允许每个进程运行一段时间、循环轮转、选择运行最少的进程作为下一个运行进程。它不依靠 nice 值来计算运行时长，而是从总数进程上计算出一个进程应该运行多久。但 CFS 没有摒弃 nice 值，而是将它用作进程获得处理器运行比的权重。</p><p>每个进程按照其权重在全部可运行进程中所占的比例来运行。为了计算准确的运行时间，CFS 为完美多任务中的无限小的调度周期的近似值设立了一个目标，称为“目标延迟”。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;进程调度&lt;/h1&gt;
&lt;p&gt;进程调度程序是确保进程能有效工作的一个内核子系统，它负责决定：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;哪个进程可以投入运行；&lt;/li&gt;
&lt;li&gt;进程何时运行；&lt;/li&gt;
&lt;li&gt;进程可以运行的时长；&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;可以将它看作在&lt;u&gt;可运行态进</summary>
      
    
    
    
    <category term="study" scheme="https://www.jk404.cn/categories/study/"/>
    
    <category term="book" scheme="https://www.jk404.cn/categories/study/book/"/>
    
    <category term="Linux Kernel Development" scheme="https://www.jk404.cn/categories/study/book/Linux-Kernel-Development/"/>
    
    
  </entry>
  
  <entry>
    <title>Linux_kernel_development_03_process_management</title>
    <link href="https://www.jk404.cn/study/book/Linux-Kernel-Development/Linux-kernel-development-03-process-management/"/>
    <id>https://www.jk404.cn/study/book/Linux-Kernel-Development/Linux-kernel-development-03-process-management/</id>
    <published>2022-12-19T18:40:37.000Z</published>
    <updated>2022-12-22T17:19:08.594Z</updated>
    
    <content type="html"><![CDATA[<h1>进程管理</h1><p>操作系统为用户程序服务，因此，进程管理是所有操作系统的心脏所在。</p><h2 id="进程">进程</h2><p>进程是处于执行期的程序及其相关的资源的总称，它包含其他资源（如打开的文件、挂起的信号等），内核需要有效、透明地管理所有细节。进程还有另一个名称：任务(task)</p><p>线程(thread)是在进程中的活动对象，每个线程都拥有一个独立的程序计数器、进程栈和一组进程寄存器。内核调度的是线程，对Linux而言，线程不与进程区分，它只是一种特殊的进程。</p><p>进程提供两种虚拟机制：</p><ul><li>虚拟处理器：许多进程共享一个处理器，但是虚拟处理器使得进程认为自己在独享处理器。</li><li>虚拟内存：许多进程共享一个内存资源，但是虚拟内存使得进程认为自己独享整个内存资源。</li></ul><p>在Linux中，通常使用 <code>fork()</code> 系统调用，通过复制一个现有进程来创建一个新进程。</p><ul><li>父进程：调用 <code>fork()</code> 的进程</li><li>子进程：新生成的进程</li></ul><p>在 <code>fork()</code> 调用结束后，父进程恢复执行，子进程也开始执行。因此，fork() 系统调用会从内核返回两次，一次回到父进程，一次回到子进程。</p><p>在 <code>fork()</code> 之后，通常会使用 <code>exec()</code> 来创建新的地址空间，并把新的程序载入其中。而在现代Linux中，<code>fork()</code> 实际上是由 <code>clone()</code> 系统调用实现的。程序最终通过 <code>exit()</code> 系统调用退出执行，进程终结，占用资源被释放。父进程可以通过 <code>wait4()</code> 系统调用查询子进程是否终结，进程退出执行后被设置为僵死状态，直到父进程的调用 <code>wait()</code> 或 <code>waitpid()</code> 为止。</p><h2 id="进程描述符和任务结构">进程描述符和任务结构</h2><p>内核把进程的列表存放在任务队列(task list)或者是任务数组(task array)的双向循环链表中。链表中的每一项都是类型为 <code>task_struct</code> 的结构，该结构被称为<strong>进程描述符</strong>(process descriptor)，在 <code>&lt;linux/sched.h&gt;</code> 中被定义。该结构包含一个具体进程的所有信息。</p><h3 id="进程描述符的分配">进程描述符的分配</h3><p>Linux 通过 <code>slab</code> 分配器分配 <code>task_struct</code> 结构。在 2.6 版本前的内核中，各个进程的 <code>task_struct</code> 存放在它们内核栈的末尾。而现在则使用 slab 动态生成 <code>task_struct</code> ，所以只需要往栈中压入一个新的结构 <code>struct thread_info</code> 即可。这个结构可以使得在汇编代码中能很容易计算出其偏移。</p><p>在内核源码树中的 <code>arch</code> 文件夹中，存放了不同架构所对应的代码，例如 <code>x86</code> 的 <code>struct thread_info</code> 在文件 <code>arch/x86/include/asm/thread_info.h</code> 中定义。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">thread_info</span> &#123;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span>*<span class="title">task</span>;</span><span class="comment">/* main task structure */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">exec_domain</span>*<span class="title">exec_domain</span>;</span><span class="comment">/* execution domain */</span></span><br><span class="line">__u32flags;<span class="comment">/* low level flags */</span></span><br><span class="line">__u32status;<span class="comment">/* thread synchronous flags */</span></span><br><span class="line">__u32cpu;<span class="comment">/* current CPU */</span></span><br><span class="line"><span class="keyword">int</span>preempt_count;<span class="comment">/* 0 =&gt; preemptable,</span></span><br><span class="line"><span class="comment">   &lt;0 =&gt; BUG */</span></span><br><span class="line"><span class="keyword">mm_segment_t</span>addr_limit;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">restart_block</span>    <span class="title">restart_block</span>;</span></span><br><span class="line"><span class="keyword">void</span> __user*sysenter_return;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> CONFIG_X86_32</span></span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span>           previous_esp;   <span class="comment">/* ESP of the previous stack in</span></span><br><span class="line"><span class="comment">   case of nested (IRQ) stacks</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line">__u8supervisor_stack[<span class="number">0</span>];</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"><span class="keyword">int</span>uaccess_err;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>每个任务的 <code>thread_info</code> 结构在它的内核栈的末尾分配，结构中 <code>task</code> 域中存放的是指向该任务的 <code>task_struct</code> 的指针。</p><h3 id="进程描述符的存放">进程描述符的存放</h3><p>内核通过唯一的<u>进程标识值</u>(process identification value)或 <code>PID</code> 来标识每个进程，内核把每个进程的 <code>PID</code> 存放在它们各自的 <code>task_struct</code> 中。<code>task_struct</code> 在 <code>include/linux/sched.h</code> 中定义，其结构体内还有许多其他的数据：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> &#123;</span></span><br><span class="line"><span class="keyword">volatile</span> <span class="keyword">long</span> state;<span class="comment">/* -1 unrunnable, 0 runnable, &gt;0 stopped */</span></span><br><span class="line"><span class="keyword">void</span> *<span class="built_in">stack</span>;</span><br><span class="line"><span class="keyword">atomic_t</span> usage;</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">int</span> flags;<span class="comment">/* per process flags, defined below */</span></span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">int</span> ptrace;</span><br><span class="line"></span><br><span class="line"><span class="keyword">int</span> lock_depth;<span class="comment">/* BKL lock depth */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">    </span><br><span class="line"><span class="keyword">pid_t</span> pid;</span><br><span class="line"><span class="keyword">pid_t</span> tgid;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p><code>PID</code> 的数据类型是 <code>pid_t</code> ，这是一个隐含类型（表示该数据类型是未知或不相关的），而实际上它就是一个 <code>int</code> 整型数据，其默认最大值设置为 <code>32768</code> （<code>short int</code> 短整型的最大值）。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* include/linux/types.h */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">__kernel_pid_t</span><span class="keyword">pid_t</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* include/asm-generic/posix_types.h */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> __kernel_pid_t</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">int</span><span class="keyword">__kernel_pid_t</span>;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br></pre></td></tr></table></figure><p><code>&lt;linux/threads.h&gt;</code> 中限制 <code>PID</code> 的最大值为 400 万，可以通过修改 <code>/proc/sys/kernel/pid_max</code> 来提高上限。这个最大值实际上就是系统允许同时存在的进程的最大数目。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* include/linux/threads.h */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * This controls the default maximum pid allocated to a process</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PID_MAX_DEFAULT (CONFIG_BASE_SMALL ? 0x1000 : 0x8000)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * A maximum of 4 million PIDs should be enough for a while.</span></span><br><span class="line"><span class="comment"> * [<span class="doctag">NOTE:</span> PID/TIDs are limited to 2^29 ~= 500+ million, see futex.h.]</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PID_MAX_LIMIT (CONFIG_BASE_SMALL ? PAGE_SIZE * 8 : \</span></span><br><span class="line"><span class="meta">(sizeof(long) &gt; 4 ? 4 * 1024 * 1024 : PID_MAX_DEFAULT))</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br></pre></td></tr></table></figure><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ cat /proc/sys/kernel/pid_max</span><br><span class="line">4194304</span><br></pre></td></tr></table></figure><p>在内核中，访问任务通常需要获得指向该任务 <code>task_struct</code> 的指针，通过 current 宏查找到当前正在运行的进程的 <code>task_struct</code> 。这个宏与硬件体系结构相关，需要专门处理。在寄存器多的体系结构中，有专门的寄存器用于存放指向当前进程的 <code>task_struct</code> 的指针，例如在 MIPS 架构中，获取 current 宏只需要直接访问寄存器：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// arch/mips/include/asm/thread_info.h</span></span><br><span class="line"><span class="comment">/* How to get the thread information struct from C.  */</span></span><br><span class="line"><span class="keyword">register</span> <span class="class"><span class="keyword">struct</span> <span class="title">thread_info</span> *__<span class="title">current_thread_info</span> __<span class="title">asm__</span>(&quot;$28&quot;);</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> current_thread_info()  __current_thread_info</span></span><br></pre></td></tr></table></figure><p>但在 <code>x86</code> 这种寄存器较少的架构中，只能通过在内核栈的尾部压入 <code>thread_info</code> 结构，通过计算偏移来简介查找对应的 <code>task_struct</code> 。具体操作由 <code>current_thread_info()</code> 函数完成：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// arch/x86/include/asm/thread_info.h</span></span><br><span class="line"><span class="comment">/* how to get the thread information struct from C */</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">inline</span> struct thread_info *<span class="title">current_thread_info</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">return</span> (struct thread_info *)</span><br><span class="line">(current_stack_pointer &amp; ~(THREAD_SIZE - <span class="number">1</span>));</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">     * PAGE_SHIFT determines the page size </span></span><br><span class="line"><span class="comment">     * #define PAGE_SHIFT 12</span></span><br><span class="line"><span class="comment">     * #define PAGE_SIZE(1UL &lt;&lt; PAGE_SHIFT)     // =&gt; PAGE_SIZE = 1000000000000(4096)</span></span><br><span class="line"><span class="comment">     * </span></span><br><span class="line"><span class="comment">     * #ifdef CONFIG_4KSTACKS</span></span><br><span class="line"><span class="comment">     * #define THREAD_ORDER0</span></span><br><span class="line"><span class="comment">     * #else</span></span><br><span class="line"><span class="comment">     * #define THREAD_ORDER1</span></span><br><span class="line"><span class="comment">     * #endif</span></span><br><span class="line"><span class="comment">     *</span></span><br><span class="line"><span class="comment">     * #define THREAD_SIZE (PAGE_SIZE &lt;&lt; THREAD_ORDER) // =&gt; THREAD_SIZE = 10000000000000(8192)</span></span><br><span class="line"><span class="comment">     * ~(THREAD_SIZE - 1) = 1111111111111111111111111111111111111111111111111110000000000000（-8192）</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">// arch/mn10300/include/asm/thread_info.h</span></span><br><span class="line"><span class="comment">/* how to get the current stack pointer from C */</span></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">inline</span> <span class="keyword">unsigned</span> <span class="keyword">long</span> <span class="title">current_stack_pointer</span><span class="params">(<span class="keyword">void</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> sp;</span><br><span class="line"><span class="keyword">asm</span>(<span class="string">&quot;mov sp,%0; &quot;</span>:<span class="string">&quot;=r&quot;</span> (sp));</span><br><span class="line"><span class="keyword">return</span> sp;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><code>current_stack_pointer &amp; ~(THREAD_SIZE - 1)</code> 相当于屏蔽了 <code>sp</code> 的后13个有效位，汇编代码为：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">movl $-8192, %eax</span><br><span class="line">andl %esp, %eax</span><br></pre></td></tr></table></figure><p>上述代码中，默认的栈大小是 8 KB，如果栈大小是 4 KB ，那么 <code>THREAD_ORDER</code> 将为 0 ，最终进行 <code>andl</code> 的数值是 -4096 ，即屏蔽后 <code>sp</code> 的12 位。</p><p>最后，在得到指向当前进程的 <code>thread_info</code> 结构体的指针后，返回其 <code>task</code> 域，即 <code>task_struct</code> 结构体的地址。</p><h3 id="进程的几种状态">进程的几种状态</h3><p>task_struct 的 state 域描述了进程的当前状态，它必然是五种状态的其中一种：</p><ul><li><code>TASK_RUNNING</code> - 运行，进程是可执行的，两种情形：<ul><li>正在执行；</li><li>在运行队列中等待执行；</li></ul></li><li><code>TASK_INTERRUPTIBLE</code> - 可中断，进程在睡眠（被阻塞），需要等待某些条件达成才能进入运行状态。在接收到信号时，可被提前唤醒并准备进入运行态。</li><li><code>TASK_UNINTERRUPTIBLE</code> - 不可中断，进程在睡眠（被阻塞），需要等待某些条件达成才能进入运行状态。不接受信号的影响。</li><li><code>__TASK_TRACED</code> - 正在被其他进程跟踪的进程，如通过 <code>ptrace</code> 对被调试程序进行跟踪时，被调试程序出于该状态。</li><li><code>__TASK_STOPPED</code> - 停止，没有进入运行态，也不能进入运行态。在接收到 <code>SIGSTOP</code> 、 <code>SIGTSTP</code> 、 <code>SIGTTIN</code> 、 <code>SIGTTOU</code> 后进入该状态。此外，在调试期间的程序接收到任何信号，也会进入该状态。</li></ul><h3 id="设置当前进程状态">设置当前进程状态</h3><p>内核经常需要调整进程的状态，这是它会使用 <code>set_task_state(task, state)</code> 函数，将指定的 <code>task</code> 的状态设置为 <code>state</code> 参数所指示的状态。等价于 <code>task-&gt;state = state</code> 。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// include/linux/sched.h</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * set_current_state() includes a barrier so that the write of current-&gt;state</span></span><br><span class="line"><span class="comment"> * is correctly serialised wrt the caller&#x27;s subsequent test of whether to</span></span><br><span class="line"><span class="comment"> * actually sleep:</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> *set_current_state(TASK_UNINTERRUPTIBLE);</span></span><br><span class="line"><span class="comment"> *if (do_i_need_to_sleep())</span></span><br><span class="line"><span class="comment"> *schedule();</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * If the caller does not need such serialisation then use __set_current_state()</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> __set_current_state(state_value)\</span></span><br><span class="line"><span class="meta">do &#123; current-&gt;state = (state_value); &#125; while (0)</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> set_current_state(state_value)\</span></span><br><span class="line"><span class="meta">set_mb(current-&gt;state, (state_value))</span></span><br></pre></td></tr></table></figure><h3 id="进程上下文">进程上下文</h3><p>代码从硬盘上的可执行文件中载入到进程的地址空间，一般程序在用户空间执行。当程序进行系统调用或出发某个异常后，该程序就陷入了内核。内核代表该进程执行，并处于进程上下文中，上下文中的 <code>current</code> 宏是有效的。除非有更高优先级的进程需要执行并由调度器做出调整，否则内核退出后，程序会在用户空间中恢复执行。</p><p>用户程序对内核提供的所有服务的访问必须通过接口：系统调用、异常处理。</p><h3 id="进程家族树">进程家族树</h3><p>所有进程都是 <code>PID</code> 为 1 的 <code>init</code> 进程的后代。 内核在系统启动的最后阶段启动 <code>init</code> 进程，其读取系统的初始化脚本 <code>initscript</code> 并执行相关程序，最终完成系统启动的过程。因此：</p><ul><li><p>每个进程必有一个父进程。</p></li><li><p>每个进程可以拥有零个或多个子进程。</p></li><li><p>拥有同一个父进程的所有进程被称为兄弟。</p></li></ul><p>可以看到，在 <code>task_struct</code> 结构体中，包含了一个指向其父进程 <code>task_struct</code> 结构体地址的指针 <code>parent</code> ，还包含一个指向其子进程 <code>task_struct</code> 结构体的链表指针 <code>children</code> 。</p><p>由此，对于当前进程的 <code>task_struct current</code> ，可以用以下方式访问到 <code>parent</code> 和 <code>children</code> 。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 访问父进程的 task_struct */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">my_parent</span> =</span> current-&gt;parent;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 遍历访问子进程的 task_struct */</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">task</span>;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> *<span class="title">list</span>;</span></span><br><span class="line"></span><br><span class="line">list_for_each(<span class="built_in">list</span>, &amp;current-&gt;children)&#123;</span><br><span class="line">    task = list_entry(<span class="built_in">list</span>, struct task_struct, sibling);</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * 相当于</span></span><br><span class="line"><span class="comment"> * for (list = (current-&gt;children)-&gt;next; prefetch(list-&gt;next), list != (current-&gt;children);list = list-&gt;next) </span></span><br><span class="line"><span class="comment"> task = list_entry(list, struct task_struct, sibling);</span></span><br><span class="line"><span class="comment">*/</span></span><br></pre></td></tr></table></figure><p><code>list_head</code> 和 <code>list_for_each</code> 等与链表相关的结构体和函数在 <code>include/linux/list.h</code> 中定义。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// include/linux/list.h</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Simple doubly linked list implementation.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Some of the internal functions (&quot;__xxx&quot;) are useful when</span></span><br><span class="line"><span class="comment"> * manipulating whole lists rather than single entries, as</span></span><br><span class="line"><span class="comment"> * sometimes we already know the next/prev entries and we can</span></span><br><span class="line"><span class="comment"> * generate better code by using them directly rather than</span></span><br><span class="line"><span class="comment"> * using the generic single-entry routines.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> &#123;</span></span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">list_head</span> *<span class="title">next</span>, *<span class="title">prev</span>;</span></span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LIST_HEAD_INIT(name) &#123; &amp;(name), &amp;(name) &#125;</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> LIST_HEAD(name) \</span></span><br><span class="line"><span class="meta">struct list_head name = LIST_HEAD_INIT(name)</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">INIT_LIST_HEAD</span><span class="params">(struct list_head *<span class="built_in">list</span>)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="built_in">list</span>-&gt;next = <span class="built_in">list</span>;</span><br><span class="line"><span class="built_in">list</span>-&gt;prev = <span class="built_in">list</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* ... */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * list_for_each-iterate over a list</span></span><br><span class="line"><span class="comment"> * @pos:the &amp;struct list_head to use as a loop cursor.</span></span><br><span class="line"><span class="comment"> * @head:the head for your list.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> list_for_each(pos, head) \</span></span><br><span class="line"><span class="meta">for (pos = (head)-&gt;next; prefetch(pos-&gt;next), pos != (head); \</span></span><br><span class="line"><span class="meta">        pos = pos-&gt;next)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* ... */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * list_entry - get the struct for this entry</span></span><br><span class="line"><span class="comment"> * @ptr:the &amp;struct list_head pointer.</span></span><br><span class="line"><span class="comment"> * @type:the type of the struct this is embedded in.</span></span><br><span class="line"><span class="comment"> * @member:the name of the list_struct within the struct.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> list_entry(ptr, type, member) \</span></span><br><span class="line"><span class="meta">container_of(ptr, type, member)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * container_of(ptr, type, member) 在 include/linux/kernel.h 中定义</span></span><br><span class="line"><span class="comment"> * container_of - cast a member of a structure out to the containing structure</span></span><br><span class="line"><span class="comment"> * @ptr:the pointer to the member.</span></span><br><span class="line"><span class="comment"> * @type:the type of the container struct this is embedded in.</span></span><br><span class="line"><span class="comment"> * @member:the name of the member within the struct.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure><p><code>sibling</code> 是 <code>task_struct</code> 结构体的成员，是一个存储了该进程的兄弟进程的链表：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">struct list_head sibling;/* linkage in my parent&#x27;s children list */</span><br></pre></td></tr></table></figure><p>关于 <code>prefetch(const void *, ...)</code> ：</p><p><img src="/study/book/Linux-Kernel-Development/Linux-kernel-development-03-process-management/figure01.png" alt></p><p><code>init</code> 进程的进程描述符是作为 <code>init_task</code> 静态分配的。所以，与其纠结先有鸡还是先有蛋，不如直接让女娲造人。下面的代码可以很好地演示所有进程间的关系。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">task</span>;</span></span><br><span class="line"><span class="keyword">for</span> (task = current; task != &amp;init_task; task = task-&gt;parent)</span><br><span class="line">;</span><br></pre></td></tr></table></figure><p>在执行完上述代码后，最终 <code>task</code> 会指向 <code>init</code> 的 <code>task_struct</code> 。</p><p>对于给定的进程 <code>task_struct</code> 指针 <code>task</code> ，获得其下一个进程的指针：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">list_entry(task-&gt;task.next, struct task_struct, task);</span><br></pre></td></tr></table></figure><p>在内核中有封装好的 <code>next_task(task)</code> 宏，完成了上述的功能。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> next_task(p) \</span></span><br><span class="line"><span class="meta">list_entry_rcu((p)-&gt;tasks.next, struct task_struct, tasks)</span></span><br></pre></td></tr></table></figure><p>获得其上一个进程的指针：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">list_entry(task-&gt;task.prev, struct task_struct, task);</span><br></pre></td></tr></table></figure><p>在内核中有封装好的 <code>prev_task(task)</code> 宏，完成了上述的功能。</p><p>如果想要遍历任务列表的话，使用 <code>for_each_process(task)</code> 是很好的选择：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">task</span>;</span></span><br><span class="line"></span><br><span class="line">for_each_process(task)&#123;</span><br><span class="line">printk(<span class="string">&quot;%s[%d]\n&quot;</span>, task-&gt;comm, task-&gt;pid);<span class="comment">// 打印任务名称</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* #define for_each_process(p) \</span></span><br><span class="line"><span class="comment"> *for (p = &amp;init_task ; (p = next_task(p)) != &amp;init_task ; )</span></span><br><span class="line"><span class="comment"> */</span></span><br></pre></td></tr></table></figure><h2 id="进程创建">进程创建</h2><p>许多操作系统在创建进程时，提供的是 spawn 机制：首先在新的地址空间里创建进程，读入可执行文件，最后开始执行。</p><p>Unix 将上述的步骤分解到了两个独立的函数中执行： <code>fork()</code> 和 <code>exec()</code> 。</p><ul><li><code>fork()</code> 通过拷贝当前进程创建一个子进程。此时，子进程和父进程的区别仅仅在于 <code>PID</code> 、 <code>PPID</code> 以及某些资源和统计量。</li><li><code>exec()</code> 读取可执行文件并将其载入地址空间，开始运行。</li></ul><h3 id="写时拷贝">写时拷贝</h3><p>传统的 fork() 系统调用直接把<u>所有的资源</u>复制给新创建的进程，这样会存在一些弊端：</p><ul><li>拷贝的数据不一定会被共享给子进程；</li><li>如果新进程打算立即执行一个新的映像，所有的拷贝将被丢弃；</li></ul><p>Linux的 fork() 系统调用使用<u>写时拷贝(copy-on-write)</u>页实现，它可以推迟甚至免除拷贝数据的技术，在不复制整个进程地址空间下，让父子进程共享同一个拷贝。</p><p>该技术的主要思想是：只有需要写入时，数据才会被复制，对应的页才会被拷贝，这之后各个进程将拥有各自的拷贝。而在这之前，只是<u>以只读方式共享</u>。</p><p>copy-on-write 是一个很好的优化技术。但是在作者写这本书时，脏牛漏洞(dirty cow)还没有被发现，所以我们同时也要注意到这些技术的缺陷。</p><h3 id="fork">fork()</h3><p>Linux通过 <code>clone()</code> 系统调用来实现 <code>fork()</code> ，需要指明一系列的参数（父子进程、需要共享的资源）。大致流程是：</p><ul><li><p><code>fork()</code>, <code>vfork()</code> 和 <code>__clone()</code> 根据各自需要的参数标志去调用 <code>clone()</code> ；</p><ul><li><p>当执行 <code>fork()</code>, <code>vfork()</code> 系统调用后，程序陷入内核，调用：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">PTREGSCALL0(fork)</span><br><span class="line">PTREGSCALL0(vfork)</span><br></pre></td></tr></table></figure></li><li><p>关于 <code>PTREGSCALL0()</code> ：将 <code>esp+4</code> 的值保存到 <code>eax</code> 寄存器，然后跳转到 <code>sys_fork()</code> 或 <code>sys_vfork()</code> 。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* </span></span><br><span class="line"><span class="comment"> * System calls that need a pt_regs pointer.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> PTREGSCALL0(name) \</span></span><br><span class="line"><span class="meta">ALIGN; \</span></span><br><span class="line"><span class="meta">ptregs_##name: \</span></span><br><span class="line"><span class="meta">leal 4(%esp),%eax; \</span></span><br><span class="line"><span class="meta">jmp sys_##name;</span></span><br></pre></td></tr></table></figure></li><li><p>下面以 <code>sys_fork()</code> 为例：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">sys_fork</span><span class="params">(struct pt_regs *regs)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">return</span> do_fork(SIGCHLD, regs-&gt;sp, regs, <span class="number">0</span>, <span class="literal">NULL</span>, <span class="literal">NULL</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul></li><li><p><code>clone()</code> 调用 <code>do_fork()</code> 。</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">  </span><br></pre></td></tr></table></figure></li><li><p><code>do_fork()</code> 在 <code>kernel/fork.c</code> 中定义，它完成了创建进程的大部分工作。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> *  Ok, this is the main fork-routine.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * It copies the process, and if successful kick-starts</span></span><br><span class="line"><span class="comment"> * it and waits for it to finish using the VM if required.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">long</span> <span class="title">do_fork</span><span class="params">(<span class="keyword">unsigned</span> <span class="keyword">long</span> clone_flags,</span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="keyword">unsigned</span> <span class="keyword">long</span> stack_start,</span></span></span><br><span class="line"><span class="params"><span class="function">      struct pt_regs *regs,</span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="keyword">unsigned</span> <span class="keyword">long</span> stack_size,</span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="keyword">int</span> __user *parent_tidptr,</span></span></span><br><span class="line"><span class="params"><span class="function">      <span class="keyword">int</span> __user *child_tidptr)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">p</span>;</span></span><br><span class="line"><span class="keyword">int</span> trace = <span class="number">0</span>;</span><br><span class="line"><span class="keyword">long</span> nr;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Do some preliminary argument and permissions checking before we</span></span><br><span class="line"><span class="comment"> * actually start allocating stuff</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">if</span> (clone_flags &amp; CLONE_NEWUSER) &#123;</span><br><span class="line"><span class="keyword">if</span> (clone_flags &amp; CLONE_THREAD)</span><br><span class="line"><span class="keyword">return</span> -EINVAL;</span><br><span class="line"><span class="comment">/* hopefully this check will go away when userns support is</span></span><br><span class="line"><span class="comment"> * complete</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">if</span> (!capable(CAP_SYS_ADMIN) || !capable(CAP_SETUID) ||</span><br><span class="line">!capable(CAP_SETGID))</span><br><span class="line"><span class="keyword">return</span> -EPERM;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * We hope to recycle these flags after 2.6.26</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">if</span> (unlikely(clone_flags &amp; CLONE_STOPPED)) &#123;</span><br><span class="line"><span class="keyword">static</span> <span class="keyword">int</span> __read_mostly count = <span class="number">100</span>;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (count &gt; <span class="number">0</span> &amp;&amp; printk_ratelimit()) &#123;</span><br><span class="line"><span class="keyword">char</span> comm[TASK_COMM_LEN];</span><br><span class="line"></span><br><span class="line">count--;</span><br><span class="line">printk(KERN_INFO <span class="string">&quot;fork(): process `%s&#x27; used deprecated &quot;</span></span><br><span class="line"><span class="string">&quot;clone flags 0x%lx\n&quot;</span>,</span><br><span class="line">get_task_comm(comm, current),</span><br><span class="line">clone_flags &amp; CLONE_STOPPED);</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * When called from kernel_thread, don&#x27;t do user tracing stuff.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">if</span> (likely(user_mode(regs)))</span><br><span class="line">trace = tracehook_prepare_clone(clone_flags);</span><br><span class="line"></span><br><span class="line">p = copy_process(clone_flags, stack_start, regs, stack_size,</span><br><span class="line"> child_tidptr, <span class="literal">NULL</span>, trace);</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Do this prior waking up the new thread - the thread pointer</span></span><br><span class="line"><span class="comment"> * might get invalid after that point, if the thread exits quickly.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="keyword">if</span> (!IS_ERR(p)) &#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">completion</span> <span class="title">vfork</span>;</span></span><br><span class="line"></span><br><span class="line">trace_sched_process_fork(current, p);</span><br><span class="line"></span><br><span class="line">nr = task_pid_vnr(p);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (clone_flags &amp; CLONE_PARENT_SETTID)</span><br><span class="line">put_user(nr, parent_tidptr);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (clone_flags &amp; CLONE_VFORK) &#123;</span><br><span class="line">p-&gt;vfork_done = &amp;vfork;</span><br><span class="line">init_completion(&amp;vfork);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">audit_finish_fork(p);</span><br><span class="line">tracehook_report_clone(regs, clone_flags, nr, p);</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * We set PF_STARTING at creation in case tracing wants to</span></span><br><span class="line"><span class="comment"> * use this to distinguish a fully live task from one that</span></span><br><span class="line"><span class="comment"> * hasn&#x27;t gotten to tracehook_report_clone() yet.  Now we</span></span><br><span class="line"><span class="comment"> * clear it and set the child going.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">p-&gt;flags &amp;= ~PF_STARTING;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (unlikely(clone_flags &amp; CLONE_STOPPED)) &#123;</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * We&#x27;ll start up with an immediate SIGSTOP.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">sigaddset(&amp;p-&gt;pending.signal, SIGSTOP);</span><br><span class="line">set_tsk_thread_flag(p, TIF_SIGPENDING);</span><br><span class="line">__set_task_state(p, TASK_STOPPED);</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">wake_up_new_task(p, clone_flags);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">tracehook_report_clone_complete(trace, regs,</span><br><span class="line">clone_flags, nr, p);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (clone_flags &amp; CLONE_VFORK) &#123;</span><br><span class="line">freezer_do_not_count();</span><br><span class="line">wait_for_completion(&amp;vfork);</span><br><span class="line">freezer_count();</span><br><span class="line">tracehook_report_vfork_done(p, nr);</span><br><span class="line">&#125;</span><br><span class="line">&#125; <span class="keyword">else</span> &#123;</span><br><span class="line">nr = PTR_ERR(p);</span><br><span class="line">&#125;</span><br><span class="line"><span class="keyword">return</span> nr;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>其中，<code>do_fork()</code> 调用了 <code>copy_process()</code> 函数。</p></li><li><p><code>copy_process()</code> 主要完成的工作有：</p><ul><li><p>调用 <code>dup_task_struct()</code> 为新进程创建一个内核栈、 thread_info 结构和 task_struct ，这些值与当前进程相同。此时，父子进程的描述符是完全相同的。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* This creates a new process as a copy of the old one, but does not actually start it yet. </span></span><br><span class="line"><span class="comment">It copies the registers, and all the appropriate parts of the process environment (as per the clone flags). The actual kick-off is left to the caller.*/</span></span><br><span class="line">p = dup_task_struct(current);</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Allocate a return stack for newly created task */</span></span><br><span class="line">ftrace_graph_init_task(p);</span><br><span class="line"></span><br></pre></td></tr></table></figure></li><li><p>调用 <code>copy_creds()</code> 为新进程复制进程凭据，即credential。</p></li><li><p>检查并确保在创建新的子进程后，当前用户所拥有的进程数目没有超出分配的资源上限。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (nr_threads &gt;= max_threads)</span><br><span class="line"><span class="keyword">goto</span> bad_fork_cleanup_count;</span><br></pre></td></tr></table></figure></li><li><p>在确保新进程能够合法创建后，子进程着手使自己与父进程区分开来。其 <code>task_struct</code> 内一些成员需要初始化，但大多数数据都未被修改。</p></li><li><p>而后，子进程的状态被设置为 <code>TASK_UNINTERRUPTIBLE</code> ，以保证该进程不会投入运行。</p></li><li><p>调用 <code>copy_flags()</code> 来更新 <code>task_struct</code> 的成员 <code>flags</code> ，表明进程是否拥有超级用户权限的 <code>PF_SUPERPRIV</code> 标志被清零，表明进程还没有调用 <code>exec()</code> 函数的 <code>PF_FORKNOEXEC</code> 标志被设置，表明进程准备被创建的<code>PF_STARTING</code> 标志被设置。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">void</span> <span class="title">copy_flags</span><span class="params">(<span class="keyword">unsigned</span> <span class="keyword">long</span> clone_flags, struct task_struct *p)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="keyword">unsigned</span> <span class="keyword">long</span> new_flags = p-&gt;flags;</span><br><span class="line"></span><br><span class="line">new_flags &amp;= ~PF_SUPERPRIV;</span><br><span class="line">new_flags |= PF_FORKNOEXEC;</span><br><span class="line">new_flags |= PF_STARTING;</span><br><span class="line">p-&gt;flags = new_flags;</span><br><span class="line">clear_freeze_flag(p);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>在进行许多检查合法性措施后，调用 <code>alloc_pid()</code> 为新进程分配一个有效的 <code>PID</code> 。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">pid = alloc_pid(p-&gt;nsproxy-&gt;pid_ns);</span><br></pre></td></tr></table></figure></li><li><p>根据传递给 <code>clone()</code> 的参数标志 <code>clone_flags</code> ，<code>copy_progress()</code> 拷贝或共享打开的文件、文件系统信息、信号处理函数、进程地址空间和命名空间等。一般情况下，这些资源会被给定进程的所有线程共享。否则，这些资源对每个进程是不同的，因而被拷贝到这里。</p></li><li><p>最后，<code>copy_progress()</code> 做扫尾工作，并返回一个指向子进程的指针。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">total_forks++;</span><br><span class="line">spin_unlock(&amp;current-&gt;sighand-&gt;siglock);</span><br><span class="line">write_unlock_irq(&amp;tasklist_lock);</span><br><span class="line">proc_fork_connector(p);</span><br><span class="line">cgroup_post_fork(p);</span><br><span class="line">perf_event_fork(p);</span><br><span class="line"><span class="keyword">return</span> p;</span><br></pre></td></tr></table></figure></li></ul></li><li><p>回到 <code>do_fork()</code> 后，如果 <code>copy_process()</code> 返回成功，新创建的子进程会被唤醒并投入运行，并且内核有意选择子进程首先执行，因为如果父进程首先执行的话，可能会开始向地址空间进行写操作，而子进程一般会马上调用<code>exec()</code> ，这样可以避免 copy-on-write 带来的开销。但子进程并非总能被首先执行。</p></li></ul><h3 id="vfork">vfork()</h3><p>除了不拷贝父进程的页表向外，<code>vfork()</code> 和 <code>fork()</code> 的功能相同。子进程作为父进程的一个单独线程在它的地址空间里运行，父进程被阻塞时，直到子进程退出或执行 <code>exec()</code> 。子进程不能向不能先地址空间中写入。</p><p>理想情况下，不要使用 <code>vfork()</code> 。</p><h2 id="线程实现">线程实现</h2><p>线程机制提供了在<u>同一程序</u>内<u>共享内存地址空间</u>运行的<u>一组线程</u>，它们支持并发程序设计技术(concurrent programming) ，在多处理器系统上也能保持真正的并行处理(parallelism)。</p><p>Linux的内核不知道线程的存在，线程仅仅被视为一个<u>与其他进程共享某些资源的进程</u>，它们拥有自己唯一的 <code>task_struct</code> 。而诸如 Microsoft Windows 和或是 Sun Solaris 这类冲动做系统，在内核中专门支持线程的机制（通常把这类线程称作轻量级进程(lightweight processes)）。</p><h3 id="创建线程">创建线程</h3><p>Linux内核对线程的视角就注定了内核在创建线程时与创建进程的过程大同小异。只不过在调用 <code>clone()</code> 的时候需要传递一些<u>参数标志</u>来指明需要共享的资源。</p><p>在 <code>include/linux/sched.h</code> 中，定义了 <code>clone()</code> 的参数标志。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * cloning flags:</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CSIGNAL0x000000ff<span class="comment">/* signal mask to be sent at exit */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_VM0x00000100<span class="comment">/* set if VM shared between processes */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_FS0x00000200<span class="comment">/* set if fs info shared between processes */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_FILES0x00000400<span class="comment">/* set if open files shared between processes */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_SIGHAND0x00000800<span class="comment">/* set if signal handlers and blocked signals shared */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_PTRACE0x00002000<span class="comment">/* set if we want to let tracing continue on the child too */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_VFORK0x00004000<span class="comment">/* set if the parent wants the child to wake it up on mm_release */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_PARENT0x00008000<span class="comment">/* set if we want to have the same parent as the cloner */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_THREAD0x00010000<span class="comment">/* Same thread group? */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_NEWNS0x00020000<span class="comment">/* New namespace group? */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_SYSVSEM0x00040000<span class="comment">/* share system V SEM_UNDO semantics */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_SETTLS0x00080000<span class="comment">/* create a new TLS for the child */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_PARENT_SETTID0x00100000<span class="comment">/* set the TID in the parent */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_CHILD_CLEARTID0x00200000<span class="comment">/* clear the TID in the child */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_DETACHED0x00400000<span class="comment">/* Unused, ignored */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_UNTRACED0x00800000<span class="comment">/* set if the tracing process can&#x27;t force CLONE_PTRACE on this clone */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_CHILD_SETTID0x01000000<span class="comment">/* set the TID in the child */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_STOPPED0x02000000<span class="comment">/* Start in stopped state */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_NEWUTS0x04000000<span class="comment">/* New utsname group? */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_NEWIPC0x08000000<span class="comment">/* New ipcs */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_NEWUSER0x10000000<span class="comment">/* New user namespace */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_NEWPID0x20000000<span class="comment">/* New pid namespace */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_NEWNET0x40000000<span class="comment">/* New network namespace */</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> CLONE_IO0x80000000<span class="comment">/* Clone io context */</span></span></span><br></pre></td></tr></table></figure><p>对于 <code>fork()</code> 而言，如下代码产生的结果相似，只是父子进程共向地址空间、文件系统资源、文件描述符和信号处理程序。换而言之，子进程和它的父进程就是所谓的线程。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line">clone(CLONE_VM | CLONE_FS | CLONE_FILES | CLONE_SIGHAND, <span class="number">0</span>);</span><br></pre></td></tr></table></figure><p>而对于 <code>vfork()</code> 而言，实现相似结果的是：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">clone(CLONE_VFORK | CLONE_VM | SIGCHLD, 0);</span><br></pre></td></tr></table></figure><h3 id="内核线程">内核线程</h3><p>内核经常需要在后台执行操作，这类型的任务可以通过内核线程(kernel thread)完成。它们是独立运行在内和空间的标准进程，它们没有独立的地址空间，即指向地址空间的 <code>mm</code> 指针被设置为 <code>NULL</code> 。除了它们无法到用户空间去之外，和普通进程一样。</p><p>内核线程只能由其他内核线程创建。内核通过 <code>kthread</code> 内核进程来衍生出所有新的内核线程，其接口在 <code>kernel/kthread.c</code> 中有定义。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * kthread_create - create a kthread.</span></span><br><span class="line"><span class="comment"> * @threadfn: the function to run until signal_pending(current).</span></span><br><span class="line"><span class="comment"> * @data: data ptr for @threadfn.</span></span><br><span class="line"><span class="comment"> * @namefmt: printf-style name for the thread.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Description: This helper function creates and names a kernel</span></span><br><span class="line"><span class="comment"> * thread.  The thread will be stopped: use wake_up_process() to start</span></span><br><span class="line"><span class="comment"> * it.  See also kthread_run().</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * When woken, the thread will run @threadfn() with @data as its</span></span><br><span class="line"><span class="comment"> * argument. @threadfn() can either call do_exit() directly if it is a</span></span><br><span class="line"><span class="comment"> * standalone thread for which noone will call kthread_stop(), or</span></span><br><span class="line"><span class="comment"> * return when &#x27;kthread_should_stop()&#x27; is true (which means</span></span><br><span class="line"><span class="comment"> * kthread_stop() has been called).  The return value should be zero</span></span><br><span class="line"><span class="comment"> * or a negative error number; it will be passed to kthread_stop().</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Returns a task_struct or ERR_PTR(-ENOMEM).</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function">struct task_struct *<span class="title">kthread_create</span><span class="params">(<span class="keyword">int</span> (*threadfn)(<span class="keyword">void</span> *data),</span></span></span><br><span class="line"><span class="params"><span class="function">   <span class="keyword">void</span> *data,</span></span></span><br><span class="line"><span class="params"><span class="function">   <span class="keyword">const</span> <span class="keyword">char</span> namefmt[],</span></span></span><br><span class="line"><span class="params"><span class="function">   ...)</span></span></span><br></pre></td></tr></table></figure><p>新的任务是由 <code>kthread</code> 内核进程通过 <code>clone()</code> 系统调用来创建的。通过注释可以知道，新进程将运行 <code>threadfn</code> 函数，传递的参数是 <code>data</code> ，进程被命名为 <code>namefmt</code> 。</p><p>新进程出于不可运行状态，如果不调用 <code>wake_up_process()</code> 去唤醒，那么它不会主动运行。可以直接通过 <code>kthread_run()</code> 来创建并运行进程，它只是简单调用了 <code>kthread_create()</code> 和 <code>wake_up_process()</code> 。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * kthread_run - create and wake a thread.</span></span><br><span class="line"><span class="comment"> * @threadfn: the function to run until signal_pending(current).</span></span><br><span class="line"><span class="comment"> * @data: data ptr for @threadfn.</span></span><br><span class="line"><span class="comment"> * @namefmt: printf-style name for the thread.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Description: Convenient wrapper for kthread_create() followed by</span></span><br><span class="line"><span class="comment"> * wake_up_process().  Returns the kthread or ERR_PTR(-ENOMEM).</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> kthread_run(threadfn, data, namefmt, ...)   \</span></span><br><span class="line"><span class="meta">(&#123;   \</span></span><br><span class="line"><span class="meta">struct task_struct *__k   \</span></span><br><span class="line"><span class="meta">= kthread_create(threadfn, data, namefmt, ## __VA_ARGS__); \</span></span><br><span class="line"><span class="meta"><span class="meta-keyword">if</span> (!IS_ERR(__k))   \</span></span><br><span class="line"><span class="meta">wake_up_process(__k);   \</span></span><br><span class="line"><span class="meta">__k;   \</span></span><br><span class="line"><span class="meta">&#125;)</span></span><br></pre></td></tr></table></figure><p>内核线程启动后就一直运行，直到调用 <code>do_exit()</code> 或者内核其他部分调用了 <code>kthread_stop()</code> 时退出，传递给 <code>kthread_stop()</code> 的参数为 <code>kthread_create()</code> 返回的 <code>task_struct</code> 结构的地址。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">/**</span></span><br><span class="line"><span class="comment"> * kthread_stop - stop a thread created by kthread_create().</span></span><br><span class="line"><span class="comment"> * @k: thread created by kthread_create().</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Sets kthread_should_stop() for @k to return true, wakes it, and</span></span><br><span class="line"><span class="comment"> * waits for it to exit. This can also be called after kthread_create()</span></span><br><span class="line"><span class="comment"> * instead of calling wake_up_process(): the thread will exit without</span></span><br><span class="line"><span class="comment"> * calling threadfn().</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * If threadfn() may call do_exit() itself, the caller must ensure</span></span><br><span class="line"><span class="comment"> * task_struct can&#x27;t go away.</span></span><br><span class="line"><span class="comment"> *</span></span><br><span class="line"><span class="comment"> * Returns the result of threadfn(), or %-EINTR if wake_up_process()</span></span><br><span class="line"><span class="comment"> * was never called.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">kthread_stop</span><span class="params">(struct task_struct *k)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">kthread</span> *<span class="title">kthread</span>;</span></span><br><span class="line"><span class="keyword">int</span> ret;</span><br><span class="line"></span><br><span class="line">trace_sched_kthread_stop(k);</span><br><span class="line">get_task_struct(k);</span><br><span class="line"></span><br><span class="line">kthread = to_kthread(k);</span><br><span class="line">barrier(); <span class="comment">/* it might have exited */</span></span><br><span class="line"><span class="keyword">if</span> (k-&gt;vfork_done != <span class="literal">NULL</span>) &#123;</span><br><span class="line">kthread-&gt;should_stop = <span class="number">1</span>;</span><br><span class="line">wake_up_process(k);</span><br><span class="line">wait_for_completion(&amp;kthread-&gt;exited);</span><br><span class="line">&#125;</span><br><span class="line">ret = k-&gt;exit_code;</span><br><span class="line"></span><br><span class="line">put_task_struct(k);</span><br><span class="line">trace_sched_kthread_stop_ret(ret);</span><br><span class="line"></span><br><span class="line"><span class="keyword">return</span> ret;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="进程终结">进程终结</h2><p>当一个进程终结时，内核必须释放它所占有的资源，并告知父进程。</p><p>一般，进程的析构是自身引起的，在调用 <code>exit()</code> 时触发。大部分任务都要靠 <code>do_exit()</code> 来完成，该函数没有返回值，在 <code>kernel/exit.c</code> 中定义。</p><p>该函数内容如下：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function">NORET_TYPE <span class="keyword">void</span> <span class="title">do_exit</span><span class="params">(<span class="keyword">long</span> code)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"><span class="class"><span class="keyword">struct</span> <span class="title">task_struct</span> *<span class="title">tsk</span> =</span> current;</span><br><span class="line"><span class="keyword">int</span> group_dead;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* ... */</span></span><br><span class="line">    </span><br><span class="line">exit_irq_thread();  <span class="comment">/* irq_thread exits */</span></span><br><span class="line"></span><br><span class="line">exit_signals(tsk);  <span class="comment">/* sets PF_EXITING */</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * tsk-&gt;flags are checked in the futex code to protect against</span></span><br><span class="line"><span class="comment"> * an exiting task cleaning up the robust pi futexes.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">smp_mb();</span><br><span class="line">raw_spin_unlock_wait(&amp;tsk-&gt;pi_lock);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (unlikely(in_atomic()))</span><br><span class="line">printk(KERN_INFO <span class="string">&quot;note: %s[%d] exited with preempt_count %d\n&quot;</span>,</span><br><span class="line">current-&gt;comm, task_pid_nr(current),</span><br><span class="line">preempt_count());</span><br><span class="line"></span><br><span class="line">acct_update_integrals(tsk);</span><br><span class="line"><span class="comment">/* sync mm&#x27;s RSS info before statistics gathering */</span></span><br><span class="line"><span class="keyword">if</span> (tsk-&gt;mm)</span><br><span class="line">sync_mm_rss(tsk, tsk-&gt;mm);</span><br><span class="line">group_dead = atomic_dec_and_test(&amp;tsk-&gt;signal-&gt;live);</span><br><span class="line"><span class="keyword">if</span> (group_dead) &#123;</span><br><span class="line">hrtimer_cancel(&amp;tsk-&gt;signal-&gt;real_timer);</span><br><span class="line">exit_itimers(tsk-&gt;signal);</span><br><span class="line"><span class="keyword">if</span> (tsk-&gt;mm)</span><br><span class="line">setmax_mm_hiwater_rss(&amp;tsk-&gt;signal-&gt;maxrss, tsk-&gt;mm);</span><br><span class="line">&#125;</span><br><span class="line">acct_collect(code, group_dead);</span><br><span class="line"><span class="keyword">if</span> (group_dead)</span><br><span class="line">tty_audit_exit();</span><br><span class="line"><span class="keyword">if</span> (unlikely(tsk-&gt;audit_context))</span><br><span class="line">audit_free(tsk);</span><br><span class="line"></span><br><span class="line">tsk-&gt;exit_code = code;</span><br><span class="line">taskstats_exit(tsk, group_dead);</span><br><span class="line"></span><br><span class="line">exit_mm(tsk);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (group_dead)</span><br><span class="line">acct_process();</span><br><span class="line">trace_sched_process_exit(tsk);</span><br><span class="line"></span><br><span class="line">exit_sem(tsk);</span><br><span class="line">exit_files(tsk);</span><br><span class="line">exit_fs(tsk);</span><br><span class="line">check_stack_usage();</span><br><span class="line">exit_thread();</span><br><span class="line">cgroup_exit(tsk, <span class="number">1</span>);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (group_dead)</span><br><span class="line">disassociate_ctty(<span class="number">1</span>);</span><br><span class="line"></span><br><span class="line">module_put(task_thread_info(tsk)-&gt;exec_domain-&gt;<span class="keyword">module</span>);</span><br><span class="line"></span><br><span class="line">proc_exit_connector(tsk);</span><br><span class="line"></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * <span class="doctag">FIXME:</span> do that only when needed, using sched_exit tracepoint</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">flush_ptrace_hw_breakpoint(tsk);</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Flush inherited counters to the parent - before the parent</span></span><br><span class="line"><span class="comment"> * gets woken up by child-exit notifications.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">perf_event_exit_task(tsk);</span><br><span class="line"></span><br><span class="line">exit_notify(tsk, group_dead);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> CONFIG_NUMA</span></span><br><span class="line">mpol_put(tsk-&gt;mempolicy);</span><br><span class="line">tsk-&gt;mempolicy = <span class="literal">NULL</span>;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">ifdef</span> CONFIG_FUTEX</span></span><br><span class="line"><span class="keyword">if</span> (unlikely(current-&gt;pi_state_cache))</span><br><span class="line">kfree(current-&gt;pi_state_cache);</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span></span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * Make sure we are holding no locks:</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">debug_check_no_locks_held(tsk);</span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment"> * We can do this unlocked here. The futex code uses this flag</span></span><br><span class="line"><span class="comment"> * just to verify whether the pi state cleanup has been done</span></span><br><span class="line"><span class="comment"> * or not. In the worst case it loops once more.</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line">tsk-&gt;flags |= PF_EXITPIDONE;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (tsk-&gt;io_context)</span><br><span class="line">exit_io_context(tsk);</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span> (tsk-&gt;splice_pipe)</span><br><span class="line">__free_pipe_info(tsk-&gt;splice_pipe);</span><br><span class="line"></span><br><span class="line">validate_creds_for_do_exit(tsk);</span><br><span class="line"></span><br><span class="line">preempt_disable();</span><br><span class="line">exit_rcu();</span><br><span class="line"><span class="comment">/* causes final put_task_struct in finish_task_switch(). */</span></span><br><span class="line">tsk-&gt;state = TASK_DEAD;</span><br><span class="line">schedule();</span><br><span class="line">BUG();</span><br><span class="line"><span class="comment">/* Avoid &quot;noreturn function does return&quot;.  */</span></span><br><span class="line"><span class="keyword">for</span> (;;)</span><br><span class="line">cpu_relax();<span class="comment">/* For when BUG is null */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>从上述代码中可以分析得到，<code>do_exit()</code> 做了以下大致的工作：</p><ul><li><p>调用 <code>exit_signals()</code> 将 <code>task_struct</code> 中的 <code>flags</code> 成员设置为 <code>PF_EXITING</code>；</p></li><li><p>调用 <code>del_timer_sync()</code> 删除任一内核定时器，根据返回结果确保没有定时器在排队，也没有定时器处理程序在运行；</p></li><li><p>如果 BSD 的进程记账功能是开启的，则调用 <code>acct_update_integrals()</code> 来输出记账信息；</p></li><li><p>然后调用 <code>exit_mm()</code> 函数来释放进程占用的 <code>mm_struct</code>，如果没有别的进程使用它们，就将它们彻底释放。</p></li><li><p>接着调用 <code>sem__exit()</code> 函数，如果进程排队等候 IPC 信号，则它将离开队列。</p></li><li><p>调用 <code>exit_files()</code>  和 <code>eixt_fs()</code> 函数，分别递减文件描述符和文件系统数据的引用计数。如果某个数值降为零，则代表没有进程在使用相应的资源，此时可以释放。</p></li><li><p>把存放在 <code>task_struct</code> 的 <code>exit_code</code> 成员中的任务退出代码置为由 <code>exit()</code> 提供的退出代码，或者去完成任何其他由内核机制规定的退出动作。退出代码存放在这里供 父进程随时检索。</p></li><li><p>调用 <code>exit_notify()</code> 向父进程发送信号，给子进程重新寻找养父，养父为线程组中的其他线程或者为 <code>init</code> 线程，把进程状态（存放在 <code>task_struct</code> 结构的 <code>exit_state</code> 中）设成 <code>EXIT_ZOMBIE</code> 。</p></li><li><p><code>do_exit()</code> 调用 <code>schedule()</code> 切换到新的进程，出于 <code>EXIT_ZOMBIE</code> 的进程不会再被调度，所以这是程序执行到最后一段代码。</p></li></ul><h3 id="删除进程描述符">删除进程描述符</h3>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;进程管理&lt;/h1&gt;
&lt;p&gt;操作系统为用户程序服务，因此，进程管理是所有操作系统的心脏所在。&lt;/p&gt;
&lt;h2 id=&quot;进程&quot;&gt;进程&lt;/h2&gt;
&lt;p&gt;进程是处于执行期的程序及其相关的资源的总称，它包含其他资源（如打开的文件、挂起的信号等），内核需要有效、透明地管理所有细节。进</summary>
      
    
    
    
    <category term="study" scheme="https://www.jk404.cn/categories/study/"/>
    
    <category term="book" scheme="https://www.jk404.cn/categories/study/book/"/>
    
    <category term="Linux Kernel Development" scheme="https://www.jk404.cn/categories/study/book/Linux-Kernel-Development/"/>
    
    
  </entry>
  
  <entry>
    <title>Linux_kernel_development_02_kernel_intro</title>
    <link href="https://www.jk404.cn/study/book/Linux-Kernel-Development/Linux-kernel-development-02-kernel-intro/"/>
    <id>https://www.jk404.cn/study/book/Linux-Kernel-Development/Linux-kernel-development-02-kernel-intro/</id>
    <published>2022-12-18T04:21:37.000Z</published>
    <updated>2022-12-22T10:27:15.946Z</updated>
    
    <content type="html"><![CDATA[<h1>从内核出发</h1><p>第二章介绍内核源码获取、编译、安装以及内核程序和用户态程序的差别。</p><h2 id="获取内核源码">获取内核源码</h2><p><a href="http://www.kernel.org">http://www.kernel.org</a></p><p><a href="https://mirrors.edge.kernel.org/pub/linux/kernel/">https://mirrors.edge.kernel.org/pub/linux/kernel/</a></p><ul><li><p>使用Git：git clone git://git.kernel.org/pub/scm/linux/kernel/git/torvalds/linux.git</p></li><li><p>安装内核源代码：<code>tar xvjf linux-x.y.z.tar.bz2</code> or <code>tar xvzf linux-x.y.z.tar.gz</code></p><p>内核源码一般安装在<code>/usr/src/linux</code>下，，不要把这个源码树用于开发，因为编译用的内核版本就链接到这棵树。不要用root身份对内核进行修改，应当建立自己的主目录，仅以root身份安装内核，且安装新内核时，<code>/usr/src/linux</code>目录都应当原封不动。</p></li><li><p>使用补丁：<code>patch -p1 &lt; ../patch-x.y.z</code></p></li></ul><h2 id="内核源码树">内核源码树</h2><ul><li><code>arch</code> - 特定体系结构的源码</li><li><code>block</code> - 块设备I/O层</li><li><code>crypto</code> - 加密API</li><li><code>Documentation</code> - 内核源码文档</li><li><code>drivers</code> - 设备驱动程序</li><li><code>firmware</code> - 使用某些驱动程序而需要的设备固件</li><li><code>fs</code> - VFS和各种文件系统</li><li><code>include</code> - 内核头文件</li><li><code>init</code> - 内核引导和初始化</li><li><code>ipc</code> - 进程间通信代码</li><li><code>kernel</code> - 核心子系统</li><li><code>lib</code> - 通用内核函数</li><li><code>mm</code> - 内存管理子系统和VM</li><li><code>net</code> - 网络子系统</li><li><code>samples</code> - 示例代码</li><li><code>scripts</code> - 编译内核所用的脚本</li><li><code>security</code> - 安全模块</li><li><code>sound</code> - 语音子系统</li><li><code>usr</code> - 早期用户空间代码，所谓的initramfs</li><li><code>tools</code> - 开发工具</li><li><code>virt</code> - 虚拟化基础结构</li></ul><h2 id="编译内核">编译内核</h2><h3 id="内核配置">内核配置</h3><p>可以配置的各种选项，以<code>CONFIG_FEATURE</code>形式表示，其前缀为<code>CONFIG</code>。</p><p>选项的选择一般有两种：</p><ul><li><code>yes</code> or <code>no</code>：yes表示把代码编译进主内核印象中，而不是作为模块</li><li><code>yes</code> or <code>no</code> or <code>module</code>：驱动程序一般都用三选一的配置项，module意味着配置项被选定了，以独立代码段的模块形式编译安装。</li></ul><p>内核提供了各种不同的工具，用于简化对内核的配置：</p><ul><li><code>make config</code> - 一种耗费巨大时间的对选项逐一进行配置的方式。</li><li><code>make menuconfig</code>  - 基于 <code>ncurse</code> 库的图形界面工具。</li><li><code>make gconfig</code> - 基于 <code>gtk+</code> 的图形工具。</li><li><code>make defconfig</code>  - 默认配置</li></ul><p>配置项存放在根目录的 <code>.config</code> 中，在修改了配置后，在编译内核之前都应当执行 <code>make oldconfig</code> 进行验证和更新。</p><p>配置选项 <code>CONFIG_IKCONFIG_PROC</code> 把完整的压缩过的内核配置文件存放在 <code>/proc/config.gz</code> 下，当编译一个新内核时可以克隆当前配置。如果已启用，则可从 <code>/proc</code> 下复制出配置文件并且使用它来编译一个新内核：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">zcat /proc/config.gz &gt; .config    <span class="comment"># 克隆当前配置</span></span><br><span class="line">make oldconfig</span><br></pre></td></tr></table></figure><p>一旦内核配置完成，使用命令 <code>make</code> 进行编译。</p><h3 id="减少编译的垃圾信息">减少编译的垃圾信息</h3><p>对输出进行重定向：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">make &gt; .. /detritus</span><br></pre></td></tr></table></figure><p>或者是把无用的输出信息重定向到永无返回值的黑洞 <code>/dev/null</code> :</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">make &gt; /dev/null</span><br></pre></td></tr></table></figure><h3 id="衍生多个编译作业">衍生多个编译作业</h3><p>默认情况下， make 只衍生一个作业，因为 Makefiles 经常会出现不正确的依赖关系，导致编译出错。可以以多个作业编译内核：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">make -j&lt;n&gt;    <span class="comment"># n 是要衍生出的作业数，实际上单核处理器一般衍生出一个或两个作业</span></span><br></pre></td></tr></table></figure><p>另外，利用 <code>distcc</code> 或者 <code>ccache</code> 工具也可以动态改善内核编译的时间。</p><h2 id="安装新内核">安装新内核</h2><p>内核编译后需要安装，而安装和体系结构、启动引导工具(<code>bootloader</code>)相关。查阅 <code>bootloader</code> 的说明，按照其指示将内核映像拷贝到指定位置，按照启动要求安装它。</p><p>一定保证随时有一个或两个可以启动的内核，以免新编译的内核出问题。</p><p>【例子】 在使用 <code>grub</code> 的 <code>x86</code> 系统上，可能需要把 <code>arch/i386/boot/bzImage</code> 拷贝到 <code>/boot</code> 目录下，像 <code>vmlinuz-version</code> 这样命名它，然后编辑 <code>/etc/grub/grub.conf</code> 文件，为内核新建一个新的启动项。 使用 <code>LILO</code> 启动的系统应当编辑 <code>/etc/lilo.conf</code> ，然后运行 lilo。</p><p>模块的安装只需要用root身份执行：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">make modules_install</span><br></pre></td></tr></table></figure><p>将已编译的模块安装到主目录 <code>/lib/modules</code> 下。</p><p>编译时，根目录下出现 <code>System.map</code> 文件，这是一份符号对照表，用以将内核符号和它们的起始地址对应起来，方便调试。</p><h2 id="内核开发的特点">内核开发的特点</h2><ul><li>不能访问C库和标准的C头文件；</li><li>必须使用GNU C；</li><li>缺乏用户空间的内存保护机制；</li><li>难以执行浮点运算；</li><li>每个进程只有一个很小的定长堆栈；</li><li>内核支持异步中断、抢占和 <code>SMP</code> ，要时刻注意同步和并发；</li><li>要考虑可移植性的重要性；</li></ul><h3 id="不能访问C库和C头文件">不能访问C库和C头文件</h3><p>基本的头文件放置在根目录的 <code>include</code> 文件夹中。</p><p>体系结构相关的头文件在 <code>arch/&lt;architecture&gt;/include/asm</code> 目录下。</p><p>【例子】在内核中，没有 <code>printf()</code> 函数，取而代之的是 <code>printk()</code> 。它们之间的一个显著区别是， <code>printk()</code> 允许用一个标志来设置优先级， <code>syslogd</code> 会根据该标志来决定什么地方显示这条消息。例如： <code>printk(KERN_ERR “this is an error!\\n”)</code></p><h3 id="GNU-C">GNU C</h3><p>内核并不完全符合 <code>ANSI C</code> 标准。实际上，只要有可能，内核开发者经常需要用到 <code>gcc</code> 提供的语言扩展功能。 <code>gcc</code> 是多种 <code>GNU</code> 编译器的集合。</p><p>内核代码中的一些C语言扩展：</p><ul><li><p>内联(inline)函数</p><p>直译应该是：“在字里行间展开”。函数执行时会在它所调用的位置上展开，节省了函数调用和返回的开销（函数栈、寄存器存储和恢复）。这么做，还能让编译器把被调用函数(callee)的代码和调用函数(caller)本身一起进行优化。</p><p>但是，内联函数使得代码变长，占用更多内存空间和指令缓存。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">static</span> <span class="keyword">inline</span> <span class="keyword">void</span> <span class="title">foo</span><span class="params">(<span class="keyword">unsigned</span> <span class="keyword">long</span> param)</span></span></span><br></pre></td></tr></table></figure><p>技巧：</p><ul><li>通常把对时间要求较高、代码长度短的函数定义为内联函数，如果代码长度长，会被反复调用，没有特别的时间限制，则不推荐用内联函数；</li><li>通常在头文件中定义内联函数；</li><li>在内核中，优先使用内联函数，不使用复杂的宏；</li></ul></li><li><p>内联汇编</p><p>当知道对应的体系结构时，可以用 <code>asm()</code> 指令嵌入汇编代码。</p><p>【例子】下面的内联汇编代码用于执行 <code>x86</code> 处理器的 <code>rdtsc</code> 指令，返回时间戳  <code>tsc</code> 寄存器的值。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">unsigned</span> <span class="keyword">int</span> low, high;</span><br><span class="line"><span class="function"><span class="keyword">asm</span> <span class="title">volatile</span><span class="params">(<span class="string">&quot;rdtsc&quot;</span> : <span class="string">&quot;=a&quot;</span> (low), <span class="string">&quot;=d&quot;</span> (high))</span></span>;</span><br></pre></td></tr></table></figure><p>在近体系结构的底层或对执行时间有严格要求时会使用内联汇编。</p></li><li><p>分支声明</p><p>使用 <code>likely()</code> 和 <code>unlikely()</code> 来分别标记通常为真和绝少为真的条件分支。</p><p>【例子】</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">if</span> (unlikely(error))&#123;    <span class="comment">/* 认为error绝少为 1 ，大多数时间为 0 */</span></span><br><span class="line"><span class="comment">/* .... */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="keyword">if</span>(ikely(success))&#123;    <span class="comment">/* 认为success通常为 1 ，通常不为 0 */</span></span><br><span class="line"><span class="comment">/* ... */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li></ul><h3 id="无内存保护机制">无内存保护机制</h3><p>如果是用户程序进行一次非法的内存访问，内核会发现这个错误，并发送 <code>SIGSEGV</code> 信号来结束整个进程。但是，若是内核自己非法访问内存，则会导致 <code>oops</code> ，而杀死自己。内核的内存都不分页，所以每用一个字节，物理内存就会减少一个字节。</p><h3 id="不要轻易使用浮点数">不要轻易使用浮点数</h3><p>用户程序使用浮点数时，内核会完成从整数操作到浮点数操作的模式转换，通常捕获陷阱并着手于整数到浮点数的转变。但内核不能完美地支持浮点数操作，因为它本身不能陷入。</p><h3 id="容积小且固定的栈">容积小且固定的栈</h3><p>内核栈大小随体系结构而变。在 x86 上，栈在编译时配置，可以是 4KB 或是 8KB。内核栈的大小是两页，32 位机上是 8KB ，而 64 位机上是 16 KB，这是固定不变的。每个处理器都有自己的栈。</p><h3 id="同步和并发">同步和并发</h3><p>内核很容易发生条件竞争，通常用自旋锁和信号量来解决这些问题。</p><ul><li>Linux 是抢占多任务操作系统，内核的进程调度程序即兴对进程进行调度和重新调度，内核必须和这些任务同步。</li><li>Linux 内核支持对称多处理器系统 (SMP) ，如果没有适当保护，同时在两个或以上的处理器上执行内核代码，可能会同时访问共享的同一个资源。</li><li>中断是异步到来的，不会顾及正在执行的代码。因此，中断可能在代码访问资源的时候到来，这样，中断处理程序可能访问同一资源。</li><li>Linux 内核可以抢占，所以不加以保护的话，内核中一段正在执行的代码可能会被另一段代码抢占，从而导致几段代码同时访问相同的资源。</li></ul><h3 id="可移植性的重要性">可移植性的重要性</h3><p>大部分C代码应当与体系结构无关，在不同体系结构的计算机上要能编译和执行，因此必须把体系结构相关的代码从内核代码树的特定目录中适当分离出来。</p><p>诸如 保持字节序、64位对齐、不假定字长和页面长度 等准则都有助于移植性。</p><h3 id="在线检索源代码">在线检索源代码</h3><p><a href="https://elixir.bootlin.com/linux/v2.6.34/source">https://elixir.bootlin.com/linux/v2.6.34/source</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;从内核出发&lt;/h1&gt;
&lt;p&gt;第二章介绍内核源码获取、编译、安装以及内核程序和用户态程序的差别。&lt;/p&gt;
&lt;h2 id=&quot;获取内核源码&quot;&gt;获取内核源码&lt;/h2&gt;
&lt;p&gt;&lt;a href=&quot;http://www.kernel.org&quot;&gt;http://www.kernel.org</summary>
      
    
    
    
    <category term="study" scheme="https://www.jk404.cn/categories/study/"/>
    
    <category term="book" scheme="https://www.jk404.cn/categories/study/book/"/>
    
    <category term="Linux Kernel Development" scheme="https://www.jk404.cn/categories/study/book/Linux-Kernel-Development/"/>
    
    
  </entry>
  
  <entry>
    <title></title>
    <link href="https://www.jk404.cn/uncategorized/README/"/>
    <id>https://www.jk404.cn/uncategorized/README/</id>
    <published>2022-12-17T17:12:52.781Z</published>
    <updated>2022-12-17T17:12:52.781Z</updated>
    
    <content type="html"><![CDATA[<h1>CS_Learning</h1>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;CS_Learning&lt;/h1&gt;
</summary>
      
    
    
    
    
  </entry>
  
  <entry>
    <title>self-driving_03_Sensing</title>
    <link href="https://www.jk404.cn/study/course/self-driving/self-driving-03-Sensing/"/>
    <id>https://www.jk404.cn/study/course/self-driving/self-driving-03-Sensing/</id>
    <published>2022-11-17T20:00:06.000Z</published>
    <updated>2022-11-28T17:19:02.253Z</updated>
    
    <content type="html"><![CDATA[<h1>感知与分割算法</h1><ul><li>无人驾驶感知系统概述</li><li>静态环境感知与分割算法</li></ul><h2 id="无人驾驶中的感知">无人驾驶中的感知</h2><p>广义上：</p><ul><li>感知外界：<ul><li>外在环境：静态环境、动态物体</li><li>传感器：单目（图片）/双目（深度图/稠密点云）/激光雷达（稀疏点云）/毫米波雷达/超声波雷达</li></ul></li><li>感知自身（定位）：<ul><li>感知自身的运动状态（位置/朝向/速度/…）</li><li>传感器：GPS IMU HDmap SLAM （单目、双目、RGBD、激光雷达、超声波雷达，包括VIO）</li></ul></li></ul><p>四大图像任务：</p><ul><li>分类</li><li>分割（像素分类）</li><li>检测（框分类）</li><li>跟踪（使用物体的一切信息进行时序关联）</li></ul><h3 id="分割">分割</h3><p>Segementation</p><ul><li>实战基于传统方法的车道线检测</li><li>图片分割算法综述</li><li>实战基于图片分割的车道线检测</li></ul><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># 传统方法</span></span><br><span class="line"><span class="keyword">import</span> cv2 <span class="keyword">as</span> cv</span><br><span class="line"><span class="keyword">import</span> numpy <span class="keyword">as</span> np</span><br><span class="line"><span class="comment"># import matplotlib.pyplot as plt</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">do_canny</span>(<span class="params">frame</span>):</span></span><br><span class="line">    <span class="comment"># Converts frame to grayscale because we only need the luminance channel for detecting edges - less computationally expensive</span></span><br><span class="line">    gray = cv.cvtColor(frame, cv.COLOR_RGB2GRAY)</span><br><span class="line">    <span class="comment"># Applies a 5x5 gaussian blur with deviation of 0 to frame - not mandatory since Canny will do this for us</span></span><br><span class="line">    blur = cv.GaussianBlur(gray, (<span class="number">5</span>, <span class="number">5</span>), <span class="number">0</span>)</span><br><span class="line">    <span class="comment"># Applies Canny edge detector with minVal of 50 and maxVal of 150</span></span><br><span class="line">    canny = cv.Canny(blur, <span class="number">50</span>, <span class="number">150</span>)</span><br><span class="line">    <span class="keyword">return</span> canny</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">do_segment</span>(<span class="params">frame</span>):</span></span><br><span class="line">    <span class="comment"># Since an image is a multi-directional array containing the relative intensities of each pixel in the image, we can use frame.shape to return a tuple: [number of rows, number of columns, number of channels] of the dimensions of the frame</span></span><br><span class="line">    <span class="comment"># frame.shape[0] give us the number of rows of pixels the frame has. Since height begins from 0 at the top, the y-coordinate of the bottom of the frame is its height</span></span><br><span class="line">    height = frame.shape[<span class="number">0</span>]</span><br><span class="line">    <span class="comment"># Creates a triangular polygon for the mask defined by three (x, y) coordinates</span></span><br><span class="line">    polygons = np.array([</span><br><span class="line">                            [(<span class="number">0</span>, height), (<span class="number">800</span>, height), (<span class="number">380</span>, <span class="number">290</span>)]</span><br><span class="line">                        ])</span><br><span class="line">    <span class="comment"># Creates an image filled with zero intensities with the same dimensions as the frame</span></span><br><span class="line">    mask = np.zeros_like(frame)</span><br><span class="line">    <span class="comment"># Allows the mask to be filled with values of 1 and the other areas to be filled with values of 0</span></span><br><span class="line">    cv.fillPoly(mask, polygons, <span class="number">255</span>)</span><br><span class="line">    <span class="comment"># A bitwise and operation between the mask and frame keeps only the triangular area of the frame</span></span><br><span class="line">    segment = cv.bitwise_and(frame, mask)</span><br><span class="line">    <span class="keyword">return</span> segment</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">calculate_lines</span>(<span class="params">frame, lines</span>):</span></span><br><span class="line">    <span class="comment"># Empty arrays to store the coordinates of the left and right lines</span></span><br><span class="line">    left = []</span><br><span class="line">    right = []</span><br><span class="line">    <span class="comment"># Loops through every detected line</span></span><br><span class="line">    <span class="keyword">for</span> line <span class="keyword">in</span> lines:</span><br><span class="line">        <span class="comment"># Reshapes line from 2D array to 1D array</span></span><br><span class="line">        x1, y1, x2, y2 = line.reshape(<span class="number">4</span>)</span><br><span class="line">        <span class="comment"># Fits a linear polynomial to the x and y coordinates and returns a vector of coefficients which describe the slope and y-intercept</span></span><br><span class="line">        parameters = np.polyfit((x1, x2), (y1, y2), <span class="number">1</span>)</span><br><span class="line">        slope = parameters[<span class="number">0</span>]</span><br><span class="line">        y_intercept = parameters[<span class="number">1</span>]</span><br><span class="line">        <span class="comment"># If slope is negative, the line is to the left of the lane, and otherwise, the line is to the right of the lane</span></span><br><span class="line">        <span class="keyword">if</span> slope &lt; <span class="number">0</span>:</span><br><span class="line">            left.append((slope, y_intercept))</span><br><span class="line">        <span class="keyword">else</span>:</span><br><span class="line">            right.append((slope, y_intercept))</span><br><span class="line">    <span class="comment"># Averages out all the values for left and right into a single slope and y-intercept value for each line</span></span><br><span class="line">    left_avg = np.average(left, axis = <span class="number">0</span>)</span><br><span class="line">    right_avg = np.average(right, axis = <span class="number">0</span>)</span><br><span class="line">    <span class="comment"># Calculates the x1, y1, x2, y2 coordinates for the left and right lines</span></span><br><span class="line">    left_line = calculate_coordinates(frame, left_avg)</span><br><span class="line">    right_line = calculate_coordinates(frame, right_avg)</span><br><span class="line">    <span class="keyword">return</span> np.array([left_line, right_line])</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">calculate_coordinates</span>(<span class="params">frame, parameters</span>):</span></span><br><span class="line">    slope, intercept = parameters</span><br><span class="line">    <span class="comment"># Sets initial y-coordinate as height from top down (bottom of the frame)</span></span><br><span class="line">    y1 = frame.shape[<span class="number">0</span>]</span><br><span class="line">    <span class="comment"># Sets final y-coordinate as 150 above the bottom of the frame</span></span><br><span class="line">    y2 = <span class="built_in">int</span>(y1 - <span class="number">150</span>)</span><br><span class="line">    <span class="comment"># Sets initial x-coordinate as (y1 - b) / m since y1 = mx1 + b</span></span><br><span class="line">    x1 = <span class="built_in">int</span>((y1 - intercept) / slope)</span><br><span class="line">    <span class="comment"># Sets final x-coordinate as (y2 - b) / m since y2 = mx2 + b</span></span><br><span class="line">    x2 = <span class="built_in">int</span>((y2 - intercept) / slope)</span><br><span class="line">    <span class="keyword">return</span> np.array([x1, y1, x2, y2])</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">visualize_lines</span>(<span class="params">frame, lines</span>):</span></span><br><span class="line">    <span class="comment"># Creates an image filled with zero intensities with the same dimensions as the frame</span></span><br><span class="line">    lines_visualize = np.zeros_like(frame)</span><br><span class="line">    <span class="comment"># Checks if any lines are detected</span></span><br><span class="line">    <span class="keyword">if</span> lines <span class="keyword">is</span> <span class="keyword">not</span> <span class="literal">None</span>:</span><br><span class="line">        <span class="keyword">for</span> x1, y1, x2, y2 <span class="keyword">in</span> lines:</span><br><span class="line">            <span class="comment"># Draws lines between two coordinates with green color and 5 thickness</span></span><br><span class="line">            cv.line(lines_visualize, (x1, y1), (x2, y2), (<span class="number">0</span>, <span class="number">255</span>, <span class="number">0</span>), <span class="number">5</span>)</span><br><span class="line">    <span class="keyword">return</span> lines_visualize</span><br><span class="line"></span><br><span class="line"><span class="comment"># The video feed is read in as a VideoCapture object</span></span><br><span class="line">cap = cv.VideoCapture(<span class="string">&quot;input.mp4&quot;</span>)</span><br><span class="line"><span class="keyword">while</span> (cap.isOpened()):</span><br><span class="line">    <span class="comment"># ret = a boolean return value from getting the frame, frame = the current frame being projected in the video</span></span><br><span class="line">    ret, frame = cap.read()</span><br><span class="line">    canny = do_canny(frame)</span><br><span class="line">    cv.imshow(<span class="string">&quot;canny&quot;</span>, canny)</span><br><span class="line">    <span class="comment"># plt.imshow(frame)</span></span><br><span class="line">    <span class="comment"># plt.show()</span></span><br><span class="line">    segment = do_segment(canny)</span><br><span class="line">    hough = cv.HoughLinesP(segment, <span class="number">2</span>, np.pi / <span class="number">180</span>, <span class="number">100</span>, np.array([]), minLineLength = <span class="number">100</span>, maxLineGap = <span class="number">50</span>)</span><br><span class="line">    <span class="comment"># Averages multiple detected lines from hough into one line for left border of lane and one line for right border of lane</span></span><br><span class="line">    lines = calculate_lines(frame, hough)</span><br><span class="line">    <span class="comment"># Visualizes the lines</span></span><br><span class="line">    lines_visualize = visualize_lines(frame, lines)</span><br><span class="line">    cv.imshow(<span class="string">&quot;hough&quot;</span>, lines_visualize)</span><br><span class="line">    <span class="comment"># Overlays lines on frame by taking their weighted sums and adding an arbitrary scalar value of 1 as the gamma argument</span></span><br><span class="line">    output = cv.addWeighted(frame, <span class="number">0.9</span>, lines_visualize, <span class="number">1</span>, <span class="number">1</span>)</span><br><span class="line">    <span class="comment"># Opens a new window and displays the output frame</span></span><br><span class="line">    cv.imshow(<span class="string">&quot;output&quot;</span>, output)</span><br><span class="line">    <span class="comment"># Frames are read by intervals of 10 milliseconds. The programs breaks out of the while loop when the user presses the &#x27;q&#x27; key</span></span><br><span class="line">    <span class="keyword">if</span> cv.waitKey(<span class="number">10</span>) &amp; <span class="number">0xFF</span> == <span class="built_in">ord</span>(<span class="string">&#x27;q&#x27;</span>):</span><br><span class="line">        <span class="keyword">break</span></span><br><span class="line"><span class="comment"># The following frees up resources and closes all windows</span></span><br><span class="line">cap.release()</span><br><span class="line">cv.destroyAllWindows()</span><br></pre></td></tr></table></figure><p>pro</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;感知与分割算法&lt;/h1&gt;
&lt;ul&gt;
&lt;li&gt;无人驾驶感知系统概述&lt;/li&gt;
&lt;li&gt;静态环境感知与分割算法&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id=&quot;无人驾驶中的感知&quot;&gt;无人驾驶中的感知&lt;/h2&gt;
&lt;p&gt;广义上：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;感知外界：
&lt;ul&gt;
&lt;li&gt;外在环</summary>
      
    
    
    
    <category term="study" scheme="https://www.jk404.cn/categories/study/"/>
    
    <category term="course" scheme="https://www.jk404.cn/categories/study/course/"/>
    
    <category term="self-driving" scheme="https://www.jk404.cn/categories/study/course/self-driving/"/>
    
    
  </entry>
  
  <entry>
    <title>docker_01_intro</title>
    <link href="https://www.jk404.cn/docker/docker-01-intro/"/>
    <id>https://www.jk404.cn/docker/docker-01-intro/</id>
    <published>2022-11-14T08:23:07.000Z</published>
    <updated>2022-11-15T00:06:54.646Z</updated>
    
    <content type="html"><![CDATA[<h1>Introduction</h1><h2 id="简介-v2">简介</h2><p>Docker 最初是由 dotCloud 公司创始人 Solomon Hykes 在法国期间发起的一个公司内部项目。其使用 Go 语言进行开发，基于 Linux 内核的 <code>cgroup</code> 、 <code>namespace</code> 、 <code>AUFS</code> 类的 <code>Union FS</code> 等技术，对进程进行封装隔离，属于操作系统层面的虚拟化技术。</p><p>由于隔离的进程独立于宿主和其他的隔离进程，因此也称其为容器。最初实现基于 <code>LXC</code> ，从 0.7 版本后开始去除 <code>LXC</code> ，转而使用自行研发的 <code>libcontainer</code> ，从 1.11 版本开始则使用 <code>runC</code> 和 <code>containerd</code> 。</p><p>Docker与传统的虚拟化方式不同在于：</p><ul><li>传统虚拟机技术是虚拟出一套硬件后，在器上运行一个完整操作系统，再再该系统上运行所需应用进程。</li><li>容器内的应用进程直接运行于宿主的内核，容器内没有自己的内核，也没有虚拟的硬件。</li></ul><p>这也突出了 Docker 的优点：</p><ul><li>更高效的利用系统资源：不需要虚拟硬件以及运行完整的操作系统。</li><li>更快速的启动时间：直接运行与宿主内核，无需启动完整的操作系统。</li><li>抑制的运行环境：Docker的镜像提供了除内核外完整的运行时环境，确保了应用运行环境一致性。</li><li>持续交付和部署：对于开发和运维（DevOps）人员来说，一次创建或配置就可以在任意地方正常运行是很好的。Docker 可以通过定制应用镜像来实现持续集成、持续交付、部署。通过 <code>Dockerfile</code> 进行镜像构建，结合持续集成系统 <code>Continuous Integration</code> 进行集成测试，而运维人员则可以直接在生产环境中快速部署该镜像。</li><li>更轻松的迁移：在很多平台上都可以运行，运行结果都是一致的。</li><li>更轻松的维护和扩展：使用分层存储以及镜像的技术，使得应用重复部分的复用更为容易，各个开源项目团队一起维护了一大批高质量的官方镜像。</li></ul><h2 id="概念">概念</h2><ul><li>镜像 image</li><li>容器 Container</li><li>仓库 Repository</li></ul><h3 id="镜像">镜像</h3><p>操作系统分为内核和用户空间，内核启动后，会挂在<code>root</code>文件系统提供用户空间支持。<code>Docker</code>镜像就相当于是一个<code>root</code>文件系统，只不过比较特殊。除了提供容器运行时所需的程序、库、资源、配置等文件外，还包含了一些为运行时准备的配置参数（匿名卷、环境变量、用户等）。</p><p>镜像不包含任何动态数据，其内容在构建后不会被改变。</p><p>利用<code>Union FS</code>技术，设计为<strong>分层存储</strong>的架构。严格来说，镜像并非是一个ISO那样的文件，而只是一个虚拟的概念，其实际体现并非由一个文件组成，而是由一组文件系统组成（多层文件系统联合组成）。</p><p>镜像构建时，会一层层构建，前一层是后一层的基础，每一层构建完成后就不会发生改变，后一层的任何改变只发生在自身层。例如，删除前一层的文件，实际上不是真的删除了前一层的文件，而是仅仅在当前层标记为文件已删除，在最终容器运行时，虽然看不到该文件，但事实上该文件一直跟随镜像。因此，构建镜像要格外小心，遵循最小化原则。</p><p>分层存储使得镜像复用、定制更为容易，甚至可以用之前构建好的镜像作为基础层，然后进一步添加新的层，以定制自己所需的内容。</p><h3 id="容器">容器</h3><p>Image 和 Container 的关系，就像是面向对象程序设计中的 类 和 实例 一样，镜像是静态的定义，容器时镜像运行时的实体。</p><p>容器可以被创建、启动、停止、删除、暂停等。</p><p>容器的本质是 进程 ，但它运行在属于自己的独立的 命名空间 ，因此它可以拥有自己的<code>root</code>文件系统、网络配置、进程空间、甚至用户ID空间。</p><p>容器也采用分层存储，每个容器运行时，以镜像为基础层，在其上创建也给当前容器的存储层——容器存储层。任何保存于容器存储层的数据都会随容器删除而消失。Docker最佳实践的要求为：容器不应该向其存储层写入任何数据，容器存储层应该保持无状态化，所有文件写入操作应该使用数据卷(Volume)或者绑定宿主目录。这些位置的读写会跳过容器存储层，直接对宿主(或者remote)发生读写。</p><p>数据卷的生存周期独立于容器，不会随容器删除而消失。</p><h3 id="仓库-v2">仓库</h3><p>Docker Registry 提供集中的存储、分发镜像的服务，一个 Docker Registry 中可以包含多个仓库(Repository)，每个仓库可以包含多个标签(Tag)，每个标签对应一个镜像。</p><p>通常，一个仓库会包含同一个软件不同版本的镜像，而标签就常用于对应该软件的各个版本。使用<code>&lt;Repository Name&gt;:&lt;Tag&gt;</code>的格式来指定具体是这个软件的哪个版本的镜像，如果不给出标签将以<code>latest</code>作为默认标签。</p><p>以 Ubuntu 镜像为例，ubuntu是仓库的名字，包含不同的版本标签：<code>14.04</code>、<code>16.04</code>等，可以通过<code>ubuntu:14.04</code>来指定具体的镜像。</p><p>仓库名常以两段式路径形式出现，如：klose/ubuntu，klose为Docker Registry多用户环境下的用户名，后者是对应的软件名。这种形式并非绝对。</p><p>最常使用的公开服务是官方的 <code>Docker Hub</code>，也是默认的Registry。还有 <code>CoreOS</code>的<code>Quay.io</code>，以及<code>Google</code>的<code>Google Container Registry</code>，<code>Kubernetes</code>的镜像使用的就是这个服务。云服务商针对Docker Hub的镜像服务称为加速器。有：阿里云加速器、DaoCloud加速器、灵雀加速器等。是用加速器会直接从国内的地址下载Docker Hub的镜像。</p><p>用户可以本地搭建私有Docker Registry。</p><h2 id="安装-v2">安装</h2><p>这里使用的是 Ubuntu Xenial 16.04(LTS)。</p><p>Docker目前支持的Ubuntu最低版本为18.04LTS，需要安装在64位的x86或ARM平台上，并且要求内核版本不低于3.10，实际上内核越新越好。</p><p>官方安装文档：<a href="https://docs.docker.com/engine/install/ubuntu/">https://docs.docker.com/engine/install/ubuntu/</a></p><p>阿里云安装文档：<a href="https://developer.aliyun.com/article/110806">https://developer.aliyun.com/article/110806</a></p><h3 id="内核">内核</h3><p>通过下列命令查看内核版本详细信息：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> uname -a</span></span><br></pre></td></tr></table></figure><p>如果内核版本过低可以进行升级：</p><p>Ubuntu 12.04 LTS</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo apt-get install -y --install-recommends linux-generic-lts-trusty</span><br></pre></td></tr></table></figure><p>Ubuntu 14.04 LTS</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">sudo apt-get install -y --install-recommends linux-generic-lts-xenial</span><br></pre></td></tr></table></figure><h3 id="配置GRUB引导参数">配置GRUB引导参数</h3><p>在使用Docker期间，或者在 <code>docker info</code> 中可以看到警告信息：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">WARNING: Your kernel does not support cgroup swap limit. WARNING: Your kernel does not support swap limit capabilities. Limitation discarded.</span><br></pre></td></tr></table></figure><p>或者是：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">WARNING: No memory limit support</span><br><span class="line">WARNING: No swap limit support</span><br><span class="line">WARNING: No oom kill disable support</span><br></pre></td></tr></table></figure><p>如果需要这些功能，要修改GRUB的配置文件<code>/etc/default/grub</code>，在<code>GRUB_CMDLINE_LINUX</code>中添加内核引导参数<code>cgroup_enable=memory swapaccount=1</code>，然后对GRUB进行更新：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> sudo update-grub</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> sudo reboot</span></span><br></pre></td></tr></table></figure><h3 id="脚本自动安装">脚本自动安装</h3><p>官方的简化安装脚本，适用于Ubuntu和Debian：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> curl -sSL https://get.docker.com/ | sh</span></span><br></pre></td></tr></table></figure><p>该脚本自动将一切工作做好，并且把Docker安装在系统中。</p><p>阿里云的安装脚本：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> curl -sSL http://acs-public-mirror.oss-cn-hangzhou.aliyuncs.com/docker-engine/internet | sh -</span></span><br></pre></td></tr></table></figure><p>DaoCloud 的安装脚本</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> curl -sSL https://get.daocloud.io/docker | sh</span></span><br></pre></td></tr></table></figure><h3 id="添加APT镜像源">添加APT镜像源</h3><p>使用Docker官方的软件源以保持最新状态。</p><p>国内的一些软件源镜像（比如阿里云）不是太在意系统安全上的细节，可能依旧使用不安全的 HTTP，对于这些源可以不执行这一步。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> sudo apt-get update</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> sudo apt-get install apt-transport-https ca-certificates</span></span><br></pre></td></tr></table></figure><p>为了确认所下载软件包的合法性，需要添加 Docker 官方软件源的 GPG 密钥。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> sudo apt-key adv --keyserver hkp://p80.pool.sks-keyservers.net:80 --recv-keys 58118E89F3A912897C070ADBF76221572C52609D</span></span><br></pre></td></tr></table></figure><p>然后，我们需要向 source.list 中添加 Docker 软件源。</p><table><thead><tr><th>操作系统版本</th><th>REPO</th></tr></thead><tbody><tr><td>Precise 12.04(LTS)</td><td>deb <a href="https://apt.dockerproject.org/repo">https://apt.dockerproject.org/repo</a> ubuntu-precise main</td></tr><tr><td>Trusty 14.04(LTS)</td><td>deb <a href="https://apt.dockerproject.org/repo">https://apt.dockerproject.org/repo</a> ubuntu-trusty main</td></tr><tr><td>Xenial 16.04(LTS)</td><td>deb <a href="https://apt.dockerproject.org/repo">https://apt.dockerproject.org/repo</a> ubuntu-xenial main</td></tr></tbody></table><p>用下面的命令将APT源添加到<code>source.list</code>（将其中的<REPO>替换位上表的值）：</REPO></p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> <span class="built_in">echo</span> <span class="string">&quot;&lt;REPO&gt;&quot;</span> | sudo tee /etc/apt/sources.list.d/docker.list</span></span><br></pre></td></tr></table></figure><p>添加成功后进行更新：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> sudo apt-get update</span></span><br></pre></td></tr></table></figure><h3 id="安装docker">安装docker</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> sudo apt-get install docker-engine</span></span><br></pre></td></tr></table></figure><h3 id="启动Docker引擎">启动Docker引擎</h3><p>Ubuntu 12.04/14.04、Debian 7 Wheezy</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ sudo service docker start</span><br></pre></td></tr></table></figure><p>Ubuntu 16.04、Debian 8 Jessie/Stretch</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ sudo systemctl enable docker</span><br><span class="line">$ sudo systemctl start docker</span><br></pre></td></tr></table></figure><h3 id="建立docker用户组">建立docker用户组</h3><p>默认情况下，<code>docker</code> 命令会使用 <code>Unix socket</code> 与 Docker 引擎通讯，而只有 <code>root</code> 用户和 <code>dcoker</code> 用户组的用户才可以访问Docker引擎的<code>Unix socket</code> 处于安全考虑，Linux系统上不会直接用root用户，因此更好的做法是将需要使用docker的用户加入docker用户组。</p><p>建立<code>docker</code>组：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> sudo groupadd docker</span></span><br></pre></td></tr></table></figure><p>将当前用户加入docker组：</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">$</span><span class="bash"> sudo usermod -aG docker <span class="variable">$USER</span></span></span><br></pre></td></tr></table></figure><h3 id="其他操作系统">其他操作系统</h3><p><code>CentOS</code>：<a href="http://shouce.jb51.net/docker_practice/install/centos.html">http://shouce.jb51.net/docker_practice/install/centos.html</a></p><p><code>macOS</code>：<a href="http://shouce.jb51.net/docker_practice/install/mac.html">http://shouce.jb51.net/docker_practice/install/mac.html</a></p><h3 id="镜像加速器">镜像加速器</h3><p>关于加速器的使用参考：<a href="http://shouce.jb51.net/docker_practice/install/mirror.html">http://shouce.jb51.net/docker_practice/install/mirror.html</a></p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;Introduction&lt;/h1&gt;
&lt;h2 id=&quot;简介-v2&quot;&gt;简介&lt;/h2&gt;
&lt;p&gt;Docker 最初是由 dotCloud 公司创始人 Solomon Hykes 在法国期间发起的一个公司内部项目。其使用 Go 语言进行开发，基于 Linux 内核的 &lt;code&gt;c</summary>
      
    
    
    
    <category term="docker" scheme="https://www.jk404.cn/categories/docker/"/>
    
    
  </entry>
  
  <entry>
    <title>self-driving_02_ROS</title>
    <link href="https://www.jk404.cn/study/course/self-driving/self-driving-02-ROS/"/>
    <id>https://www.jk404.cn/study/course/self-driving/self-driving-02-ROS/</id>
    <published>2022-11-12T08:30:29.000Z</published>
    <updated>2022-11-20T17:29:01.703Z</updated>
    
    <content type="html"><![CDATA[<h1>ROS</h1><p>ROS: The Robot Operating System</p><h2 id="相关概念">相关概念</h2><p>为什么使用ROS：</p><ul><li>点对点设计</li><li>分布式设计</li><li>多语言</li><li>轻量级</li><li>免费且开源</li><li>社区完善</li></ul><p>ROS 不是传统意义上的操作系统，而是一种系统软件框架，使用了流行的面向服务SOA的软件技术，通过网络协议将节点间数据通信解耦，以集成不同语言不同功能的代码。</p><p>ROS 不是编程语言，是一个函数库，除包含客户端外，还包含一个中心服务器、一系列命令行工具、图形化界面工具以及编译环境。</p><p>ROS也不是集成开发环境。</p><p>Talker&amp;Listener模型</p><ul><li>消息以一种publish/subscribe的方式传递</li><li>节点可以在给定的主体中发布/订阅消息</li><li>一个节点可以订阅/发布多个不同的主题</li><li>允许多个节点订阅/发布同一个主题</li><li>订阅节点和发布节点并不知道相互之间的存在</li></ul><p>Catkin</p><ul><li>ROS的一个最简单最常见的编译系统</li><li>ROS对CMake进行了扩展</li><li>适合ROS进行大型项目，多包，多节点的场景下进行批量编译</li><li>操作简单，一次配置，多次使用</li><li>跨依赖项目编译</li></ul><p>抽象程度为：catkin_ws-&gt;CMakeLists.txt-&gt;Makefile</p><p>由此，Cmake编译单个节点，catkin可以定义节点与节点之间的依赖关系。</p><p>Packages &amp; Catkin Workspaces</p><ul><li>Package是ROS系统中最底层最基本的在组织，存放各种文件：库、工具、可执行文件等</li><li>Catkkin workspaces，包的顶层工作目录，一个catkin workspace包含一个工程下面多个ros package</li></ul><p>Package.xml</p><ul><li>每个包的描述文件，都需要放置在包的根目录下，对包的名字/版本/作者/维护者/依赖关系进行说明</li></ul><p>CmakeList.txt</p><ul><li>定义一个包的编译指令</li><li>cmake不会找package.xml文件，依据cmakelists.txt文件编译需要清晰指出头文件和库文件的指向</li><li>catkin_package(CATKIN_DEPENDS roscpp)声明依赖本包同时需要的其他ros包</li><li>find_package(catkin REQUIRED COMPONENTS…)声明编译本包所需要的其他ros包</li><li>add_executable声明编译本包生成的可执行文件</li><li>target_link_libraries 链接可执行文件和依赖库</li></ul><p>ROS NODE</p><ul><li>一个节点是ROS程序包的一个可执行文件</li><li>ROS节点可以使用ROS客户库与其他节点通信</li><li>节点可以发布或者接受一个话题</li><li>节点可以提供或使用某项服务</li><li>常用命令<ul><li>rosnode list 查看当前注册到ros master的所有节点</li><li>Rosnode info 查看某个节点的具体信息</li></ul></li></ul><p>ROS TOPIC</p><ul><li>节点之间通过一个ROS topic来互相通信</li><li>通过publisher声明所发布topic名称</li><li>通过subscriber声明所需要监听的topic名称</li><li>常用命令<ul><li>Topic list 查看当前注册到ros master的所有topic列表</li><li>rostopic echo 把当前topic输出到控制台</li></ul></li></ul><p>ROS PARAM</p><ul><li>Rosparam命令允许在ROS参数服务器上从存储和赋值数据，参数服务器可以存储浮点型、整型、布尔值、字典和列表值</li><li>小型KV库</li><li>常用命令<ul><li>rosparam set 设置参数</li><li>rosparam get 获取参数</li><li>rosparam list 查看当前服务器上的参数</li></ul></li></ul><p>安装&amp;配置</p><ul><li><p>在<a href="https://wiki.ros.org/kinetic/Installation/Ubuntu">这里</a>安装Ubuntu1604所对应的kinetic版本的ROS。</p></li><li><p>ROS Workspace</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">mkdir -p ~/catkin_ws/src</span><br><span class="line">cd ~/catkin_ws</span><br><span class="line">catkin_make</span><br><span class="line"><span class="meta"></span></span><br><span class="line"><span class="meta">#</span><span class="bash">在~/.bashrc中添加</span></span><br><span class="line">source /home/klose/catkin_ws/devel/setup.bash</span><br></pre></td></tr></table></figure></li></ul><h2 id="code">code</h2><ul><li>flora_talker</li><li>flora_listener</li><li>flora_msgs</li></ul><h3 id="flora-talker">flora_talker</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> ~/catkin_ws/src</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> catkin_create_pkg flora_say std_msgs roscpp</span></span><br></pre></td></tr></table></figure><p>生成了flora_say文件夹</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">// flora_say/src/ 中新建 flora_say_node.cpp 文件</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;ros/ros.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;std_msgs/String.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;sstream&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;flora_msgs/FloraMsg.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span>&#123;</span><br><span class="line">    ros::<span class="built_in">init</span>(argc, argv, <span class="string">&quot;flora_talker&quot;</span>);</span><br><span class="line">    ros::NodeHandle n;</span><br><span class="line">    ros::Publisher flora_pub = n.advertise&lt;std_msgs::String&gt;(<span class="string">&quot;/flora_topic&quot;</span>, <span class="number">10</span>);</span><br><span class="line">    ros::Publisher flora_pub_new = n.advertise&lt;flora_msgs::FloraMsg&gt;(<span class="string">&quot;/flora_topic_new&quot;</span>, <span class="number">10</span>);</span><br><span class="line">    <span class="function">ros::Rate <span class="title">loop_rate</span><span class="params">(<span class="number">10</span>)</span></span>;</span><br><span class="line">    <span class="keyword">int</span> count = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span>(ros::<span class="built_in">ok</span>())&#123;</span><br><span class="line">        std_msgs::String msg;</span><br><span class="line">        std::stringstream ss;</span><br><span class="line">        ss &lt;&lt; <span class="string">&quot;hello, flora&quot;</span> &lt;&lt; count;</span><br><span class="line">        count ++;</span><br><span class="line">        msg.data = ss.<span class="built_in">str</span>();</span><br><span class="line">        flora_pub.<span class="built_in">publish</span>(msg);</span><br><span class="line"></span><br><span class="line">        <span class="comment">// 参数中心</span></span><br><span class="line">        std::string param_string;</span><br><span class="line">        n.param&lt;std::string&gt;(<span class="string">&quot;myparam&quot;</span>, param_string,<span class="string">&quot;love you, flora&quot;</span>);</span><br><span class="line"></span><br><span class="line">        flora_msgs::FloraMsg floraMsg;</span><br><span class="line">        floraMsg.id = count;</span><br><span class="line">        floraMsg.detail = param_string;</span><br><span class="line"></span><br><span class="line">        flora_pub_new.<span class="built_in">publish</span>(floraMsg);</span><br><span class="line"></span><br><span class="line">        loop_rate.<span class="built_in">sleep</span>();</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>修改 CMakeLists.txt ：</p><figure class="highlight makefile"><table><tr><td class="code"><pre><span class="line">cmake_minimum_required(VERSION 3.0.2)</span><br><span class="line">project(flora_say)</span><br><span class="line"></span><br><span class="line"><span class="comment">## Compile as C++11, supported in ROS Kinetic and newer</span></span><br><span class="line"><span class="comment"># add_compile_options(-std=c++11)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">## Find catkin macros and libraries</span></span><br><span class="line"><span class="comment">## if COMPONENTS list like find_package(catkin REQUIRED COMPONENTS xyz)</span></span><br><span class="line"><span class="comment">## is used, also find other catkin packages</span></span><br><span class="line">find_package(catkin REQUIRED COMPONENTS</span><br><span class="line">  roscpp</span><br><span class="line">  std_msgs</span><br><span class="line">        flora_msgs</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">...</span><br><span class="line"></span><br><span class="line"><span class="comment">###################################</span></span><br><span class="line"><span class="comment">## catkin specific configuration ##</span></span><br><span class="line"><span class="comment">###################################</span></span><br><span class="line"><span class="comment">## The catkin_package macro generates cmake config files for your package</span></span><br><span class="line"><span class="comment">## Declare things to be passed to dependent projects</span></span><br><span class="line"><span class="comment">## INCLUDE_DIRS: uncomment this if your package contains header files</span></span><br><span class="line"><span class="comment">## LIBRARIES: libraries you create in this project that dependent projects also need</span></span><br><span class="line"><span class="comment">## CATKIN_DEPENDS: catkin_packages dependent projects also need</span></span><br><span class="line"><span class="comment">## DEPENDS: system dependencies of this project that dependent projects also need</span></span><br><span class="line">catkin_package(</span><br><span class="line">  <span class="comment">#INCLUDE_DIRS include</span></span><br><span class="line">  <span class="comment">#LIBRARIES flora_say</span></span><br><span class="line">  CATKIN_DEPENDS roscpp std_msgs flora_msgs</span><br><span class="line">  <span class="comment">#DEPENDS system_lib</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment">###########</span></span><br><span class="line"><span class="comment">## Build ##</span></span><br><span class="line"><span class="comment">###########</span></span><br><span class="line"></span><br><span class="line"><span class="comment">## Specify additional locations of header files</span></span><br><span class="line"><span class="comment">## Your package locations should be listed before other locations</span></span><br><span class="line">include_directories(</span><br><span class="line"><span class="comment"># include</span></span><br><span class="line">  $&#123;catkin_INCLUDE_DIRS&#125;</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="comment">## Declare a C++ library</span></span><br><span class="line"><span class="comment"># add_library($&#123;PROJECT_NAME&#125;</span></span><br><span class="line"><span class="comment">#   src/$&#123;PROJECT_NAME&#125;/flora_say.cpp</span></span><br><span class="line"><span class="comment"># )</span></span><br><span class="line"></span><br><span class="line"><span class="comment">## Add cmake target dependencies of the library</span></span><br><span class="line"><span class="comment">## as an example, code may need to be generated before libraries</span></span><br><span class="line"><span class="comment">## either from message generation or dynamic reconfigure</span></span><br><span class="line"><span class="comment"># add_dependencies($&#123;PROJECT_NAME&#125; $&#123;$&#123;PROJECT_NAME&#125;_EXPORTED_TARGETS&#125; $&#123;catkin_EXPORTED_TARGETS&#125;)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">## Declare a C++ executable</span></span><br><span class="line"><span class="comment">## With catkin_make all packages are built within a single CMake context</span></span><br><span class="line"><span class="comment">## The recommended prefix ensures that target names across packages don&#x27;t collide</span></span><br><span class="line">add_executable($&#123;PROJECT_NAME&#125;_node src/flora_say_node.cpp)</span><br><span class="line"></span><br><span class="line"><span class="comment">## Rename C++ executable without prefix</span></span><br><span class="line"><span class="comment">## The above recommended prefix causes long target names, the following renames the</span></span><br><span class="line"><span class="comment">## target back to the shorter version for ease of user use</span></span><br><span class="line"><span class="comment">## e.g. &quot;rosrun someones_pkg node&quot; instead of &quot;rosrun someones_pkg someones_pkg_node&quot;</span></span><br><span class="line"><span class="comment"># set_target_properties($&#123;PROJECT_NAME&#125;_node PROPERTIES OUTPUT_NAME node PREFIX &quot;&quot;)</span></span><br><span class="line"></span><br><span class="line"><span class="comment">## Add cmake target dependencies of the executable</span></span><br><span class="line"><span class="comment">## same as for the library above</span></span><br><span class="line">add_dependencies($&#123;PROJECT_NAME&#125;_node $&#123;$&#123;PROJECT_NAME&#125;_EXPORTED_TARGETS&#125; $&#123;catkin_EXPORTED_TARGETS&#125;)</span><br><span class="line"></span><br><span class="line"><span class="comment">## Specify libraries to link a library or executable target against</span></span><br><span class="line">target_link_libraries(</span><br><span class="line">        $&#123;PROJECT_NAME&#125;_node</span><br><span class="line">        $&#123;catkin_LIBRARIES&#125;</span><br><span class="line">)</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>修改 package.xml，引入依赖</p><figure class="highlight xml"><table><tr><td class="code"><pre><span class="line">...</span><br><span class="line">  <span class="comment">&lt;!-- Use build_depend for packages you need at compile time: --&gt;</span></span><br><span class="line">     <span class="tag">&lt;<span class="name">build_depend</span>&gt;</span>message_generation<span class="tag">&lt;/<span class="name">build_depend</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- Use build_export_depend for packages you need in order to build against this package: --&gt;</span></span><br><span class="line">  <span class="comment">&lt;!--   &lt;build_export_depend&gt;message_generation&lt;/build_export_depend&gt; --&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- Use buildtool_depend for build tool packages: --&gt;</span></span><br><span class="line">  <span class="comment">&lt;!--   &lt;buildtool_depend&gt;catkin&lt;/buildtool_depend&gt; --&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- Use exec_depend for packages you need at runtime: --&gt;</span></span><br><span class="line">     <span class="tag">&lt;<span class="name">exec_depend</span>&gt;</span>message_runtime<span class="tag">&lt;/<span class="name">exec_depend</span>&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- Use test_depend for packages you need only for testing: --&gt;</span></span><br><span class="line">  <span class="comment">&lt;!--   &lt;test_depend&gt;gtest&lt;/test_depend&gt; --&gt;</span></span><br><span class="line">  <span class="comment">&lt;!-- Use doc_depend for packages you need only for building documentation: --&gt;</span></span><br><span class="line">  <span class="comment">&lt;!--   &lt;doc_depend&gt;doxygen&lt;/doc_depend&gt; --&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">buildtool_depend</span>&gt;</span>catkin<span class="tag">&lt;/<span class="name">buildtool_depend</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">build_depend</span>&gt;</span>roscpp<span class="tag">&lt;/<span class="name">build_depend</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">build_depend</span>&gt;</span>std_msgs<span class="tag">&lt;/<span class="name">build_depend</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">build_depend</span>&gt;</span>flora_msgs<span class="tag">&lt;/<span class="name">build_depend</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">build_export_depend</span>&gt;</span>roscpp<span class="tag">&lt;/<span class="name">build_export_depend</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">build_export_depend</span>&gt;</span>std_msgs<span class="tag">&lt;/<span class="name">build_export_depend</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">exec_depend</span>&gt;</span>roscpp<span class="tag">&lt;/<span class="name">exec_depend</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">exec_depend</span>&gt;</span>std_msgs<span class="tag">&lt;/<span class="name">exec_depend</span>&gt;</span></span><br><span class="line">  <span class="tag">&lt;<span class="name">exec_depend</span>&gt;</span>flora_msgs<span class="tag">&lt;/<span class="name">exec_depend</span>&gt;</span></span><br><span class="line">...</span><br></pre></td></tr></table></figure><p>回到catkin_ws文件夹执行<code>catkin_make</code></p><h3 id="flora-listener">flora_listener</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;ros/ros.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;std_msgs/String.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">floraCallback</span><span class="params">(<span class="keyword">const</span> std_msgs::String::ConstPtr&amp; msg)</span></span>&#123;</span><br><span class="line">    <span class="built_in">ROS_INFO</span>(<span class="string">&quot;I heard %s&quot;</span>, msg-&gt;data.<span class="built_in">c_str</span>());</span><br><span class="line">&#125;</span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> **argv)</span></span>&#123;</span><br><span class="line">    ros::<span class="built_in">init</span>(argc, argv, <span class="string">&quot;flora_listener&quot;</span>);</span><br><span class="line">    ros::NodeHandle n;</span><br><span class="line">    ros::Subscriber sub = n.<span class="built_in">subscribe</span>(<span class="string">&quot;/flora_topic&quot;</span>, <span class="number">10</span>, floraCallback);</span><br><span class="line">    ros::<span class="built_in">spin</span>();</span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>修改 CMakeLists.txt ：</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">cmake_minimum_required</span>(VERSION <span class="number">3.0</span><span class="number">.2</span>)</span><br><span class="line"><span class="built_in">project</span>(flora_listen)</span><br><span class="line"></span><br><span class="line">## Compile as C++<span class="number">11</span>, supported in ROS Kinetic <span class="keyword">and</span> newer</span><br><span class="line"># <span class="built_in">add_compile_options</span>(-std=c++<span class="number">11</span>)</span><br><span class="line"></span><br><span class="line">## Find catkin macros <span class="keyword">and</span> libraries</span><br><span class="line">#<span class="meta"># <span class="meta-keyword">if</span> COMPONENTS list like find_package(catkin REQUIRED COMPONENTS xyz)</span></span><br><span class="line">#<span class="meta"># is used, also find other catkin packages</span></span><br><span class="line"><span class="built_in">find_package</span>(catkin REQUIRED COMPONENTS</span><br><span class="line">  roscpp</span><br><span class="line">  std_msgs</span><br><span class="line">)</span><br><span class="line">    <span class="built_in">catkin_package</span>(</span><br><span class="line">#  INCLUDE_DIRS include</span><br><span class="line">#  LIBRARIES flora_listen</span><br><span class="line">  CATKIN_DEPENDS roscpp std_msgs</span><br><span class="line">#  DEPENDS system_lib</span><br><span class="line">)</span><br><span class="line"></span><br><span class="line">###########</span><br><span class="line">## Build ##</span><br><span class="line">###########</span><br><span class="line"></span><br><span class="line">## Specify additional locations of header files</span><br><span class="line">## <span class="function">Your package locations should be listed before other locations</span></span><br><span class="line"><span class="function"><span class="title">include_directories</span><span class="params">(</span></span></span><br><span class="line"><span class="params"><span class="function"># include</span></span></span><br><span class="line"><span class="params"><span class="function">  $&#123;catkin_INCLUDE_DIRS&#125;</span></span></span><br><span class="line"><span class="params"><span class="function">)</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">## Declare a C++ library</span></span><br><span class="line"><span class="function"># <span class="title">add_library</span><span class="params">($&#123;PROJECT_NAME&#125;</span></span></span><br><span class="line"><span class="params"><span class="function">#   src/$&#123;PROJECT_NAME&#125;/flora_listen_node.cpp</span></span></span><br><span class="line"><span class="params"><span class="function"># )</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">## Add cmake target dependencies of the library</span></span><br><span class="line"><span class="function">#<span class="meta"># as an example, code may need to be generated before libraries</span></span></span><br><span class="line"><span class="function">#<span class="meta"># either from message generation or dynamic reconfigure</span></span></span><br><span class="line"><span class="function"># <span class="title">add_dependencies</span><span class="params">($&#123;PROJECT_NAME&#125; $&#123;$&#123;PROJECT_NAME&#125;_EXPORTED_TARGETS&#125; $&#123;catkin_EXPORTED_TARGETS&#125;)</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">## Declare a C++ executable</span></span><br><span class="line"><span class="function">## With catkin_make all packages are built within a single CMake context</span></span><br><span class="line"><span class="function">## The recommended prefix ensures that target names across packages don&#x27;t collide</span></span><br><span class="line"><span class="function"> <span class="title">add_executable</span><span class="params">($&#123;PROJECT_NAME&#125;_node src/flora_listen_node.cpp)</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">## Rename C++ executable without prefix</span></span><br><span class="line"><span class="function">## The above recommended prefix causes <span class="keyword">long</span> target names, the following renames the</span></span><br><span class="line"><span class="function">#<span class="meta"># target back to the shorter version for ease of user use</span></span></span><br><span class="line"><span class="function">#<span class="meta"># e.g. <span class="meta-string">&quot;rosrun someones_pkg node&quot;</span> instead of <span class="meta-string">&quot;rosrun someones_pkg someones_pkg_node&quot;</span></span></span></span><br><span class="line"><span class="function"># <span class="title">set_target_properties</span><span class="params">($&#123;PROJECT_NAME&#125;_node PROPERTIES OUTPUT_NAME node PREFIX <span class="string">&quot;&quot;</span>)</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">## Add cmake target dependencies of the executable</span></span><br><span class="line"><span class="function">#<span class="meta"># same as for the library above</span></span></span><br><span class="line"><span class="function"> <span class="title">add_dependencies</span><span class="params">($&#123;PROJECT_NAME&#125;_node $&#123;$&#123;PROJECT_NAME&#125;_EXPORTED_TARGETS&#125; $&#123;catkin_EXPORTED_TARGETS&#125;)</span></span></span><br><span class="line"><span class="function"></span></span><br><span class="line"><span class="function">## Specify libraries to link a library <span class="keyword">or</span> executable target against</span></span><br><span class="line"><span class="function"> <span class="title">target_link_libraries</span><span class="params">($&#123;PROJECT_NAME&#125;_node</span></span></span><br><span class="line"><span class="params"><span class="function">   $&#123;catkin_LIBRARIES&#125;</span></span></span><br><span class="line"><span class="params"><span class="function"> )</span></span></span><br><span class="line"><span class="function"></span></span><br></pre></td></tr></table></figure><h3 id="flora-msg">flora_msg</h3><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line"><span class="meta">#</span><span class="bash"> ~/catkin_ws</span></span><br><span class="line"><span class="meta">$</span><span class="bash"> cat src/flora_msgs/msg/FloraMsg.msg</span> </span><br><span class="line">string detail</span><br><span class="line">int32 id</span><br></pre></td></tr></table></figure><h3 id="运行发布">运行发布</h3><p>ros master</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">roscore</span><br></pre></td></tr></table></figure><p>ros node</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">rosnode list</span><br><span class="line">rosrun flora_say flora_say_node</span><br></pre></td></tr></table></figure><p>ros topic</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">rostopic list</span><br><span class="line">rostopic echo /flora_say</span><br></pre></td></tr></table></figure><h2 id="docker-v2">docker</h2><p><a href="hub.docker.com">docker hub</a></p><p><a href="https://hub.docker.com/_/ros/tags?page=1&amp;name=kinetic">ros kinetic</a></p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ sudo docker pull ros:kinetic</span><br><span class="line">kinetic: Pulling from library/ros</span><br><span class="line">61e03ba1d414: Pull complete </span><br><span class="line">4afb39f216bd: Pull complete </span><br><span class="line">e489abdc9f90: Pull complete </span><br><span class="line">999fff7bcc24: Pull complete </span><br><span class="line">7dee46e5af81: Pull complete </span><br><span class="line">26d1bbf7dea9: Pull complete </span><br><span class="line">0662b1269a41: Pull complete </span><br><span class="line">f759c9d3424a: Pull complete </span><br><span class="line">defe81202a2c: Pull complete </span><br><span class="line">6b7c066b85ea: Pull complete </span><br><span class="line">b8274d96e502: Pull complete </span><br><span class="line">baea347ac716: Pull complete </span><br><span class="line">Digest: sha256:29c7cb9305475baba4aee5c0b944dca3514c38c9d7b90c5830dfc72ca454ceab</span><br><span class="line">Status: Downloaded newer image for ros:kinetic</span><br><span class="line">docker.io/library/ros:kinetic</span><br><span class="line">$ sudo docker images</span><br><span class="line">REPOSITORY   TAG       IMAGE ID       CREATED         SIZE</span><br><span class="line">ros          kinetic   a3c5711abb29   16 months ago   1.13GB</span><br></pre></td></tr></table></figure><p>build</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ sudo docker run -itd -v$(pwd):/data ros:kinetic</span><br><span class="line">4a9c7b8170285696db2d1575d593fdc53b387127b793c8692f534dd31a21d0de</span><br></pre></td></tr></table></figure><ul><li>-itd</li><li>-v$(pwd):/data</li></ul><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ sudo docker ps</span><br><span class="line">CONTAINER ID   IMAGE         COMMAND                  CREATED         STATUS         PORTS     NAMES</span><br><span class="line">4a9c7b817028   ros:kinetic   &quot;/ros_entrypoint.sh …&quot;   2 minutes ago   Up 2 minutes             awesome_einstein</span><br></pre></td></tr></table></figure><p>创建<code>catkin_ws</code>文件夹，用于编译项目：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">root@4a9c7b817028:/# mkdir catkin_ws</span><br><span class="line">root@4a9c7b817028:/# cp -r /data/src/ ./catkin_ws/</span><br></pre></td></tr></table></figure><p>将环境变量配置好：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">root@4a9c7b817028:/catkin_ws# echo &#x27;source /opt/ros/kinetic/setup.bash&#x27; &gt;&gt; /root/.bashrc</span><br><span class="line">root@4a9c7b817028:/catkin_ws# source /root/.bashrc </span><br></pre></td></tr></table></figure><p>编译</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">root@4a9c7b817028:/catkin_ws# catkin_make</span><br></pre></td></tr></table></figure><p>image制作分发</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">klose@self-driving:~/catkin_ws$ sudo docker ps -a</span><br><span class="line">CONTAINER ID   IMAGE         COMMAND                  CREATED         STATUS         PORTS     NAMES</span><br><span class="line">4a9c7b817028   ros:kinetic   &quot;/ros_entrypoint.sh …&quot;   8 minutes ago   Up 8 minutes             awesome_einstein</span><br><span class="line">klose@self-driving:~/catkin_ws$ sudo docker commit 4a9c7b817028 myimage</span><br><span class="line">sha256:b17e1ef21afbaaf7e04cc0c8a52f3a8118ece554b568e03c63577f8183429e0f</span><br><span class="line">klose@self-driving:~/catkin_ws$ sudo docker images</span><br><span class="line">REPOSITORY   TAG       IMAGE ID       CREATED         SIZE</span><br><span class="line">myimage      latest    b17e1ef21afb   5 seconds ago   1.13GB</span><br><span class="line">ros          kinetic   a3c5711abb29   16 months ago   1.13GB</span><br></pre></td></tr></table></figure><h2 id="任务">任务</h2><ul><li>建立一个talker listener模型</li><li>构建自己的message，包含两个int32类型的值</li><li>Talker通过该message发送随即两个数字，Listener收到后分别进行累加后输出</li><li>使用Docker打包上传共享镜像</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;ROS&lt;/h1&gt;
&lt;p&gt;ROS: The Robot Operating System&lt;/p&gt;
&lt;h2 id=&quot;相关概念&quot;&gt;相关概念&lt;/h2&gt;
&lt;p&gt;为什么使用ROS：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;点对点设计&lt;/li&gt;
&lt;li&gt;分布式设计&lt;/li&gt;
&lt;li&gt;多语言&lt;/li&gt;</summary>
      
    
    
    
    <category term="study" scheme="https://www.jk404.cn/categories/study/"/>
    
    <category term="course" scheme="https://www.jk404.cn/categories/study/course/"/>
    
    <category term="self-driving" scheme="https://www.jk404.cn/categories/study/course/self-driving/"/>
    
    
  </entry>
  
  <entry>
    <title>self-driving_01_intro</title>
    <link href="https://www.jk404.cn/study/course/self-driving/self-driving-01-intro/"/>
    <id>https://www.jk404.cn/study/course/self-driving/self-driving-01-intro/</id>
    <published>2022-11-12T04:01:58.000Z</published>
    <updated>2022-11-12T08:29:29.643Z</updated>
    
    <content type="html"><![CDATA[<h1>概述</h1><blockquote><p>“A self-driving car, also known as a robot car, autonomous car, or driverless car, is a vehicle that is capable of sensing its environment and moving with little or no human input.”– wikipedia</p></blockquote><h2 id="无人驾驶技术路径">无人驾驶技术路径</h2><ul><li>无人驾驶分级</li><li>L2/L3自动驾驶</li><li>L4自动驾驶</li></ul><h3 id="无人驾驶分级">无人驾驶分级</h3><p>美国汽车工程师协会SAE，将自动驾驶分为6个级别L0~L5。</p><hr><p>责任主体在于<strong>驾驶员</strong>：</p><p>L0：纯人工</p><p>L1：巡航定速（Adaptive Cruise Control, ACC），巡航装置可以纵向控制车辆，加速减速。</p><p>L2：车道保持辅助（Lane Keeping），系统可以纵向控制，也可以横向控制汽车，但是人仍为主导。（特斯拉）</p><p>L2.5：在简单路况下的变道能力。</p><hr><p>责任主体在于<strong>汽车</strong>：</p><p>L3：在L2基础上提供变道能力。</p><p><strong>L4：相当于全无人驾驶，大部分时间由车主导。</strong></p><p>L5：驾驶能力几乎与人类没有关系，没有方向盘、脚踏板以及其他接管设备。</p><hr><h3 id="L2级别无人驾驶">L2级别无人驾驶</h3><p>代表：Tesla Model X</p><ul><li>L2高级辅助驾驶</li><li>驾驶员需要对安全负责</li><li>启动条件要求不高（速度和车道线）</li><li>早期采用Mobileye提供感知技术</li><li>无法解决复杂路口问题</li><li>主动变道需要人提供变道指示</li></ul><h3 id="L3级别无人驾驶">L3级别无人驾驶</h3><p>代表：Audi A8</p><ul><li>L3自动驾驶</li><li>自动驾驶期间，车辆负全责</li><li>进入自动驾驶的条件苛刻（多车道路，60km/h以下速度，天气良好）</li><li>量产车里智能性最好的</li><li>暂时没有主动变道能力</li><li>遇到紧急情况或者不符合条件的情况需要通知驾驶员接管，10s时间。</li></ul><h3 id="L4级别无人驾驶">L4级别无人驾驶</h3><p>代表：Waymo</p><ul><li>L4全自动驾驶</li><li>曾有一段时间取消了安全员</li><li>商业运营阶段</li><li>世界最领先的自动驾驶能力</li><li>成本昂贵</li></ul><p>代表：Nuro</p><ul><li>特殊场景下的Level 4能力</li><li>物流配送车</li><li>成本要求不能太高</li><li>可以不用考虑乘坐体感</li><li>相关物流公司都有布局，比如：京东、美团、菜鸟等等</li></ul><p>实现思路：</p><ul><li>V2X：Vehicle to Everything（车路协同）<ul><li>V2V：Vehicle to Vehicle</li><li>V2I：Vehicle to Installtion</li><li>V2P：Vehicle to people</li></ul></li><li>边缘计算：RSU（路侧单元）、OBU（车载单元）</li><li>5G通信能力：LTE-V协议，专门针对车间通讯的协议，可兼容4G和5G</li><li>主车智能：深度学习</li><li>感知能力：高度复杂冗余的传感器</li><li>决策能力：大数据下的智能决策</li><li>高精地图：丰富的地图信息数据</li><li>定位：精确的位置获取能力</li><li>权责问题处理：RSS模型（Responsibility-Sensitive Safely）责任敏感安全模型：利用可衡量的具体数据来区分事故的责任</li></ul><h2 id="无人驾驶技术概述">无人驾驶技术概述</h2><ul><li>L4自动驾驶系统架构</li><li>自动驾驶硬件</li><li>自动驾驶软件</li></ul><h3 id="L4自动驾驶系统架构">L4自动驾驶系统架构</h3><p><img src="/study/course/self-driving/self-driving-01-intro/figure_01.png" alt></p><h3 id="自动驾驶硬件">自动驾驶硬件</h3><ul><li>128L LiDAR + 若干 16L LiDAR 辅助盲区，激光雷达平行偏下的感知范围</li><li>Front Cameras + Side Cameras</li><li>Radar 毫米波雷达，弥补光线不足时的感知能力</li><li>Industrial PC 工控机</li><li>IMU + GPS Antenna + GPS Receiver 定位</li></ul><h4 id="感知传感器">感知传感器</h4><ul><li>摄像头：物体识别、追踪</li><li>激光雷达：障碍物位置识别、绘制地图、辅助定位。精准度很高，作为主传感器</li><li>毫米波雷达：坏天气下辅助感知物体的位置和速度，观测距离远，但容易误检</li><li>超声波：近距离高敏，作为安全冗余设备检测车辆的碰撞安全问题</li></ul><h4 id="定位系统传感器">定位系统传感器</h4><ul><li>IMU：实时监测自身的姿态，200Hz或更高。包括了三个单轴的加速度计和三个单轴的陀螺仪。</li><li>GNSS：GPS，无人车一般使用RTK（载波相位差分）来实现定位，10Hz</li></ul><h4 id="计算单元">计算单元</h4><p>车载计算单元IPC：</p><ul><li>高效连接计算单元内部各计算设备，连接外部传感器的信息输入和存储</li><li>冗余设计，防止单点故障</li><li>考虑车规、电磁干扰和震动方面的设计以及ISO-26262标准的要求（ASIL D级别，故障率是10FIT，10亿各小时里面出一次故障）</li></ul><h4 id="车辆线控系统">车辆线控系统</h4><ul><li>线控系统：控制是由简单命令完成的，不是由物理操作。</li><li>将传统车辆的液压系统和真空助力泵 设计为 电控化的零部件。</li></ul><h3 id="自动驾驶软件">自动驾驶软件</h3><p><img src="/study/course/self-driving/self-driving-01-intro/figure_02.png" alt></p><ul><li>RTOS 实时操作系统</li><li>Framework 框架</li><li>各类模块</li><li>HMI 可视化</li><li>Cloud Platform 云平台</li></ul><p>感知系统（Computer Vision + Sensor Fusion）处理后，传递信息给定位系统（Localization），按照信息进行路径规划，最后控制系统执行行驶指令。</p><h4 id="RTOS">RTOS</h4><ul><li>实时操作系统<ul><li>QNX，类Unix系统，具有强实时性，符合车规级的实时操作系统</li><li>RT Linux，Linux内核补丁，通过软实时进行监控</li></ul></li><li>Framework：<ul><li>ROS，机器人操作系统：B-&gt;M</li><li>YARP、Microsoft Robotics、MOOS、Cybertron</li></ul></li></ul><h4 id="HD-Map">HD Map</h4><p>High Dimensional Map 作为其他模块的支撑</p><ul><li>不同于导航地图，最大的特点就是高纬度和高精度</li><li>道路网的精确三维表征，例如交叉路口布局和路标位置</li><li>地图语义信息，例如道路速度限制，左转车道开始的位置</li><li>导航地图只能达到米级精度，高精地图能达到厘米级</li><li>高精地图坐标系：WGS84、墨卡托坐标系</li><li>提供许多准确的静态物体信息</li><li>定位可以用于计算相对位置</li><li>帮助传感器缩小检测范围，缩小感兴趣区域ROI</li><li>计算道路导航信息</li><li>帮助车辆识别车道的确切中心线</li></ul><h4 id="Localization">Localization</h4><ul><li><p>无人车最重要的基础就是位置</p><ul><li><p>INS：Inertial Navigation System 惯性导航系统，较高更新频率</p><ul><li>IMU获取自身状态后通过状态矩阵递推下一时刻的位置</li><li>如果没有矫正的话，状态递推会随着时间不断产生误差，导致最终位置发散</li></ul></li><li><p>RTK，载波相位差分系统，较低更新频率</p><ul><li>多加了一个静止基站，基站位置不变，有固定坐标 $(X_{基站固定},Y_{基站固定})$ ，而基站从卫星获取到自身位置坐标为 $(X_{基站卫星},Y_{基站卫星})$ ，这二者的误差为 $Error=(X_{基站固定}-X_{基站卫星},Y_{基站固定}-Y_{基站卫星})$ ，该误差可以衡量车辆是否偏航，即汽车坐标应当在 $(X_{汽车卫星},Y_{汽车卫星}) \space ± \space Error $ 之间。</li></ul></li><li><p>通过 Kalman Filter 卡尔曼滤波整合两类数据各取优势，得出高准确性的实时信息。</p></li></ul></li><li><p>几何定位：</p><ul><li>激光雷达：点云</li><li>摄像头：图像</li><li>高精地图</li></ul><p>迭代最近点 ICP：每个时刻进行多次点云与高精地图位置的比对，选择最近点作为位置校正。</p><p>直方图滤波 Histogram Filter</p></li></ul><h4 id="Preception">Preception</h4><ul><li><p>任务：</p><ul><li><p>检测：得到物体在环境中的位置</p></li><li><p>分类：明确物体是什么</p></li><li><p>跟踪：随时间的持续观察移动物体并保持物体一致性</p></li><li><p>分割：图像中每个像素与语义类别进行匹配，合理语义分割</p></li></ul></li><li><p>方法：</p><ul><li>深度学习：监督学习、半监督学习、强化学习</li><li>R-CNN系列、YOLO、SSD</li></ul></li><li><p>计算融合：</p><ul><li>前融合：将数据基础信息进行融合，得到Raw Data</li><li>后融合：将不同传感器分别执行操作后，将数据融合</li></ul></li></ul><h4 id="Prediction">Prediction</h4><ul><li><p>任务</p><ul><li>实时性</li><li>准确性</li></ul></li><li><p>方法：</p><ul><li><p>基于状态进行检测</p><ul><li>Kalman Filter</li><li>Particle Filter</li></ul></li><li><p>基于车道序列进行预测通过</p><ul><li>机器学习模型化简为分类问题</li></ul></li><li><p>行人预测：行人预测很难</p></li></ul></li></ul><h4 id="Planning">Planning</h4><ul><li>任务<ul><li>导航线路规划</li><li>精细轨迹表述</li></ul></li><li>方法<ul><li>数学问题转换：将物理世界的地图转化为数学上的图表达</li><li>最优路径搜索：通过数学上的最优路径求解出确定解</li><li>考虑车辆的体感和安全性</li></ul></li></ul><h4 id="Control">Control</h4><ul><li>任务<ul><li>输入信息：目标轨迹、车辆状态，输出：方向盘、油门</li><li>时效性、准确性、稳定性要求都非常高</li></ul></li><li>方法：<ul><li>传统控制算法PID可以满足车辆控制要求，控制算法优化上由LQR、MPC</li></ul></li></ul><h2 id="问题总结">问题总结</h2><ul><li>我在哪？</li><li>我周围有什么？</li><li>他们要到哪里去？</li><li>我该怎么走？</li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;概述&lt;/h1&gt;
&lt;blockquote&gt;
&lt;p&gt;“A self-driving car, also known as a robot car, autonomous car, or driverless car, is a vehicle that is capable </summary>
      
    
    
    
    <category term="study" scheme="https://www.jk404.cn/categories/study/"/>
    
    <category term="course" scheme="https://www.jk404.cn/categories/study/course/"/>
    
    <category term="self-driving" scheme="https://www.jk404.cn/categories/study/course/self-driving/"/>
    
    
  </entry>
  
  <entry>
    <title>data_structure_03_stack_queue</title>
    <link href="https://www.jk404.cn/study/course/data-structure/data-structure-03-stack-queue/"/>
    <id>https://www.jk404.cn/study/course/data-structure/data-structure-03-stack-queue/</id>
    <published>2022-11-09T01:40:05.000Z</published>
    <updated>2022-11-22T09:28:21.842Z</updated>
    
    <content type="html"><![CDATA[<h1>Stack &amp; Queue</h1><p>重点内容：</p><ul><li>栈和队列的基本概念</li><li>栈和队列的顺序存储结构</li><li>栈和队列的链式存储结构</li><li>栈和队列的应用</li><li>特殊矩阵的压缩存储</li></ul><p>知识框架：</p><p><img src="/study/course/data-structure/data-structure-03-stack-queue/data-structure-03-stack-queue%5Cfigure_01.jpg" alt></p><h2 id="栈-stack">栈 stack</h2><h3 id="栈的基本概念">栈的基本概念</h3><p>栈(stack) - 许在一段进行插入或删除操作的线性表</p><p>栈顶(top) - 允许进行插入和删除的一端</p><p>栈底(bottom) - 固定的，不允许进行插入和删除的一端</p><p>空栈(empty stack) - 不含任何元素的空表</p><h3 id="栈的性质">栈的性质</h3><ul><li>后进先出(Last In First Out, LIFO)</li><li>$n$ 个不同元素进栈，出栈元素不同排列的个数为 $\frac{1}{n+1}C {2n\choose n}$，该公式也被称为卡特兰(Catalan)数，可采用数学归纳法证明</li></ul><h3 id="栈的基本操作">栈的基本操作</h3><ul><li><code>InitStack(&amp;S)</code> - 初始化空栈S</li><li><code>StackEmpty(S)</code> - 判断栈是否为空，空则返回<code>true</code>，不空则返回<code>false</code></li><li><code>Push(&amp;S, x)</code> - 进栈，若S未满，则将x加入</li><li><code>Pop(&amp;S, &amp;x)</code> - 出栈，若S非空，弹出并返回给x</li><li><code>GetTop(S, &amp;x)</code> - 获取栈顶元素，用x返回</li><li><code>DestroyStack(&amp;S)</code> - 销毁栈，释放空间</li></ul><h3 id="栈的顺序存储">栈的顺序存储</h3><p>采用顺序存储结构的栈称为<strong>顺序栈</strong>，利用一组地址连续的存储单元存放自栈底到栈顶的数据元素，用一个指针<code>top</code>指示当前栈顶元素的位置。</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="keyword">int</span> ElemType;</span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAXSIZE 50</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    ElemType data[MAXSIZE]; <span class="comment">/* 数组存放入栈元素 */</span></span><br><span class="line">    <span class="keyword">int</span> top;                <span class="comment">/* 指针，指向栈顶 */</span></span><br><span class="line">&#125; SeqStack;</span><br></pre></td></tr></table></figure><p>下面是一些基础操作：</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 栈初始化 */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">InitStack</span><span class="params">(SeqStack &amp;S)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 指针设置为-1 */</span></span><br><span class="line">    S.top = <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 判空 */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">isStackEmpty</span><span class="params">(SeqStack S)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (S.top == <span class="number">-1</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 入栈 */</span></span><br><span class="line"><span class="function">SeqStack <span class="title">Push</span><span class="params">(SeqStack &amp;S, ElemType x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (S.top &gt;= MAXSIZE)</span><br><span class="line">        <span class="keyword">return</span> S;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 指针+1后，数组对应索引赋值为x */</span></span><br><span class="line">        S.top++;</span><br><span class="line">        S.data[S.top] = x;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> S;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 弹出 */</span></span><br><span class="line"><span class="function">SeqStack <span class="title">Pop</span><span class="params">(SeqStack &amp;S, ElemType &amp;x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">isStackEmpty</span>(S))</span><br><span class="line">    &#123;</span><br><span class="line">        x = S.data[S.top];</span><br><span class="line">        S.top--;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> S;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 获取栈顶元素 */</span></span><br><span class="line"><span class="function">ElemType <span class="title">GetTop</span><span class="params">(SeqStack S)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">isStackEmpty</span>(S))</span><br><span class="line">        <span class="keyword">return</span> S.data[S.top];</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>利用栈底位置相对不变的特性，两个顺序栈共享一个一维数组空间，将两个栈的栈底分别设置在共享空间的两端，两个栈顶想可共享空间的中间延伸。这种形式称为：<strong>共享栈</strong>。</p><p><img src="/study/course/data-structure/data-structure-03-stack-queue/data-structure-03-stack-queue%5Cfigure_02.jpg" alt></p><ul><li><code>topA=-1, topB=MAXSIZE</code> 时，A、B栈为空</li><li><code>topB - topA = 1</code> 时，共享栈满</li><li>进栈：<ul><li>stackA：S.data[++S.top]</li><li>stackB：S.data[–S.top]</li></ul></li><li>出栈：<ul><li>stackA：S.data[S.top–]</li><li>stackB：S.data[S.top++]</li></ul></li></ul><h3 id="栈的链式存储">栈的链式存储</h3><p>链栈的优点时便于多个栈共享存储空间，提高效率，且不存在栈满溢出的情况。</p><p>规定单链表的表头为操作端，且规定链栈没有头结点。</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 链栈 */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LNode</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    ElemType data;</span><br><span class="line">    LNode *next;</span><br><span class="line"></span><br><span class="line">&#125; LNode, *LinkStack;</span><br></pre></td></tr></table></figure><h3 id="课后题">课后题</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;malloc.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stack&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">int</span> ElemType;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 链表结点 */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LNode</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="comment">/* data */</span></span><br><span class="line">    ElemType data;      <span class="comment">// 数据单元</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">LNode</span> *<span class="title">next</span>;</span> <span class="comment">// 指向下一个 node 的指针</span></span><br><span class="line">&#125; LNode, *Linklist;     <span class="comment">// 可以表示单结点或是指向整个链表的指针</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">PrintLinkList</span><span class="params">(Linklist L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (L-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        L = L-&gt;next; <span class="comment">/* 适用于带头结点时，越过头结点直接指向第一个元素域 */</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>, L-&gt;data);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function">Linklist <span class="title">InsertListFromTail</span><span class="params">(Linklist &amp;L, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 头结点 */</span></span><br><span class="line">    L = (Linklist)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line">    L-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 引入一个尾结点，初始化为与头结点相同的域 */</span></span><br><span class="line">    LNode *tail = L;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 获取数据域 */</span></span><br><span class="line">        ElemType x;</span><br><span class="line">        <span class="built_in">scanf_s</span>(<span class="string">&quot;%c&quot;</span>, &amp;x);</span><br><span class="line">        LNode *p = (LNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line">        p-&gt;data = x;</span><br><span class="line">        <span class="comment">/* 添加在 tail 后 */</span></span><br><span class="line">        p-&gt;next = tail-&gt;next;</span><br><span class="line">        tail-&gt;next = p;</span><br><span class="line">        <span class="comment">/* tail = p，使得下一个插入结点能够在 p 之后 */</span></span><br><span class="line">        tail = p;</span><br><span class="line">    &#125;</span><br><span class="line">    tail-&gt;next = <span class="literal">NULL</span>; <span class="comment">// 尾结点指针置空</span></span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 顺序栈 */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAXSIZE 50</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    ElemType data[MAXSIZE]; <span class="comment">/* 数组存放入栈元素 */</span></span><br><span class="line">    <span class="keyword">int</span> top;                <span class="comment">/* 指针，指向栈顶 */</span></span><br><span class="line">&#125; SeqStack;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 链栈 */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LSNode</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    ElemType data;</span><br><span class="line">    LSNode *next;</span><br><span class="line"></span><br><span class="line">&#125; LSNode, *LinkStack;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 栈初始化 */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">InitStack</span><span class="params">(SeqStack &amp;S)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 指针设置为-1 */</span></span><br><span class="line">    S.top = <span class="number">-1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 判空 */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">isStackEmpty</span><span class="params">(SeqStack S)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (S.top == <span class="number">-1</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 入栈 */</span></span><br><span class="line"><span class="function">SeqStack <span class="title">Push</span><span class="params">(SeqStack &amp;S, ElemType x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (S.top &gt;= MAXSIZE)</span><br><span class="line">        <span class="keyword">return</span> S;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 指针+1后，数组对应索引赋值为x */</span></span><br><span class="line">        S.top++;</span><br><span class="line">        S.data[S.top] = x;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> S;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 弹出 */</span></span><br><span class="line"><span class="function">SeqStack <span class="title">Pop</span><span class="params">(SeqStack &amp;S, ElemType &amp;x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">isStackEmpty</span>(S))</span><br><span class="line">    &#123;</span><br><span class="line">        x = S.data[S.top];</span><br><span class="line">        S.top--;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> S;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 获取栈顶元素 */</span></span><br><span class="line"><span class="function">ElemType <span class="title">GetTop</span><span class="params">(SeqStack S)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (!<span class="built_in">isStackEmpty</span>(S))</span><br><span class="line">        <span class="keyword">return</span> S.data[S.top];</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q3.判定操作是否合法 */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">isLegalOperate_Q3</span><span class="params">(<span class="keyword">char</span> op[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> j = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (op[i] != <span class="string">&#x27;\0&#x27;</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in"><span class="keyword">switch</span></span> (op[i])</span><br><span class="line">        &#123;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;I&#x27;</span>:</span><br><span class="line">            j++;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;O&#x27;</span>:</span><br><span class="line">            j--;</span><br><span class="line">            <span class="keyword">if</span> (j &lt; <span class="number">0</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="built_in">printf</span>(<span class="string">&quot;illegal\n&quot;</span>);</span><br><span class="line">                <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">            &#125;</span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        i++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (j != <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;illegal\n&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;legal\n&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q4.单链表表头指针为L，data为字符型，设计算法判断链表的全部n个字符是否中心对称 */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">isSymmetry_Q4</span><span class="params">(Linklist L, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 使用顺序栈，压入单链表前1/2的元素，然后逐一判断后续元素和栈顶元素是否一一相等 */</span></span><br><span class="line">    <span class="keyword">int</span> i;</span><br><span class="line">    ElemType *s = (ElemType *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(ElemType) * (n / <span class="number">2</span>));</span><br><span class="line">    LNode *p = L-&gt;next;</span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>; i &lt; n / <span class="number">2</span>; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        s[i] = p-&gt;data;</span><br><span class="line">        p = p-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    i--;</span><br><span class="line">    <span class="keyword">if</span> (n % <span class="number">2</span> == <span class="number">1</span>)</span><br><span class="line">        p = p-&gt;next;</span><br><span class="line">    <span class="keyword">while</span> (p &amp;&amp; s[i] == p-&gt;data)</span><br><span class="line">    &#123;</span><br><span class="line">        i--;</span><br><span class="line">        p = p-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (i == <span class="number">-1</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q5. 两个栈s1，s2 都采用顺序栈，共享一个存储区[0,...,maxsize-1]，采用栈顶相向，迎面增长的方式存储，设计s1、s2有关的入栈和出栈算法 */</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="comment">/* data */</span></span><br><span class="line">    ElemType data[MAXSIZE];</span><br><span class="line">    <span class="keyword">int</span> top[<span class="number">2</span>];</span><br><span class="line">&#125; shareS;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">sharePush</span><span class="params">(shareS ss, <span class="keyword">int</span> i, ElemType x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (i != <span class="number">0</span> || i != <span class="number">1</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">if</span> (ss.top[<span class="number">1</span>] - ss.top[<span class="number">0</span>] == <span class="number">1</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (i == <span class="number">0</span>)</span><br><span class="line">            ss.data[++ss.top[<span class="number">0</span>]] = x;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            ss.data[--ss.top[<span class="number">1</span>]] = x;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">sharePop</span><span class="params">(shareS ss, <span class="keyword">int</span> i, ElemType &amp;x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (i != <span class="number">0</span> || i != <span class="number">1</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="built_in"><span class="keyword">switch</span></span> (i)</span><br><span class="line">    &#123;</span><br><span class="line">    <span class="keyword">case</span> <span class="number">0</span>:</span><br><span class="line">        <span class="keyword">if</span> (ss.top[<span class="number">0</span>] != <span class="number">-1</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            x = ss.data[ss.top[<span class="number">0</span>]--];</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    <span class="keyword">case</span> <span class="number">1</span>:</span><br><span class="line">        <span class="keyword">if</span> (ss.top[<span class="number">1</span>] != MAXSIZE)</span><br><span class="line">        &#123;</span><br><span class="line">            x = ss.data[ss.top[<span class="number">1</span>]++];</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* Q3 */</span></span><br><span class="line">    <span class="comment">// char op[8];</span></span><br><span class="line">    <span class="comment">// for (int i = 0; i &lt; 8; i++)</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     scanf_s(&quot;%c&quot;, &amp;op[i]);</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">    <span class="comment">// isLegalOperate_Q3(op);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q4 */</span></span><br><span class="line">    <span class="comment">/* Linklist L;</span></span><br><span class="line"><span class="comment">    InsertListFromTail(L, 5);</span></span><br><span class="line"><span class="comment">    if (isSymmetry_Q4(L, 5))</span></span><br><span class="line"><span class="comment">    &#123;</span></span><br><span class="line"><span class="comment">        printf(&quot;is symmetry.\n&quot;);</span></span><br><span class="line"><span class="comment">    &#125;</span></span><br><span class="line"><span class="comment">    else</span></span><br><span class="line"><span class="comment">    &#123;</span></span><br><span class="line"><span class="comment">        printf(&quot;not symmetry.\n&quot;);</span></span><br><span class="line"><span class="comment">    &#125; */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q5 */</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="队列-queue">队列 queue</h2><h3 id="队列的基本概念">队列的基本概念</h3><p>Queue也是操作受限的线性表，只允许在表的一端进行插入，在表的另一端进行删除。</p><h3 id="队列的性质">队列的性质</h3><ul><li>先进先出 First In First Out，FIFO</li><li>队头 Front，允许删除的一端</li><li>队尾 Rear，允许插入的一端</li><li>空队列，不含任何元素的空表</li></ul><h3 id="队列的基本操作">队列的基本操作</h3><ul><li><code>InitQueue(&amp;Q)</code> - 初始化队列，构造一个空队列Q</li><li><code>QueueEmpty(Q)</code> - 队列判空</li><li><code>EnQueue(&amp;Q, x)</code> - 入队，若队列Q未满则将x加入，x称为新的队尾</li><li><code>DeQueue(&amp;Q, &amp;x)</code> - 出队，若队列Q非空，删除队头元素，用x返回</li><li><code>GetHead(Q, &amp;x)</code> - 读队头元素，返回给x</li></ul><p>不能随意读取栈或队列中间的某个数据。</p><h3 id="队列的顺序存储">队列的顺序存储</h3><p>一般采用的是循环队列：</p><ul><li>入队时，使用<code>(front+1)%MAXSIZE</code>更新rear的值</li><li>出队时，使用<code>(front+1)%MAXSIZE</code>更新front的值</li><li>牺牲一个元素的空间，用于区分队空和队满：<ul><li><code>(rear + 1) % MAXSIZE == CQ.front</code> 时，表示队满</li><li><code>rear == front</code> 时，表示队空</li></ul></li></ul><p>数据结构</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="comment">/* data */</span></span><br><span class="line">    ElemType data[MAXSIZE];</span><br><span class="line">    <span class="keyword">int</span> front, rear;</span><br><span class="line">&#125; CircleQueue;</span><br></pre></td></tr></table></figure><p>初始化</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">CircleQueue <span class="title">InitCirQueue</span><span class="params">(CircleQueue &amp;CQ)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    CQ.front = <span class="number">0</span>;</span><br><span class="line">    CQ.rear = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">return</span> CQ;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>判空</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">isCirQueueEmpty</span><span class="params">(CircleQueue CQ)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (CQ.front == CQ.rear)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>入队和出队</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">EnCirQueue</span><span class="params">(CircleQueue &amp;CQ, ElemType x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> ((CQ.rear + <span class="number">1</span>) % MAXSIZE == CQ.front)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    CQ.data[CQ.rear] = x;</span><br><span class="line">    CQ.rear = (CQ.rear + <span class="number">1</span>) % MAXSIZE;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">DeCirQueue</span><span class="params">(CircleQueue &amp;CQ, ElemType &amp;x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (CQ.rear == CQ.front)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    x = CQ.data[CQ.front];</span><br><span class="line">    CQ.front = (CQ.front + <span class="number">1</span>) % MAXSIZE;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="队列的链式存储">队列的链式存储</h3><p>实际上，链式队列是一个带有头指针和尾指针的单链表。</p><p>数据结构</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="comment">/* data */</span></span><br><span class="line">    ElemType data;</span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">LinkNode</span> *<span class="title">next</span>;</span></span><br><span class="line">&#125; LinkNode;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="comment">/* data */</span></span><br><span class="line">    LinkNode *front, *rear;</span><br><span class="line">&#125; LinkQueue;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>初始化</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">LinkQueue <span class="title">InitLQueue</span><span class="params">(LinkQueue &amp;LQ)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    LQ.front = LQ.rear = (LinkNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LinkNode));</span><br><span class="line">    LQ.front-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>判空</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">IsLQueueEmpty</span><span class="params">(LinkQueue LQ)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (LQ.front == LQ.rear)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>入队和出队</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">EnLQueue</span><span class="params">(LinkQueue &amp;LQ, ElemType x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    LinkNode *newElement = (LinkNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LinkNode));</span><br><span class="line">    newElement-&gt;data = x;</span><br><span class="line">    newElement-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    LQ.rear-&gt;next = newElement;</span><br><span class="line">    LQ.rear = newElement;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">DeLQueue</span><span class="params">(LinkQueue &amp;LQ, ElemType &amp;x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (LQ.rear == LQ.front)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    LinkNode *delElement = LQ.front-&gt;next;</span><br><span class="line">    x = delElement-&gt;data;</span><br><span class="line"></span><br><span class="line">    LQ.front-&gt;next = delElement-&gt;next;</span><br><span class="line">    <span class="keyword">if</span> (delElement == LQ.rear)</span><br><span class="line">        LQ.front == LQ.rear;</span><br><span class="line">    <span class="built_in"><span class="keyword">delete</span></span> (delElement);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="双端队列">双端队列</h3><p>双端队列：允许两端都可以入队和出队的队列，分为前端和后端。</p><p>输入受限的双端队列：在一端进行插入和删除，而另一端只允许删除。（只有一端可以输入，两端都可以输出）</p><p>输出受限的双端队列：在一端进行插入和删除，而另一端只允许插入。（只有一端可以输出，两端都可以输入）</p><h3 id="课后题-v2">课后题</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* Q1. 使用tag来最大化利用循环队列的空间 */</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;malloc.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MAXSIZE 50</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">int</span> ElemType;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="comment">/* data */</span></span><br><span class="line">    ElemType data[MAXSIZE];</span><br><span class="line">    <span class="keyword">int</span> front, rear;</span><br><span class="line">    <span class="keyword">int</span> tag; <span class="comment">/* 用于区分队满还是队空 0空 1满 */</span></span><br><span class="line">&#125; CircleQueue;</span><br><span class="line"></span><br><span class="line"><span class="function">CircleQueue <span class="title">InitCirQueue</span><span class="params">(CircleQueue &amp;CQ)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    CQ.front = <span class="number">0</span>;</span><br><span class="line">    CQ.rear = <span class="number">0</span>;</span><br><span class="line">    CQ.tag = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">return</span> CQ;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">EnCirQueue</span><span class="params">(CircleQueue &amp;CQ, ElemType x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (CQ.rear == CQ.front &amp;&amp; CQ.tag == <span class="number">1</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    CQ.data[CQ.rear] = x;</span><br><span class="line">    CQ.rear = (CQ.rear + <span class="number">1</span>) % MAXSIZE;</span><br><span class="line">    CQ.tag = <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">DeCirQueue</span><span class="params">(CircleQueue &amp;CQ, ElemType &amp;x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (CQ.rear == CQ.front)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    x = CQ.data[CQ.front];</span><br><span class="line">    CQ.front = (CQ.front + <span class="number">1</span>) % MAXSIZE;</span><br><span class="line">    CQ.tag = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* Q2. 逆置队列 */</span></span><br><span class="line"><span class="function">CircleQueue <span class="title">ReverseQueue</span><span class="params">(CircleQueue &amp;CQ, SeqStack &amp;S)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 其实就是利用队列和栈的性质，将队列的元素依次压入栈中，将栈中的元素依次弹出到队列中 */</span></span><br><span class="line">    ElemType x;</span><br><span class="line">    <span class="keyword">while</span> (!isCirQueueEmpty)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">DeCirQueue</span>(CQ, x);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>, x);</span><br><span class="line">        <span class="built_in">Push</span>(S, x);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">while</span> (!isStackEmpty)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">Pop</span>(S, x);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>, x);</span><br><span class="line">        <span class="built_in">EnCirQueue</span>(CQ, x);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="栈和队列的应用">栈和队列的应用</h2><ul><li>栈：括号匹配、表达式求值、递归</li><li>队列：层次遍历</li></ul><h3 id="括号匹配">括号匹配</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">isMatch</span><span class="params">(<span class="keyword">char</span> *c, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    SeqStack S;</span><br><span class="line">    <span class="built_in">InitStack</span>(S);</span><br><span class="line">    <span class="keyword">char</span> ch;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in"><span class="keyword">switch</span></span> (c[i])</span><br><span class="line">        &#123;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;&#123;&#x27;</span> || <span class="string">&#x27;[&#x27;</span> || <span class="string">&#x27;(&#x27;</span>:</span><br><span class="line">            <span class="built_in">Push</span>(S, c[i]);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;&#125;&#x27;</span>:</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">GetTop</span>(S) == <span class="string">&#x27;&#123;&#x27;</span>)</span><br><span class="line">                <span class="built_in">Pop</span>(S, ch);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;]&#x27;</span>:</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">GetTop</span>(S) == <span class="string">&#x27;[&#x27;</span>)</span><br><span class="line">                <span class="built_in">Pop</span>(S, ch);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">case</span> <span class="string">&#x27;(&#x27;</span>:</span><br><span class="line">            <span class="keyword">if</span> (<span class="built_in">GetTop</span>(S) == <span class="string">&#x27;)&#x27;</span>)</span><br><span class="line">                <span class="built_in">Pop</span>(S, ch);</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        <span class="keyword">default</span>:</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (<span class="built_in">isStackEmpty</span>(S))</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;Stack &amp;amp; Queue&lt;/h1&gt;
&lt;p&gt;重点内容：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;栈和队列的基本概念&lt;/li&gt;
&lt;li&gt;栈和队列的顺序存储结构&lt;/li&gt;
&lt;li&gt;栈和队列的链式存储结构&lt;/li&gt;
&lt;li&gt;栈和队列的应用&lt;/li&gt;
&lt;li&gt;特殊矩阵的压缩存储&lt;/li</summary>
      
    
    
    
    <category term="study" scheme="https://www.jk404.cn/categories/study/"/>
    
    <category term="course" scheme="https://www.jk404.cn/categories/study/course/"/>
    
    <category term="data structure" scheme="https://www.jk404.cn/categories/study/course/data-structure/"/>
    
    
  </entry>
  
  <entry>
    <title>CS61A_lab_00_intro</title>
    <link href="https://www.jk404.cn/study/course/CS-61A-Structure-and-Interpretation-of-Computer-Programs/lab/CS61A-lab-00-intro/"/>
    <id>https://www.jk404.cn/study/course/CS-61A-Structure-and-Interpretation-of-Computer-Programs/lab/CS61A-lab-00-intro/</id>
    <published>2022-11-04T12:00:05.000Z</published>
    <updated>2022-11-04T16:04:18.405Z</updated>
    
    <content type="html"><![CDATA[<h1>Introduction</h1><p><a href="https://cs61a.org/lab/lab00/lab00.zip">lab00</a></p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">wget https://cs61a.org/lab/lab00/lab00.zip</span><br><span class="line">unzip lab00.zip</span><br></pre></td></tr></table></figure><p>实验目录下有如下文件：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ ls</span><br><span class="line">__pycache__  lab00.ok  lab00.py  ok  tests</span><br></pre></td></tr></table></figure><p>下面是解释：</p><ul><li><code>lab00.py</code>: the starter file you just edited</li><li><code>ok</code>: our testing program</li><li><code>lab00.ok</code>: a configuration file for Ok</li></ul><p>关于 <code>ok</code> 的命令生成器：</p><p><a href="https://ok-help.cs61a.org/">okpy Command Generator</a></p><p><code>lab00</code> 要求很简单：</p><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">def</span> <span class="title">twenty_twenty_two</span>():</span></span><br><span class="line">    <span class="string">&quot;&quot;&quot;Come up with the most creative expression that evaluates to 2022,</span></span><br><span class="line"><span class="string">    using only numbers and the +, *, and - operators.</span></span><br><span class="line"><span class="string"></span></span><br><span class="line"><span class="string">    &gt;&gt;&gt; twenty_twenty_two()</span></span><br><span class="line"><span class="string">    2022</span></span><br><span class="line"><span class="string">    &quot;&quot;&quot;</span></span><br><span class="line">    <span class="keyword">return</span> <span class="number">2022</span></span><br></pre></td></tr></table></figure><p>用表达式返回值 2022 即可，这里我就直接返回了😸。</p><figure class="highlight shell"><table><tr><td class="code"><pre><span class="line">python3 ok</span><br></pre></td></tr></table></figure><p>执行，如果想submit，可以换成 <code>python3 ok submit</code> 。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;Introduction&lt;/h1&gt;
&lt;p&gt;&lt;a href=&quot;https://cs61a.org/lab/lab00/lab00.zip&quot;&gt;lab00&lt;/a&gt;&lt;/p&gt;
&lt;figure class=&quot;highlight plaintext&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td cl</summary>
      
    
    
    
    <category term="study" scheme="https://www.jk404.cn/categories/study/"/>
    
    <category term="course" scheme="https://www.jk404.cn/categories/study/course/"/>
    
    <category term="CS 61A:Structure and Interpretation of Computer Programs" scheme="https://www.jk404.cn/categories/study/course/CS-61A-Structure-and-Interpretation-of-Computer-Programs/"/>
    
    <category term="lab" scheme="https://www.jk404.cn/categories/study/course/CS-61A-Structure-and-Interpretation-of-Computer-Programs/lab/"/>
    
    
  </entry>
  
  <entry>
    <title>CS61A_lecture_01_computer_science</title>
    <link href="https://www.jk404.cn/study/course/CS-61A-Structure-and-Interpretation-of-Computer-Programs/lecture/CS61A-lecture-01-computer-science/"/>
    <id>https://www.jk404.cn/study/course/CS-61A-Structure-and-Interpretation-of-Computer-Programs/lecture/CS61A-lecture-01-computer-science/</id>
    <published>2022-11-03T21:45:24.000Z</published>
    <updated>2022-12-17T16:45:29.830Z</updated>
    
    <content type="html"><![CDATA[<h1>Computer Science</h1><p><a href="http://composingprograms.com">Online textbook</a></p><h2 id="What-is-CS">What is CS?</h2><p>The study of:</p><ul><li>What problems can be solved using computation</li><li>How to solve those problems</li><li>What techniques lead to effective solutions</li></ul><p>Systems - Artificial Intelligence - Graphics - Security - Networking - Programming Languages - Theory - Scientific Computing</p><p>Artificial Intelligence:</p><ul><li>Decision Making</li><li>Robotics</li><li>Natural Language Processing<ul><li>Answering Questions</li><li>Dialog</li><li>Translation</li></ul></li><li>…</li></ul><h2 id="What-is-this-Course-about">What is this Course about?</h2><p>about managing complexity</p><ul><li>Mastering abstraction</li><li>Programming paradigms</li></ul><p>An introduction to programming</p><ul><li>Full understanding of Python fundamentals</li><li>Combining multiple ideas in large projects</li><li>How computers interpret programing languages</li></ul><p>Different types of languages: Scheme &amp; SQL</p><h2 id="Course-Policies">Course Policies</h2><p>Learning - Community - Course Staff</p><h2 id="Code">Code</h2><figure class="highlight python"><table><tr><td class="code"><pre><span class="line"><span class="comment"># Numeric expressions</span></span><br><span class="line"><span class="number">2022</span></span><br><span class="line"><span class="number">2000</span> + <span class="number">22</span></span><br><span class="line"><span class="number">1</span> + <span class="number">2</span> + <span class="number">3</span> + <span class="number">4</span> * ((<span class="number">5</span> // <span class="number">6</span>) + <span class="number">7</span> * <span class="number">8</span> * <span class="number">9</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Functions</span></span><br><span class="line"><span class="built_in">abs</span>(-<span class="number">2</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Values</span></span><br><span class="line"><span class="string">&quot;Go Bears&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="comment"># Objects</span></span><br><span class="line"><span class="comment"># Note: Download from http://composingprograms.com/shakespeare.txt</span></span><br><span class="line">shakes = <span class="built_in">open</span>(<span class="string">&#x27;shakespeare.txt&#x27;</span>)</span><br><span class="line">text = shakes.read().split()</span><br><span class="line"><span class="built_in">len</span>(text)</span><br><span class="line">text[:<span class="number">25</span>]</span><br><span class="line">text.count(<span class="string">&#x27;the&#x27;</span>)</span><br><span class="line">text.count(<span class="string">&#x27;thou&#x27;</span>)</span><br><span class="line">text.count(<span class="string">&#x27;you&#x27;</span>)</span><br><span class="line">text.count(<span class="string">&#x27;forsooth&#x27;</span>)</span><br><span class="line">text.count(<span class="string">&#x27;,&#x27;</span>)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Sets</span></span><br><span class="line">words = <span class="built_in">set</span>(text)</span><br><span class="line"><span class="built_in">len</span>(words)</span><br><span class="line"></span><br><span class="line"><span class="comment"># Combinations </span></span><br><span class="line"><span class="string">&#x27;draw&#x27;</span></span><br><span class="line"><span class="string">&#x27;draw&#x27;</span>[<span class="number">0</span>]</span><br><span class="line">&#123;w[<span class="number">0</span>] <span class="keyword">for</span> w <span class="keyword">in</span> words&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment"># Data</span></span><br><span class="line"><span class="string">&#x27;draw&#x27;</span>[::-<span class="number">1</span>]</span><br><span class="line">&#123;w <span class="keyword">for</span> w <span class="keyword">in</span> words <span class="keyword">if</span> w == w[::-<span class="number">1</span>] <span class="keyword">and</span> <span class="built_in">len</span>(w)&gt;<span class="number">4</span>&#125;</span><br><span class="line">&#123;w <span class="keyword">for</span> w <span class="keyword">in</span> words <span class="keyword">if</span> w[::-<span class="number">1</span>] <span class="keyword">in</span> words <span class="keyword">and</span> <span class="built_in">len</span>(w) == <span class="number">4</span>&#125;</span><br><span class="line">&#123;w <span class="keyword">for</span> w <span class="keyword">in</span> words <span class="keyword">if</span> w[::-<span class="number">1</span>] <span class="keyword">in</span> words <span class="keyword">and</span> <span class="built_in">len</span>(w) &gt; <span class="number">6</span>&#125;</span><br></pre></td></tr></table></figure><p>得到结果：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">&gt;&gt;&gt; 2022</span><br><span class="line">2022</span><br><span class="line">&gt;&gt;&gt; 2022+22</span><br><span class="line">2044</span><br><span class="line">&gt;&gt;&gt; 1 + 2 + 3 + 4 * ((5 // 6) + 7 * 8 * 9)</span><br><span class="line">2022</span><br><span class="line">&gt;&gt;&gt; abs(-2)</span><br><span class="line">2</span><br><span class="line">&gt;&gt;&gt; &quot;Go Bears&quot;</span><br><span class="line">&#x27;Go Bears&#x27;</span><br><span class="line">&gt;&gt;&gt; shakes = open(&quot;shakespeare.txt&quot;)</span><br><span class="line">&gt;&gt;&gt; text=shakes.read().split()</span><br><span class="line">&gt;&gt;&gt; len(text)</span><br><span class="line">980637</span><br><span class="line">&gt;&gt;&gt; text[:25]</span><br><span class="line">[&#x27;A&#x27;, &quot;MIDSUMMER-NIGHT&#x27;S&quot;, &#x27;DREAM&#x27;, &#x27;Now&#x27;, &#x27;,&#x27;, &#x27;fair&#x27;, &#x27;Hippolyta&#x27;, &#x27;,&#x27;, &#x27;our&#x27;, &#x27;nuptial&#x27;, &#x27;hour&#x27;, &#x27;Draws&#x27;, &#x27;on&#x27;, &#x27;apace&#x27;, &#x27;:&#x27;, &#x27;four&#x27;, &#x27;happy&#x27;, &#x27;days&#x27;, &#x27;bring&#x27;, &#x27;in&#x27;, &#x27;Another&#x27;, &#x27;moon&#x27;, &#x27;;&#x27;, &#x27;but&#x27;, &#x27;O&#x27;]</span><br><span class="line">&gt;&gt;&gt; text.count(&quot;you&quot;)</span><br><span class="line">12361</span><br><span class="line">&gt;&gt;&gt; text.count(&quot;thou&quot;)</span><br><span class="line">4501</span><br><span class="line">&gt;&gt;&gt; text.count(&quot;smile&quot;)</span><br><span class="line">70</span><br><span class="line">&gt;&gt;&gt; text.count(&quot;missing&quot;)</span><br><span class="line">7</span><br><span class="line">&gt;&gt;&gt; text.count(&quot;golden&quot;)</span><br><span class="line">82</span><br><span class="line">&gt;&gt;&gt; words=set(text)</span><br><span class="line">&gt;&gt;&gt; len(words)</span><br><span class="line">33505</span><br><span class="line">&gt;&gt;&gt; &#x27;draw&#x27;</span><br><span class="line">&#x27;draw&#x27;</span><br><span class="line">&gt;&gt;&gt; &#x27;draw&#x27;[0]</span><br><span class="line">&#x27;d&#x27;</span><br><span class="line">&gt;&gt;&gt; &#123;w[0] for w in words&#125;</span><br><span class="line">&#123;&#x27;s&#x27;, &#x27;B&#x27;, &#x27;J&#x27;, &#x27;!&#x27;, &#x27;T&#x27;, &#x27;1&#x27;, &#x27;w&#x27;, &#x27;D&#x27;, &#x27;2&#x27;, &#x27;U&#x27;, &#x27;,&#x27;, &#x27;O&#x27;, &#x27;I&#x27;, &#x27;;&#x27;, &#x27;m&#x27;, &#x27;&quot;&#x27;, &#x27;7&#x27;, &#x27;F&#x27;, &#x27;H&#x27;, &#x27;h&#x27;, &#x27;N&#x27;, &#x27;i&#x27;, &#x27;P&#x27;, &#x27;A&#x27;, &#x27;[&#x27;, &#x27;9&#x27;, &#x27;p&#x27;, &#x27;4&#x27;, &#x27;?&#x27;, &#x27;S&#x27;, &#x27;M&#x27;, &#x27;c&#x27;, &#x27;K&#x27;, &#x27;&amp;&#x27;, &#x27;e&#x27;, &#x27;z&#x27;, &#x27;Y&#x27;, &#x27;E&#x27;, &#x27;o&#x27;, &#x27;g&#x27;, &#x27;a&#x27;, &#x27;C&#x27;, &#x27;.&#x27;, &#x27;6&#x27;, &#x27;l&#x27;, &#x27;W&#x27;, &#x27;u&#x27;, &#x27;V&#x27;, &#x27;v&#x27;, &#x27;5&#x27;, &#x27;Q&#x27;, &#x27;R&#x27;, &#x27;G&#x27;, &#x27;L&#x27;, &#x27;Z&#x27;, &#x27;f&#x27;, &#x27;d&#x27;, &#x27;r&#x27;, &#x27;:&#x27;, &#x27;8&#x27;, &#x27;q&#x27;, &#x27;]&#x27;, &#x27;3&#x27;, &#x27;y&#x27;, &#x27;X&#x27;, &#x27;n&#x27;, &#x27;k&#x27;, &#x27;j&#x27;, &#x27;b&#x27;, &#x27;t&#x27;, &quot;&#x27;&quot;&#125;</span><br><span class="line">&gt;&gt;&gt; &#x27;draw&#x27;[::-1]</span><br><span class="line">&#x27;ward&#x27;</span><br><span class="line">&gt;&gt;&gt; &#123;w for w in words if w == w[::-1] and len(w)&gt;4&#125;</span><br><span class="line">&#123;&#x27;redder&#x27;, &#x27;rever&#x27;, &#x27;minim&#x27;, &#x27;level&#x27;, &#x27;refer&#x27;, &#x27;madam&#x27;&#125;</span><br><span class="line">&gt;&gt;&gt; &#123;w for w in words if w[::-1] in words and len(w) == 4&#125;</span><br><span class="line">&#123;&#x27;maws&#x27;, &#x27;rail&#x27;, &#x27;pots&#x27;, &#x27;meed&#x27;, &#x27;deed&#x27;, &#x27;ward&#x27;, &#x27;trow&#x27;, &#x27;rats&#x27;, &#x27;swam&#x27;, &#x27;pool&#x27;, &#x27;lees&#x27;, &#x27;nips&#x27;, &#x27;trop&#x27;, &#x27;bats&#x27;, &#x27;keel&#x27;, &#x27;seel&#x27;, &#x27;ecce&#x27;, &#x27;doom&#x27;, &#x27;esse&#x27;, &#x27;spot&#x27;, &#x27;draw&#x27;, &#x27;pins&#x27;, &#x27;wolf&#x27;, &#x27;evil&#x27;, &#x27;liar&#x27;, &#x27;tops&#x27;, &#x27;drab&#x27;, &#x27;poop&#x27;, &#x27;live&#x27;, &#x27;brag&#x27;, &#x27;meet&#x27;, &#x27;reed&#x27;, &#x27;flow&#x27;, &#x27;stab&#x27;, &#x27;pooh&#x27;, &#x27;hoop&#x27;, &#x27;peep&#x27;, &#x27;dial&#x27;, &#x27;spin&#x27;, &#x27;gums&#x27;, &#x27;rood&#x27;, &#x27;laid&#x27;, &#x27;teem&#x27;, &#x27;stop&#x27;, &#x27;snip&#x27;, &#x27;moor&#x27;, &#x27;spit&#x27;, &#x27;deer&#x27;, &#x27;star&#x27;, &#x27;sees&#x27;, &#x27;bard&#x27;, &#x27;gnat&#x27;, &#x27;wort&#x27;, &#x27;leek&#x27;, &#x27;garb&#x27;, &#x27;part&#x27;, &#x27;smug&#x27;, &#x27;leer&#x27;, &#x27;deem&#x27;, &#x27;port&#x27;, &#x27;reel&#x27;, &#x27;trap&#x27;, &#x27;mood&#x27;, &#x27;tang&#x27;, &#x27;elle&#x27;, &#x27;tips&#x27;, &#x27;noon&#x27;, &#x27;loop&#x27;, &#x27;room&#x27;, &#x27;door&#x27;&#125;</span><br><span class="line">&gt;&gt;&gt; &#123;w for w in words if w[::-1] in words and len(w) &gt; 6&#125;</span><br><span class="line">set()</span><br></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;Computer Science&lt;/h1&gt;
&lt;p&gt;&lt;a href=&quot;http://composingprograms.com&quot;&gt;Online textbook&lt;/a&gt;&lt;/p&gt;
&lt;h2 id=&quot;What-is-CS&quot;&gt;What is CS?&lt;/h2&gt;
&lt;p&gt;The stud</summary>
      
    
    
    
    <category term="study" scheme="https://www.jk404.cn/categories/study/"/>
    
    <category term="course" scheme="https://www.jk404.cn/categories/study/course/"/>
    
    <category term="CS 61A:Structure and Interpretation of Computer Programs" scheme="https://www.jk404.cn/categories/study/course/CS-61A-Structure-and-Interpretation-of-Computer-Programs/"/>
    
    <category term="lecture" scheme="https://www.jk404.cn/categories/study/course/CS-61A-Structure-and-Interpretation-of-Computer-Programs/lecture/"/>
    
    
  </entry>
  
  <entry>
    <title>Linux_kernel_development_01_intro</title>
    <link href="https://www.jk404.cn/study/book/Linux-Kernel-Development/Linux-kernel-development-01-intro/"/>
    <id>https://www.jk404.cn/study/book/Linux-Kernel-Development/Linux-kernel-development-01-intro/</id>
    <published>2022-11-02T17:32:37.000Z</published>
    <updated>2022-12-17T17:44:16.099Z</updated>
    
    <content type="html"><![CDATA[<h1>Linux内核简介</h1><p>第一章从Unix的历史认识Linux内核和Linux操作系统的前世今生。</p><h2 id="Unix历史">Unix历史</h2><p>Unix诞生于贝尔实验室的一个失败的多用户操作系统Multics。</p><p>1969年，贝尔实验室的程序员设计了一个文件系统原型，最终该原型发展成了Unix。Thompson首先在一台无人问津的 PDP-7 型机上实现了这个操作系统。</p><p>1971年，Unix被移植到了 PDP-11 型机中。</p><p>1973年，Unix操作系统用C语言进行了重写。</p><p>1977年，推出了Unix System III。</p><p>1977年，加州大学伯克利分校推出了1BSD系统，实现基于贝尔实验室的Unix版本。</p><p>1978年，推出了2BSD系统，里面博阿寒csh、vi等软件。</p><p>1979年，推出了独立开发的Unix系统，3BSD，支持虚拟内存。</p><p>1994年，重写了虚拟内存子系统VM，推出了最终官方版4.4BSD。</p><p>Unix强大的原因：</p><ul><li>Unix简洁，仅仅提供几百个系统调用并且有一个非常明确的设计目的。</li><li>Unix中，万物皆为文件，抽象使得对数据和对设备的操作通过一套相同的系统调用接口来进行。</li><li>内核和相关系统工具软件是用C语言编写的。</li><li>进程创建非常迅速，有一个非常独特的<code>fork()</code>系统调用。</li></ul><p>今天，Unix已经发展成为一个支持抢占式都多任务、多线程、虚拟内存、换页、动态链接和TCP/IP网络的现代化系统。</p><h2 id="Linux简介">Linux简介</h2><p>Linux是类Unix系统，但不是Unix。它借鉴了Unix许多设计，并且实现了Unix的API，但没有直接使用Unix的源代码。Linux是一个非商业化的产品，任何人都可以开发内核，内核也是自由公开的软件，使用GNU GPL 2.0作为限制条款。</p><h2 id="操作系统和内核简介">操作系统和内核简介</h2><p><strong>操作系统</strong> - 在整个系统中负责完成最基本的功能和系统管理，包括内核、设备驱动程序、启动引导程序、命令行Shell或者其他种类的用户界面、基本的文件管理工具和系统工具。</p><p><strong>内核</strong> - 操作系统的内在核心，系统其他部分依靠内核提供的服务。内核独立于普通应用程序，一般处于系统态，拥有受保护的内存空间和访问硬件设备的所有权限。</p><p><strong>内核空间</strong> - 系统态和被保护的空间</p><p><strong>系统调用</strong> - 应用程序通过系统调用来与内核通信。</p><p><strong>应用程序通过系统调用陷于内核</strong> - 当一个应用程序在执行一条系统调用，称其为内核正在代其执行，应用程序被称为通过系统调用在内核空间运行，而内核被称为运行于进程上下文中。</p><p><strong>中断</strong> - 硬件想要和系统通信时，发出异步的中断信号去打断处理器的执行，继而打断内核执行。中断对应一个中断号，内核通过中断号找到对应的中断服务程序，执行特定响应。中断服务程序在一个与所有进程无关的、专门的中断上下文中运行。</p><img src="/study/book/Linux-Kernel-Development/Linux-kernel-development-01-intro/figure_01.jpg" style="zoom:50%;"><p>处理器在任何指定时间点上的活动概括为：</p><ul><li>运行于用户空间，执行用户进程</li><li>运行于内核空间，处于进程上下文，代表某个特定的进程执行</li><li>运行于内核空间，处于中断上下文，与任何进程无关，处理某个特定的中断</li></ul><h1>Linux内核与传统Unix内核的比较</h1><p>Unix内核是一个不可分割的静态可执行库，通常需要硬件系统提供页机制（MMU）以管理内存，早期Linux也需要MMU支持。</p><p>操作系统内核分为两个阵营：</p><ul><li>单内核</li><li>微内核</li></ul><p>（第三阵营为外内核，通常用于科研系统）</p><h2 id="单内核">单内核</h2><p>把内核从整体桑作为一个单独的执行过程，运行在单独的地址空间上。通常以单个静态二进制文件存放在磁盘中。多数Unix系统都设计为单内核模块。</p><h2 id="微内核">微内核</h2><p>把内核分为多个独立的过程，每个过程称为服务器，它们运行在各自独立空间中。理性状况下，只有强烈请求特权服务的服务器才运行在特权模式下，其余过程都运行在用户空间。系统通过进程间通信（IPC）机制，各个服务器通过消息传递处理微内核通信。</p><p>由于IPC机制带来的开销多于函数调用，又因为内核态和用户态空间的上下文切换，因此消息传递需要一定的周期。基于此，所有实际应用的基于微内核的系统都让大部分或全部服务器放在内核中，以此实现对函数的直接调用，消除频繁的上下文切换。Windows NT和Mach内核就是微内核的典型实例，但它们在最新版本中不让任何微内核服务器运行在用户空间，这违背了微内核设计初衷。</p><p>Linux是单内核，但汲取了微内核的精华：模块化设计、抢占式内核、支持内核线程以及动态装载内核模块的能力。Linux尽力避免微内核设计上的性能缺陷，让所有任务运行在内核态，直接调用函数，无须消息传递。</p><h1>Linux内核特点</h1><ul><li>支持动态加载内核模块</li><li>支持对称多处理（SMP）机制</li><li>支持抢占（preemptive）</li><li>对于线程支持的实现：不区分线程和其他一般进程，进程只不过是其中的一些共享资源。</li><li>提供设备类的面向对象的设备模型、热插拔事件</li><li>忽略了被认为设计的很拙劣的Unix特性和一些难以实现的过时标准</li><li>…</li></ul><h1>Linux内核版本</h1><p>内核的两种版本：</p><ul><li>稳定版本</li><li>处于开发版本</li></ul><p>Linux通过一个命名机制来区分：</p><p>使用三个或四个”.”分割的数字来代表不同内核版本。</p><ul><li>第一个数字为 主版本号</li><li>第二个数字为 从版本号（奇数：开发版；偶数：稳定版）</li><li>第三个数字为 修订版本号</li><li>第四个可选数字为 稳定版本号</li></ul><h1>内核开发者社区</h1><p><a href="http://vger.kernel.org">http://vger.kernel.org</a></p><p>Linux kernel mailing list - lkml</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;Linux内核简介&lt;/h1&gt;
&lt;p&gt;第一章从Unix的历史认识Linux内核和Linux操作系统的前世今生。&lt;/p&gt;
&lt;h2 id=&quot;Unix历史&quot;&gt;Unix历史&lt;/h2&gt;
&lt;p&gt;Unix诞生于贝尔实验室的一个失败的多用户操作系统Multics。&lt;/p&gt;
&lt;p&gt;1969年</summary>
      
    
    
    
    <category term="study" scheme="https://www.jk404.cn/categories/study/"/>
    
    <category term="book" scheme="https://www.jk404.cn/categories/study/book/"/>
    
    <category term="Linux Kernel Development" scheme="https://www.jk404.cn/categories/study/book/Linux-Kernel-Development/"/>
    
    
  </entry>
  
  <entry>
    <title>Qt5.9_cpp_programming_guide_02_GUI_Base</title>
    <link href="https://www.jk404.cn/study/book/Qt5-9-c-%E5%BC%80%E5%8F%91%E6%8C%87%E5%8D%97/Qt5-9-cpp-programming-guide-02-GUI-Base/"/>
    <id>https://www.jk404.cn/study/book/Qt5-9-c-%E5%BC%80%E5%8F%91%E6%8C%87%E5%8D%97/Qt5-9-cpp-programming-guide-02-GUI-Base/</id>
    <published>2022-10-31T23:59:39.000Z</published>
    <updated>2022-11-02T17:56:52.821Z</updated>
    
    <content type="html"><![CDATA[<h1>GUI应用程序设计基础</h1><p>Qt Creator 设计 GUI 应用程序的基本方法：</p><ul><li>Qt创建的应用程序项目和基本组织结构</li><li>可视化设计的UI界面文件的原理和运行机制</li><li>信号与槽的使用方法</li><li>窗体可视化设计的底层原理</li><li>应用程序的窗体、组件布局、菜单、工具栏、Actions等的使用</li></ul><h2 id="UI文件设计与运行机制">UI文件设计与运行机制</h2><p>在Qt Creator中新建一个 Widget Application 项目 ，基类中选择 QWidget 作为窗体基类，选中 Generate form 。</p><h3 id="项目文件">项目文件</h3><p><code>samp2_1.pro</code> 文件内容如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">QT       += core gui</span><br><span class="line"></span><br><span class="line">greaterThan(QT_MAJOR_VERSION, 4): QT += widgets</span><br><span class="line"></span><br><span class="line">CONFIG += c++11</span><br><span class="line"></span><br><span class="line">DEFINES += QT_DEPRECATED_WARNINGS</span><br><span class="line"></span><br><span class="line">SOURCES += \</span><br><span class="line">    main.cpp \</span><br><span class="line">    widget.cpp</span><br><span class="line"></span><br><span class="line">HEADERS += \</span><br><span class="line">    widget.h</span><br><span class="line"></span><br><span class="line">FORMS += \</span><br><span class="line">    widget.ui</span><br><span class="line"></span><br><span class="line">qnx: target.path = /tmp/$$&#123;TARGET&#125;/bin</span><br><span class="line">else: unix:!android: target.path = /opt/$$&#123;TARGET&#125;/bin</span><br><span class="line">!isEmpty(target.path): INSTALLS += target</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>项目管理文件用于记录项目的一些设置，以及项目包含文件的组织管理。</p><p><code>QT       += core gui</code> 表示项目中加入了 core gui 模块，core gui 是 Qt 用于 GUI 设计的类库模块，如果创建的是控制台应用程序，就不需要添加 core gui 。</p><p>Qt类库以模块的形式组织各种功能的类，根据功能需求可以适当添加类库模块支持，例如，如果用到了数据库操作，则需要用到 sql 模块，在 pro 文件中需要加入 <code>QT       += sql</code> 。</p><p><code>greaterThan(QT_MAJOR_VERSION, 4): QT += widgets</code> 这是一个条件执行语句，表示当Qt主版本大于 4 时，才加入 widgets 模块。</p><p>除此外，加入 <code>TARGET = samp2_1</code> 表示生成的目标可执行文件的名称， <code>TEMPLATE = app</code> 表示项目使用的模板是 app，是一般的应用程序。</p><h3 id="界面文件">界面文件</h3><ul><li><p>组件面板：位于左侧，设计的常见组件都能在其中找到；</p></li><li><p>设计窗口：位于中间，拖放即可；</p></li><li><p>Signals 和 Slots编辑器 与 Action 编辑器 位于下方，用于可视化进行信号与槽的关联，以及可视化设计Action；</p></li><li><p>布局和界面设计工具栏：上方工具栏；</p></li><li><p>对象浏览器：右上方 Object Inspector，用树状视图显示窗体上各组件之间的布局包含关系，视图有两列，显示每个组件的对象名称和类名称；</p></li><li><p>属性编辑器：右下方 Property Editor，显示某个选中组件或窗体的各种属性和取值；</p><p><img src="/study/book/Qt5-9-c-%E5%BC%80%E5%8F%91%E6%8C%87%E5%8D%97/Qt5-9-cpp-programming-guide-02-GUI-Base/Qt5-9-cpp-programming-guide-02-GUI-Base%5Cfigure_01.jpg" alt></p><p><code>label : QLabel</code> 表示这个组件是一个QLabel类的组件；</p><p>QLabel 的继承关系为 QObject -&gt; QWidge -&gt; QFrame -&gt; QLabel ；</p></li></ul><p>放置一个 Push Button 组件，并为按钮增加一个功能：单击此按钮，关闭窗口退出程序。在信号与槽编辑器的工具栏上点击 Add 按钮，在出现条目中，Sender 选择按钮的 ObjectName ， Signal 选择 clicked() ， Receiver 选择窗体 Widget，Slot 选择 close()，这样设置后：当按钮被点击时，就执行 Widget 的 close() 函数，关闭窗口。</p><p><img src="/study/book/Qt5-9-c-%E5%BC%80%E5%8F%91%E6%8C%87%E5%8D%97/Qt5-9-cpp-programming-guide-02-GUI-Base/Qt5-9-cpp-programming-guide-02-GUI-Base%5Cfigure_02.jpg" alt></p><h3 id="主函数文件">主函数文件</h3><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;widget.h&quot;</span></span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;QApplication&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">(<span class="keyword">int</span> argc, <span class="keyword">char</span> *argv[])</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="function">QApplication <span class="title">a</span><span class="params">(argc, argv)</span></span>;<span class="comment">// 定义并创建应用程序</span></span><br><span class="line">    Widget w;<span class="comment">// 定义并创建窗口</span></span><br><span class="line">    w.<span class="built_in">show</span>();<span class="comment">// 窗口显示</span></span><br><span class="line">    <span class="keyword">return</span> a.<span class="built_in">exec</span>();<span class="comment">// 应用程序运行</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="窗体相关文件">窗体相关文件</h3><p>项目编译后，会生成ui_widget.h文件，因此共有四个文件：</p><ul><li>widget.h - 定义窗体类的头文件，定义了类 Widget</li><li>widget.cpp - Widget类的功能实现源程序文件</li><li>widget.ui - 窗体界面文件，由UI设计器自动生成，存储了窗体上各个组件的属性设置和布局</li><li>ui_widget.h - 编译后，根据窗体上的组件、信号和槽的关联等自动生成一个类的定义文件</li></ul><h4 id="widget-h">widget.h</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">ifndef</span> WIDGET_H</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> WIDGET_H</span></span><br><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;QWidget&gt;</span></span></span><br><span class="line"></span><br><span class="line">QT_BEGIN_NAMESPACE</span><br><span class="line"><span class="keyword">namespace</span> Ui &#123; <span class="class"><span class="keyword">class</span> <span class="title">Widget</span>;</span> &#125;<span class="comment">// 一个命名空间Ui， 包括一个类Widget</span></span><br><span class="line">QT_END_NAMESPACE</span><br><span class="line"></span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span> :</span> <span class="keyword">public</span> QWidget</span><br><span class="line">&#123;</span><br><span class="line">    Q_OBJECT</span><br><span class="line"></span><br><span class="line"><span class="keyword">public</span>:</span><br><span class="line">    <span class="built_in">Widget</span>(QWidget *parent = <span class="literal">nullptr</span>);</span><br><span class="line">    ~<span class="built_in">Widget</span>();</span><br><span class="line"></span><br><span class="line"><span class="keyword">private</span>:</span><br><span class="line">    Ui::Widget *ui;<span class="comment">// 使用Ui::Widget 定义了指针 *ui</span></span><br><span class="line">&#125;;</span><br><span class="line"><span class="meta">#<span class="meta-keyword">endif</span> <span class="comment">// WIDGET_H</span></span></span><br><span class="line"></span><br></pre></td></tr></table></figure><p>创建项目时，选择窗体基类为 QWidget ，因此在其中定义了一个继承自 QWidget 的类 Widget。</p><ul><li><p>namespace 声明</p><p><code>namespace Ui&#123;class Widget;&#125;</code></p><p>这里声明了一个名称为 Ui 的命名空间，其中包含一个类 Widget，这个类并不是本文件内定义的类，而是 ui_widget.h 中定义的类，用于描述界面组件。这个声明相当于一个外部类型声明。</p></li><li><p>Widget 类的定义</p><p>widget.h 中定义了一个继承于 QWidget 的类 Widget，是本实例的窗体类。</p><p>在类中使用了宏 Q_OBJECT，这是使用Qt signal和slot机制的类必须加入的一个宏。</p><p>在 public 部分定义了类的构造函数和析构函数</p><p>在 private 部分定义了一个指针 <code>Ui::Widget *ui</code> ，这个指针式用前面声明的 namespace Ui 里的 Widget 类定义的，所以指针ui式只想可视化界面的，通过该指针访问界面上的组件。</p></li></ul><h4 id="widget-cpp">widget.cpp</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;widget.h&quot;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&quot;ui_widget.h&quot;</span></span></span><br><span class="line"></span><br><span class="line">Widget::<span class="built_in">Widget</span>(QWidget *parent)</span><br><span class="line">    : <span class="built_in">QWidget</span>(parent)</span><br><span class="line">    , <span class="built_in">ui</span>(<span class="keyword">new</span> Ui::Widget)</span><br><span class="line">&#123;</span><br><span class="line">    ui-&gt;<span class="built_in">setupUi</span>(<span class="keyword">this</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line">Widget::~<span class="built_in">Widget</span>()</span><br><span class="line">&#123;</span><br><span class="line">    <span class="keyword">delete</span> ui;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>在 widget.cpp 中，目前只有构造函数和析构函数。</p><p>其中构造函数为：</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">Widget::<span class="built_in">Widget</span>(QWidget *parent)</span><br><span class="line">    : <span class="built_in">QWidget</span>(parent)</span><br><span class="line">    , <span class="built_in">ui</span>(<span class="keyword">new</span> Ui::Widget)</span><br><span class="line">&#123;</span><br><span class="line">    ui-&gt;<span class="built_in">setupUi</span>(<span class="keyword">this</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>执行父类 <code>QWidget</code> 的构造函数，创建一个 <code>Ui::Widget</code> 类的对象 <code>ui</code>。这个 <code>ui</code> 就是 <code>Widget</code> 的 <code>private</code> 部分所定义的指针变量 <code>ui</code> 。而后，执行 <code>Ui::Widget</code> 类中的 <code>setupUi()</code> 函数，这个函数实现窗口的生成和各种属性的设置，包括信号与槽的关联。</p><p>其中析构函数只是简单地删除了用 <code>new</code> 创建的指针 <code>ui</code> 。</p><h4 id="widget-ui">widget.ui</h4><p>窗口界面定义文件，是一个XML文件，定义了窗口上所有组件的属性设置、布局，及其信号与槽函数的关联。</p><h4 id="ui-widget-h">ui_widget.h</h4><p>实在对 widget.ui 文件编译后生成的一个文件，并不会出现在 Qt Creator 项目文件目录树中，但可以手工添加。 [右键项目名称节点] - [Add Existing Files …] ，添加即可。</p><p><code>ui_widget.h</code> 文件主要做了以下的工作：</p><ul><li><p>定义了一个类 <code>Ui_Widget</code> ，用于封装可视化设计的界面。</p></li><li><p>自动生成界面各个组件的类成员变量定义。例如，在可视化界面添加了按钮组件，并把它的 <code>objectName</code> 设置为 <code>btnClose</code> ，那么生成的定义就是：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">QPushButton *btnClose;</span><br></pre></td></tr></table></figure></li><li><p>定义了函数<code>setupUi()</code>用于创建各个界面组件，并设置其位置、大小、文字内容等；</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">setupUi</span><span class="params">(QWidget *Widget)</span></span></span><br><span class="line"><span class="function">    </span>&#123;</span><br><span class="line">        <span class="keyword">if</span> (Widget-&gt;<span class="built_in">objectName</span>().<span class="built_in">isEmpty</span>())</span><br><span class="line">            Widget-&gt;<span class="built_in">setObjectName</span>(QString::<span class="built_in">fromUtf8</span>(<span class="string">&quot;Widget&quot;</span>));</span><br><span class="line">        Widget-&gt;<span class="built_in">resize</span>(<span class="number">800</span>, <span class="number">600</span>);</span><br><span class="line">        QFont font;</span><br><span class="line">        font.<span class="built_in">setPointSize</span>(<span class="number">20</span>);</span><br><span class="line">        Widget-&gt;<span class="built_in">setFont</span>(font);</span><br><span class="line">        label = <span class="keyword">new</span> <span class="built_in">QLabel</span>(Widget);</span><br><span class="line">        label-&gt;<span class="built_in">setObjectName</span>(QString::<span class="built_in">fromUtf8</span>(<span class="string">&quot;label&quot;</span>));</span><br><span class="line">        label-&gt;<span class="built_in">setGeometry</span>(<span class="built_in">QRect</span>(<span class="number">290</span>, <span class="number">250</span>, <span class="number">281</span>, <span class="number">61</span>));</span><br><span class="line">        pushButton = <span class="keyword">new</span> <span class="built_in">QPushButton</span>(Widget);</span><br><span class="line">        pushButton-&gt;<span class="built_in">setObjectName</span>(QString::<span class="built_in">fromUtf8</span>(<span class="string">&quot;pushButton&quot;</span>));</span><br><span class="line">        pushButton-&gt;<span class="built_in">setGeometry</span>(<span class="built_in">QRect</span>(<span class="number">290</span>, <span class="number">430</span>, <span class="number">181</span>, <span class="number">41</span>));</span><br><span class="line"></span><br><span class="line">        <span class="built_in">retranslateUi</span>(Widget);</span><br><span class="line">        QObject::<span class="built_in">connect</span>(pushButton, <span class="built_in">SIGNAL</span>(<span class="built_in">clicked</span>()), Widget, <span class="built_in">SLOT</span>(<span class="built_in">close</span>()));</span><br><span class="line"></span><br><span class="line">        QMetaObject::<span class="built_in">connectSlotsByName</span>(Widget);</span><br><span class="line">    &#125; <span class="comment">// setupUi</span></span><br></pre></td></tr></table></figure><ul><li><p>根据可视化设计的界面内容，以代码形式创建界面上的各组件，并设置属性；</p></li><li><p>调用函数<code>retranslateUi(Widget)</code>，设置各组件的文字内容。</p></li><li><p>设置信号和槽的关联：</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">QObject::<span class="built_in">connect</span>(pushButton, <span class="built_in">SIGNAL</span>(<span class="built_in">clicked</span>()), Widget, <span class="built_in">SLOT</span>(<span class="built_in">close</span>()));</span><br><span class="line">QMetaObject::<span class="built_in">connectSlotsByName</span>(Widget);</span><br></pre></td></tr></table></figure><p>第一行调用 <code>connect()</code> 函数，将在UI设计器里的设置的信号与槽的关联转换成语句，在这里将 <code>btnClose</code> 按键的 <code>clicked()</code> 信号与窗体 <code>Widget</code> 的 <code>close()</code> 槽函数关联起来。</p><p>第二行设置了关联的方式，用于将UI设计器自动生成的组件信号的槽函数与组件信号相关联。</p></li></ul></li><li><p>定义 <code>namepspace Ui</code> ，定义一个从 <code>Ui_Widget</code> 继承的类 <code>Widget</code> 。</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="keyword">namespace</span> Ui&#123;</span><br><span class="line"><span class="class"><span class="keyword">class</span> <span class="title">Widget</span>:</span> <span class="function"><span class="keyword">public</span> <span class="title">Ui_Widget</span><span class="params">()</span></span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>由此，<code>widget.h</code> 引入该文件后，定义的 <code>Widget</code> 就继承了 <code>ui_widget.h</code> 中 <code>Ui_Widget</code> 类。</p></li></ul><h2 id="可视化UI设计">可视化UI设计</h2><h3 id="组件与功能">组件与功能</h3><p>程序的主功能是对中间一个文本框的文字字体样式和颜色进行设置。</p><p><img src="/study/book/Qt5-9-c-%E5%BC%80%E5%8F%91%E6%8C%87%E5%8D%97/Qt5-9-cpp-programming-guide-02-GUI-Base/Qt5-9-cpp-programming-guide-02-GUI-Base%5Cfigure_03.png" alt></p><p>对于界面组件的属性设置，需要注意：</p><ul><li><code>objectName</code> 是窗体上创建的组件的实例名称，界面上的每个组件需要有一个唯一的 <code>objectName</code> ，程序里访问界面组件时都是通过其 <code>objectName</code> 进行访问，自动生成槽函数名称里也有 <code>objectName</code> 。所以，组件的 <code>objectName</code> 需要在设计程序之前设置好，设置好之后一般不要再改动。若设计程序之后再改动 <code>objectName</code> ，涉及的代码需要相应改动。</li><li>窗体的 <code>objectName</code> 就是窗体的类名称，在UI设计器里不要修改，其实例名称需要在使用窗体的代码里定义。</li></ul><h3 id="组件布局">组件布局</h3><p>Qt界面设计使用了布局 Layout 功能，所谓布局，就是界面上组件的排列方式，使用布局可以使组件有规则地而非内部，并且随着窗体大小变化自动地调整大小和相对位置。布局很重要！</p><ul><li><p>界面组件的层次关系</p><p>为了将界面上各个组件分布设计更加美观，经常使用一些容器类，比如 <code>QgroupBox</code> 、<code>OtabWidget</code>、<code>QFrame</code>等等。</p><p><img src="/study/book/Qt5-9-c-%E5%BC%80%E5%8F%91%E6%8C%87%E5%8D%97/Qt5-9-cpp-programming-guide-02-GUI-Base/Qt5-9-cpp-programming-guide-02-GUI-Base%5Cfigure_04.jpg" alt></p><p><img src="/study/book/Qt5-9-c-%E5%BC%80%E5%8F%91%E6%8C%87%E5%8D%97/Qt5-9-cpp-programming-guide-02-GUI-Base/Qt5-9-cpp-programming-guide-02-GUI-Base%5Cfigure_05.jpg" alt></p><p>可以看到三类按钮被分别放置在了不同的三个<code>groupBox</code>里，这样布局就比较美观。除此之外，我还将窗口整体布局设置为水平布局。</p></li><li><p>布局管理</p><p><img src="/study/book/Qt5-9-c-%E5%BC%80%E5%8F%91%E6%8C%87%E5%8D%97/Qt5-9-cpp-programming-guide-02-GUI-Base/Qt5-9-cpp-programming-guide-02-GUI-Base%5Cfigure_06.jpg" alt></p></li><li><p>伙伴关系与Tab顺序</p><p>UI设计工具栏中的 <code>Edit Buddies</code> 按钮可以进入伙伴关系编辑状态，伙伴关系指的是界面上一个Label和一个组件相关联。例如，表示“姓名”的标签<code>nameLabel</code> 和 输入框可以进行伙伴关系绑定；</p><p><img src="/study/book/Qt5-9-c-%E5%BC%80%E5%8F%91%E6%8C%87%E5%8D%97/Qt5-9-cpp-programming-guide-02-GUI-Base/Qt5-9-cpp-programming-guide-02-GUI-Base%5Cfigure_09.jpg" alt></p><p>设置伙伴关系；</p><p><img src="/study/book/Qt5-9-c-%E5%BC%80%E5%8F%91%E6%8C%87%E5%8D%97/Qt5-9-cpp-programming-guide-02-GUI-Base/Qt5-9-cpp-programming-guide-02-GUI-Base%5Cfigure_08.jpg" alt></p><p>在Label的Text属性的“姓名”后加上 <code>(&amp;N)</code>，在“年龄”后加上 <code>(&amp;A)</code> ，<code>&amp;</code> 会自动隐藏，在用户输入 <code>Alt+N</code> 后自动锁定到姓名后面的输入框。</p><p>UI设计工具栏中的 <code>Edit Tab Order</code> 按钮可以进入 Tab 顺序编辑状态。按Tab键时输入焦点的移动顺序，在按Tab键时，焦点应该以合理的顺序在界面上移动，而不是随意地移动。</p></li></ul><h3 id="信号与槽">信号与槽</h3><p><code>Signal &amp; Slot</code> 是 Qt 编程的基础，也是 Qt 的一大创新。因为有了信号与槽的编程机制，在 Qt 中处理界面各个组件的交互操作时变得更加直观和简单。</p><p>**信号(Signal)**就是在特定情况下被发射的事件，例如 <code>PushButton</code> 最常见的信号就是鼠标单击时发射的 <code>clicked()</code> 信号，一个 <code>ComboBox</code> 最常见的信号是选择的列表项变化时发射的 <code>CurrentIndexChanged()</code> 信号。GUI 程序设计的主要内容就是对界面上各组件的信号的响应，只需要知道什么情况下发射哪些信号，合理地去响应和处理这些信号就可以了。</p><p>**槽(Slot)**就是对信号响应的函数，槽就是一个函数，与一般的C++函数是一样的，可以定义在类的任何部分（public、private或protected），可以具有任何参数，也可以被直接调用。槽函数与一般的函数不同的是：槽函数可以与一个信号关联，当信号被发射时，关联的槽函数被自动执行。</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">QObject::<span class="built_in">connect</span>(sender, <span class="built_in">SIGNAL</span>(<span class="built_in">signal</span>()), receiver, <span class="built_in">SLOT</span>(<span class="built_in">slot</span>()));</span><br></pre></td></tr></table></figure><p><code>connect()</code> 是 <code>QObject</code> 类的一个静态函数，而 <code>QObject</code> 是所有Qt类的基类，在实际调用时可以忽略前面的限定符，所以可以直接写为：</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="built_in">connect</span>(sender, <span class="built_in">SIGNAL</span>(<span class="built_in">signal</span>()), receiver, <span class="built_in">SLOT</span>(<span class="built_in">slot</span>()));</span><br></pre></td></tr></table></figure><p>其中，<code>sender</code> 是发射信号的对象的名称，<code>signal()</code> 是信号名称。信号可以看作是特殊的函数，需要带括号，有参数时还需要指明参数。<code>receiver</code> 时接收信号的对象名称，<code>slot()</code> 是槽函数的名称，需要带括号，有参数时还需要指明参数。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;GUI应用程序设计基础&lt;/h1&gt;
&lt;p&gt;Qt Creator 设计 GUI 应用程序的基本方法：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;Qt创建的应用程序项目和基本组织结构&lt;/li&gt;
&lt;li&gt;可视化设计的UI界面文件的原理和运行机制&lt;/li&gt;
&lt;li&gt;信号与槽的使用方法&lt;/li&gt;
&lt;</summary>
      
    
    
    
    <category term="study" scheme="https://www.jk404.cn/categories/study/"/>
    
    <category term="book" scheme="https://www.jk404.cn/categories/study/book/"/>
    
    <category term="Qt5.9 c++ 开发指南" scheme="https://www.jk404.cn/categories/study/book/Qt5-9-c-%E5%BC%80%E5%8F%91%E6%8C%87%E5%8D%97/"/>
    
    
  </entry>
  
  <entry>
    <title>Qt5.9_cpp_programming_guide_01_meet</title>
    <link href="https://www.jk404.cn/study/book/Qt5-9-c-%E5%BC%80%E5%8F%91%E6%8C%87%E5%8D%97/Qt5-9-cpp-programming-guide-01-meet/"/>
    <id>https://www.jk404.cn/study/book/Qt5-9-c-%E5%BC%80%E5%8F%91%E6%8C%87%E5%8D%97/Qt5-9-cpp-programming-guide-01-meet/</id>
    <published>2022-10-31T21:50:06.000Z</published>
    <updated>2022-11-01T01:33:06.981Z</updated>
    
    <content type="html"><![CDATA[<h1>认识Qt</h1><p>Qt是一套应用程序开发类库，和MFC不同，Qt是跨平台的。Qt支持PC和服务器的平台，包括Windows、Linux、macOS等，还支持移动和嵌入式操作系统，比如iOS、Embedded Linux、Android、WinRT等。跨平台意味着只需要一次编程，在不同平台上无需改动或只需少许改动后再编译，就可以形成在不同平台上运行的版本。</p><h2 id="获取与安装">获取与安装</h2><h3 id="许可类型">许可类型</h3><ul><li>商业许可</li><li>开源许可<ul><li>LGPLV3</li><li>GPLV2/GPLV3</li></ul></li></ul><p>商业许可 - 允许开发者不公开项目的源代码，其Qt版本包含更多的模块（这些模块只存在于商业许可的版本），并能够获得Qt公司的计数支持。需要向Qt购买商业许可才能获得。</p><p>开源许可 - 无需支付，但是需要遵守相关规定。</p><h3 id="版本">版本</h3><p>Qt版本更新很快，且版本更新时会新增一些类或停止维护一些以前版本的类，Qt 5和Qt 4就有较大区别，如果不是为了维护旧版本编写的程序，一定要选用最新版本的Qt进行开发。</p><h3 id="下载和安装">下载和安装</h3><p>从Qt官网可以下载最新版本的Qt软件，根据开发项目不同，Qt分为桌面和移动设备应用开发、嵌入式设备开发 两大类不同的安装包。</p><ul><li>桌面和移动设备应用开发 - PC、服务器、手机、平板电脑上运行的程序，免费下载使用</li><li>嵌入式设备开发 - 物联网设备、汽车电子设备等特定的嵌入式设备</li></ul><p>需要注册用户后才可以下载Qt安装程序，安装包分为在线和离线两种，为便于重复安装，最好下载离线安装包。</p><p>安装时需要选择想要安装的组件，其中有：</p><ul><li>MinGW - Minimalist GNU for Windows 的缩写，是Windows平台上使用的GNU工具集导入库的集合</li><li>用于UWP编译的模块 - Universal Windows Platform 的缩写，有不同编译器类型的UWP</li><li>用于Windows平台上的MSVC编译器模块 - 如 msvc2015 32-bit 等，要安装 MSVC 编译器的模块，需要计算机上安装相应版本的Visual Studio</li><li>用于Android平台的模块 - 如 Android x86 和 Android ARMv7</li><li>Sources - Qt源程序</li><li>Qt Charts - 二位图标模块，用于绘制各类型二位图标</li><li>Qt Data Visualization - 三维数据图表模块</li><li>Qt Purchasing、QtWebEngine、Qt Network Auth(TP)</li><li>Qt Script(Deprecated) - 已经过时的脚本模块</li></ul><h2 id="初步使用">初步使用</h2><h3 id="设置">设置</h3><p>[Tools] - [Options]</p><ul><li>Environment 可切换语言</li><li>Kits中可以设置编译工具</li></ul><p><a href="https://developer.microsoft.com/zh-cn/windows/downloads/sdk-archive/">Windows Software Development Kit</a></p><h2 id="第一个程序">第一个程序</h2><h3 id="项目创建流程">项目创建流程</h3><p>[File] - [New FIle or Project]</p><p>Qt Creator中可以创建多种项目：</p><ul><li>Qt Widgets Application 支持桌面平台的有图形用户界面(Graphic User Interface,GUI)</li><li>Qt Console Application 控制台应用程序，无GUI界面</li><li>Qt Quick Application 创建可部署的Qt Quick 2应用程序，界面采用QML语言，程序采用C++，可以设计出非常炫酷的用户界面，一般用于移动设备或嵌入设备上无边框的应用程序设计</li><li>Qt Quick Controls 2 Application 创建基于Qt Quick Controls 2组件的可部署的Qt Quick 2应用程序</li><li>Qt Canvas 3D Application 创建Qt Canvas 3D QML项目，支持3D画布</li></ul><p>在这里选择项目类型为 Qt Widgets Application，选择目录，再设置项目名称，这样在该目录下会创建一个项目文件夹。</p><p>选择编译工具，在编译项目时可以选择一个作为当前使用的编译工具，编译生成不同版本的可执行程序。</p><p>选择基类(base class)，三种类型可以选择：</p><ul><li>QMainWindow 是主窗口类，带有主菜单栏、工具栏和状态栏；</li><li>QWidget 是所有具有可视界面类的基类，选择QWidget创建的界面对各种界面组件都可以支持；</li><li>QDialog 是对话框类，可建立一个基于对话框的界面；</li></ul><h3 id="项目文件组成和管理">项目文件组成和管理</h3><p>在项目名称节点下，分组管理着项目内的各种源文件，分别为：</p><ul><li><p><a href="http://xxx.pro">xxx.pro</a> 项目管理文件，包括一些对项目的设置项；</p></li><li><p>Headers分组，该节点下是项目内所有头文件(.h)；</p></li><li><p>Sources分组，该节点下是项目内的所有C++源文件(.cpp)，mainwindow.cpp是主窗口类的实现文件，main.cpp是主函数文件，也是程序的入口；</p></li><li><p>Forms分组，该节点下是项目内的所有界面文件(.ui)，是用XML语言描述的。</p></li></ul><p>在下拉栏里，有如下的几个分组：</p><p><img src="/study/book/Qt5-9-c-%E5%BC%80%E5%8F%91%E6%8C%87%E5%8D%97/Qt5-9-cpp-programming-guide-01-meet/Qt5-9-cpp-programming-guide-01-meet%5Cfigure_01.jpg" alt></p><p>其中 Class View 可以显示项目内所有的类结构，便于快速切换和浏览；</p><p>双击 mainwindow.ui 会出现窗体设计界面，实际上是继承的 Qt Designer。</p><h3 id="编译、调试和执行">编译、调试和执行</h3><p>[Projects] - [Build&amp;Run] 选择哪一个编译器用于编译项目，选择[Shadow build] 会在编译后在项目同级目录下建立一个编译后的目录，如果不选择此项，会在目录下建立 [Debug] 和 [Release] 子目录。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;认识Qt&lt;/h1&gt;
&lt;p&gt;Qt是一套应用程序开发类库，和MFC不同，Qt是跨平台的。Qt支持PC和服务器的平台，包括Windows、Linux、macOS等，还支持移动和嵌入式操作系统，比如iOS、Embedded Linux、Android、WinRT等。跨平台意味着只</summary>
      
    
    
    
    <category term="study" scheme="https://www.jk404.cn/categories/study/"/>
    
    <category term="book" scheme="https://www.jk404.cn/categories/study/book/"/>
    
    <category term="Qt5.9 c++ 开发指南" scheme="https://www.jk404.cn/categories/study/book/Qt5-9-c-%E5%BC%80%E5%8F%91%E6%8C%87%E5%8D%97/"/>
    
    
  </entry>
  
  <entry>
    <title>Gopl_02_introduction</title>
    <link href="https://www.jk404.cn/study/book/The-Go-Programming-Language/Gopl-02-introduction/"/>
    <id>https://www.jk404.cn/study/book/The-Go-Programming-Language/Gopl-02-introduction/</id>
    <published>2022-10-24T18:28:23.000Z</published>
    <updated>2022-10-31T21:45:02.209Z</updated>
    
    <content type="html"><![CDATA[<h1>入门</h1><p>先扯一下go mod的问题。</p><p>在1.13版本后引入了管理用的modules机制。</p><p>执行：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">go mod init gopl</span><br></pre></td></tr></table></figure><p>得到与工作目录gopl并列的go.mod，其内容如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">module gopl</span><br><span class="line"></span><br><span class="line">go 1.17</span><br></pre></td></tr></table></figure><h2 id="hello">hello</h2><p>依旧是从hello,world开始：</p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// main.go</span></span><br><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> <span class="string">&quot;fmt&quot;</span></span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">fmt.Println(<span class="string">&quot;hello, 世界&quot;</span>)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>执行结果为：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">hello, 世界</span><br></pre></td></tr></table></figure><p>这里用中文是因为Go语言原生支持Unicode，它可以处理全世界任何语言的文本。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ go build main.go</span><br></pre></td></tr></table></figure><p>这个命令生成一个名为main的可执行的二进制文件（译注：Windows系统下生成的可执行文件是main.exe，增加了.exe后缀名），之后你可以随时运行它（译注：在Windows系统下在命令行直接输入main.exe命令运行），不需任何处理。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ ./main</span><br><span class="line">hello, 世界</span><br></pre></td></tr></table></figure><p>Go语言的代码通过<strong>包</strong>（package）组织，包类似于其它语言里的库（libraries）或者模块（modules）。一个包由位于单个目录下的一个或多个.go源代码文件组成, 目录定义包的作用。每个源文件都以一条<code>package</code>声明语句开始，这个例子里就是<code>package main</code>, 表示该文件属于哪个包，紧跟着一系列导入（import）的包，之后是存储在这个文件里的程序语句。</p><p>Go的标准库提供了100多个包，以支持常见功能，如输入、输出、排序以及文本处理。比如<code>fmt</code>包，就含有格式化输出、接收输入的函数。<code>Println</code>是其中一个基础函数，可以打印以空格间隔的一个或多个值，并在最后添加一个换行符，从而输出一整行。</p><p><code>main</code>包比较特殊。它定义了一个独立可执行的程序，而不是一个库。在<code>main</code>里的<code>main</code> <em>函数</em> 也很特殊，它是整个程序执行时的入口（译注：C系语言差不多都这样）。<code>main</code>函数所做的事情就是程序做的。当然了，<code>main</code>函数一般调用其它包里的函数完成很多工作, 比如, <code>fmt.Println</code>。</p><p>必须告诉编译器源文件需要哪些包，这就是跟随在<code>package</code>声明后面的<code>import</code>声明扮演的角色。hello world例子只用到了一个包，大多数程序需要导入多个包。</p><p>必须恰当导入需要的包，缺少了必要的包或者导入了不需要的包，程序都无法编译通过。这项严格要求避免了程序开发过程中引入未使用的包（译注：Go语言编译过程没有警告信息，争议特性之一）。</p><p><code>import</code>声明必须跟在文件的<code>package</code>声明之后。随后，则是组成程序的函数、变量、常量、类型的声明语句（分别由关键字<code>func</code>, <code>var</code>, <code>const</code>, <code>type</code>定义）。这些内容的声明顺序并不重要（译注：最好还是定一下规范）。这个例子的程序已经尽可能短了，只声明了一个函数, 其中只调用了一个其他函数。为了节省篇幅，有些时候, 示例程序会省略<code>package</code>和<code>import</code>声明，但是，这些声明在源代码里有，并且必须得有才能编译。</p><p>一个函数的声明由<code>func</code>关键字、函数名、参数列表、返回值列表（这个例子里的<code>main</code>函数参数列表和返回值都是空的）以及包含在大括号里的函数体组成。第五章进一步考察函数。</p><p>Go语言不需要在语句或者声明的末尾添加分号，除非一行上有多条语句。实际上，编译器会主动把特定符号后的换行符转换为分号, 因此换行符添加的位置会影响Go代码的正确解析（译注：比如行末是标识符、整数、浮点数、虚数、字符或字符串文字、关键字<code>break</code>、<code>continue</code>、<code>fallthrough</code>或<code>return</code>中的一个、运算符和分隔符<code>++</code>、<code>--</code>、<code>)</code>、<code>]</code>或<code>&#125;</code>中的一个）。举个例子, 函数的左括号<code>&#123;</code>必须和<code>func</code>函数声明在同一行上, 且位于末尾，不能独占一行，而在表达式<code>x + y</code>中，可在<code>+</code>后换行，不能在<code>+</code>前换行（译注：以+结尾的话不会被插入分号分隔符，但是以x结尾的话则会被分号分隔符，从而导致编译错误）。</p><p>Go语言在代码格式上采取了很强硬的态度。<code>gofmt</code>工具把代码格式化为标准格式（译注：这个格式化工具没有任何可以调整代码格式的参数，Go语言就是这么任性），并且<code>go</code>工具中的<code>fmt</code>子命令会对指定包, 否则默认为当前目录, 中所有.go源文件应用<code>gofmt</code>命令。本书中的所有代码都被gofmt过。你也应该养成格式化自己的代码的习惯。以法令方式规定标准的代码格式可以避免无尽的无意义的琐碎争执（译注：也导致了Go语言的TIOBE排名较低，因为缺少撕逼的话题）。更重要的是，这样可以做多种自动源码转换，如果放任Go语言代码格式，这些转换就不大可能了。</p><p>很多文本编辑器都可以配置为保存文件时自动执行<code>gofmt</code>，这样你的源代码总会被恰当地格式化。还有个相关的工具，<code>goimports</code>，可以根据代码需要, 自动地添加或删除<code>import</code>声明。这个工具并没有包含在标准的分发包中，可以用下面的命令安装：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">$ go get golang.org/x/tools/cmd/goimports</span><br></pre></td></tr></table></figure><p>对于大多数用户来说，下载、编译包、运行测试用例、察看Go语言的文档等等常用功能都可以用go的工具完成。</p><p>由于我使用的是Goland，可以在Tool里配置gofmt和goimports。</p><h2 id="命令行参数">命令行参数</h2><p>通常情况下，输入来自于程序外部：文件、网络连接、其它程序的输出、敲键盘的用户、命令行参数或其它类似输入源。下面几个例子会讨论其中几个输入源，首先是命令行参数。</p><p><code>os</code>包以跨平台的方式，提供了一些与操作系统交互的函数和变量。程序的命令行参数可从os包的Args变量获取；os包外部使用os.Args访问该变量。</p><p>os.Args变量是一个字符串（string）的<em>切片</em>（slice）（译注：slice和Python语言中的切片类似，是一个简版的动态数组），切片是Go语言的基础概念，稍后详细介绍。现在先把切片s当作数组元素序列, 序列的长度动态变化, 用<code>s[i]</code>访问单个元素，用<code>s[m:n]</code>获取子序列(译注：和python里的语法差不多)。序列的元素数目为len(s)。和大多数编程语言类似，区间索引时，Go言里也采用左闭右开形式, 即，区间包括第一个索引元素，不包括最后一个, 因为这样可以简化逻辑。（译注：比如a = [1, 2, 3, 4, 5], a[0:3] = [1, 2, 3]，不包含最后一个元素）。比如s[m:n]这个切片，0 ≤ m ≤ n ≤ len(s)，包含n-m个元素。</p><p>os.Args的第一个元素，os.Args[0], 是命令本身的名字；其它的元素则是程序启动时传给它的参数。s[m:n]形式的切片表达式，产生从第m个元素到第n-1个元素的切片，下个例子用到的元素包含在os.Args[1:len(os.Args)]切片中。如果省略切片表达式的m或n，会默认传入0或len(s)，因此前面的切片可以简写成os.Args[1:]。</p><p>下面是Unix里echo命令的一份实现，echo把它的命令行参数打印成一行。程序导入了两个包，用括号把它们括起来写成列表形式, 而没有分开写成独立的<code>import</code>声明。两种形式都合法，列表形式习惯上用得多。包导入顺序并不重要；gofmt工具格式化时按照字母顺序对包名排序。</p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// echo1.go</span></span><br><span class="line"><span class="comment">// 一个模拟echo的程序</span></span><br><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"><span class="string">&quot;fmt&quot;</span></span><br><span class="line"><span class="string">&quot;os&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"><span class="keyword">var</span> s, sep <span class="keyword">string</span></span><br><span class="line"><span class="keyword">for</span> i := <span class="number">1</span>; i &lt; <span class="built_in">len</span>(os.Args); i++ &#123;</span><br><span class="line">s += sep + os.Args[i] <span class="comment">// 在第一次执行时，sep为 zero</span></span><br><span class="line">sep = <span class="string">&quot; &quot;</span></span><br><span class="line">&#125;</span><br><span class="line">fmt.Println(s)</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>可以得到下列结果：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ go run echo1.go love flora</span><br><span class="line">love flora</span><br></pre></td></tr></table></figure><p>var声明定义了两个string类型的变量s和sep。变量会在声明时直接初始化。如果变量没有显式初始化，则被隐式地赋予其类型的<em>零值</em>（zero value），数值类型是0，字符串类型是空字符串&quot;&quot;。这个例子里，声明把s和sep隐式地初始化成空字符串。第2章再来详细地讲解变量和声明。</p><p>对数值类型，Go语言提供了常规的数值和逻辑运算符。</p><p>第一次迭代之后，还会再插入一个空格，因此循环结束时每个参数中间都有一个空格。这是一种二次加工（quadratic process），当参数数量庞大时，开销很大，但是对于echo，这种情形不大可能出现。</p><p>Go语言只有for循环这一种循环语句。for循环有多种形式，其中一种如下所示：</p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">for</span> initialization; condition; post &#123;</span><br><span class="line">    <span class="comment">// zero or more statements</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>for循环三个部分不需括号包围。大括号强制要求, 左大括号必须和<em>post</em>语句在同一行。</p><p><em>initialization</em>语句是可选的，在循环开始前执行。<em>initalization</em>如果存在，必须是一条<em>简单语句</em>（simple statement），即，短变量声明、自增语句、赋值语句或函数调用。<code>condition</code>是一个布尔表达式（boolean expression），其值在每次循环迭代开始时计算。如果为<code>true</code>则执行循环体语句。<code>post</code>语句在循环体执行结束后执行，之后再次对<code>condition</code>求值。<code>condition</code>值为<code>false</code>时，循环结束。</p><p>for循环的这三个部分每个都可以省略，如果省略<code>initialization</code>和<code>post</code>，分号也可以省略：</p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// a traditional &quot;while&quot; loop</span></span><br><span class="line"><span class="keyword">for</span> condition &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>如果连<code>condition</code>也省略了，像下面这样：</p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// a traditional infinite loop</span></span><br><span class="line"><span class="keyword">for</span> &#123;</span><br><span class="line">    <span class="comment">// ...</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>这就变成一个无限循环，尽管如此，还可以用其他方式终止循环, 如一条<code>break</code>或<code>return</code>语句。</p><p><code>for</code>循环的另一种形式, 在某种数据类型的区间（range）上遍历，如字符串或切片，如下：</p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// echo2.go</span></span><br><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"><span class="string">&quot;fmt&quot;</span></span><br><span class="line"><span class="string">&quot;os&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">s, sep := <span class="string">&quot;&quot;</span>, <span class="string">&quot;&quot;</span></span><br><span class="line"><span class="keyword">for</span> _, arg := <span class="keyword">range</span> os.Args[<span class="number">1</span>:] &#123;<span class="comment">// 原本_所得到的为对应的idx</span></span><br><span class="line">s += sep + arg</span><br><span class="line">sep = <span class="string">&quot; &quot;</span></span><br><span class="line">&#125;</span><br><span class="line">fmt.Println(s)</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p>每次循环迭代，<code>range</code>产生一对值；索引以及在该索引处的元素值。这个例子不需要索引，但<code>range</code>的语法要求, 要处理元素, 必须处理索引。一种思路是把索引赋值给一个临时变量, 如<code>temp</code>, 然后忽略它的值，但Go语言不允许使用无用的局部变量（local variables），因为这会导致编译错误。</p><p>Go语言中这种情况的解决方法是用<code>空标识符</code>（blank identifier），即<code>_</code>（也就是下划线）。空标识符可用于<strong>任何语法需要变量名但程序逻辑不需要</strong>的时候, 例如, 在循环里，丢弃不需要的循环索引, 保留元素值。大多数的Go程序员都会像上面这样使用<code>range</code>和<code>_</code>写<code>echo</code>程序，因为隐式地而非显式地索引os.Args，容易写对。</p><p><code>echo</code>的这个版本使用一条短变量声明来声明并初始化<code>s</code>和<code>seps</code>，也可以将这两个变量分开声明，声明一个变量有好几种方式，下面这些都等价：</p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">s := <span class="string">&quot;&quot;</span></span><br><span class="line"><span class="keyword">var</span> s <span class="keyword">string</span></span><br><span class="line"><span class="keyword">var</span> s = <span class="string">&quot;&quot;</span></span><br><span class="line"><span class="keyword">var</span> s <span class="keyword">string</span> = <span class="string">&quot;&quot;</span></span><br></pre></td></tr></table></figure><p>用哪种不用哪种，为什么呢？第一种形式，是一条短变量声明，最<strong>简洁</strong>，但只能用在函数内部，而不能用于包变量。第二种形式依赖于字符串的默认初始化零值机制，被初始化为&quot;&quot;。第三种形式用得很少，除非同时声明多个变量。第四种形式显式地标明变量的类型，当变量类型与初值类型相同时，类型冗余，但如果两者类型不同，变量类型就必须了。实践中一般使用前两种形式中的某个，<strong>初始值重要的话就显式地指定变量的类型</strong>，否则使用隐式初始化。</p><p>如果连接涉及的数据量很大，这种方式代价高昂。一种简单且高效的解决方案是使用<code>strings</code>包的<code>Join</code>函数：</p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="comment">// echo3.go</span></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    fmt.Println(strings.Join(os.Args[<span class="number">1</span>:], <span class="string">&quot; &quot;</span>))</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>练习 1.1：</strong> 修改<code>echo</code>程序，使其能够打印<code>os.Args[0]</code>，即被执行命令本身的名字。</p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"><span class="string">&quot;fmt&quot;</span></span><br><span class="line"><span class="string">&quot;os&quot;</span></span><br><span class="line"><span class="string">&quot;strings&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">fmt.Println(strings.Join(os.Args[<span class="number">0</span>:], <span class="string">&quot; &quot;</span>))</span><br><span class="line"></span><br><span class="line"><span class="comment">/* s, sep := &quot;&quot;, &quot;&quot;</span></span><br><span class="line"><span class="comment">for i := 0; i &lt; len(os.Args); i++ &#123;</span></span><br><span class="line"><span class="comment">s += sep + os.Args[i]</span></span><br><span class="line"><span class="comment">sep = &quot; &quot;</span></span><br><span class="line"><span class="comment">&#125;</span></span><br><span class="line"><span class="comment">fmt.Println(s) */</span></span><br><span class="line"></span><br><span class="line"><span class="comment">/* s, sep := &quot;&quot;, &quot;&quot;</span></span><br><span class="line"><span class="comment">for _, arg := range os.Args[0:] &#123;</span></span><br><span class="line"><span class="comment">s += sep + arg</span></span><br><span class="line"><span class="comment">sep = &quot; &quot;</span></span><br><span class="line"><span class="comment">&#125;</span></span><br><span class="line"><span class="comment">fmt.Println(s) */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><p><strong>练习 1.2：</strong> 修改<code>echo</code>程序，使其打印每个参数的索引和值，每个一行。</p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"><span class="string">&quot;fmt&quot;</span></span><br><span class="line"><span class="string">&quot;os&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line"><span class="keyword">for</span> idx, arg := <span class="keyword">range</span> os.Args[<span class="number">0</span>:] &#123;</span><br><span class="line">fmt.Println(idx, arg)</span><br><span class="line">&#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p><strong>练习 1.3：</strong> 做实验测量潜在低效的版本和使用了<code>strings.Join</code>的版本的运行时间差异。（1.6节讲解了部分<code>time</code>包，11.4节展示了如何写标准测试程序，以得到系统性的性能评测。）</p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line"><span class="string">&quot;fmt&quot;</span></span><br><span class="line"><span class="string">&quot;os&quot;</span></span><br><span class="line"><span class="string">&quot;strings&quot;</span></span><br><span class="line"><span class="string">&quot;time&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">echo1_3_1</span><span class="params">()</span></span> &#123;</span><br><span class="line">start := time.Now()</span><br><span class="line"><span class="keyword">var</span> s, sep <span class="keyword">string</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> i := <span class="number">0</span>; i &lt; <span class="built_in">len</span>(os.Args); i++ &#123;</span><br><span class="line">s += sep + os.Args[i]</span><br><span class="line">sep = <span class="string">&quot; &quot;</span></span><br><span class="line">&#125;</span><br><span class="line">fmt.Println(s)</span><br><span class="line">fmt.Printf(<span class="string">&quot;%.6fs \n&quot;</span>, time.Since(start).Seconds())</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">echo1_3_2</span><span class="params">()</span></span> &#123;</span><br><span class="line">start := time.Now()</span><br><span class="line"><span class="keyword">var</span> s, sep <span class="keyword">string</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">for</span> _, arg := <span class="keyword">range</span> os.Args &#123;</span><br><span class="line">s += sep + arg</span><br><span class="line">sep = <span class="string">&quot; &quot;</span></span><br><span class="line">&#125;</span><br><span class="line">fmt.Println(s)</span><br><span class="line">fmt.Printf(<span class="string">&quot;%.6fs \n&quot;</span>, time.Since(start).Seconds())</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">echo1_3_3</span><span class="params">()</span></span> &#123;</span><br><span class="line">start := time.Now()</span><br><span class="line">fmt.Println(strings.Join(os.Args[<span class="number">0</span>:], <span class="string">&quot; &quot;</span>))</span><br><span class="line">fmt.Printf(<span class="string">&quot;%.6fs \n&quot;</span>, time.Since(start).Seconds())</span><br><span class="line"></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="查找重复的行">查找重复的行</h2><p>对文件做拷贝、打印、搜索、排序、统计或类似事情的程序都有一个差不多的程序结构：一个处理输入的循环，在每个元素上执行计算处理，在处理的同时或最后产生输出。我们会展示一个名为<code>dup</code>的程序的三个版本；灵感来自于Unix的<code>uniq</code>命令，其寻找相邻的重复行。该程序使用的结构和包是个参考范例，可以方便地修改。</p><p><code>dup</code>的第一个版本打印标准输入中多次出现的行，以重复次数开头。该程序将引入<code>if</code>语句，<code>map</code>数据类型以及<code>bufio</code>包。</p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line"><span class="keyword">package</span> main</span><br><span class="line"></span><br><span class="line"><span class="keyword">import</span> (</span><br><span class="line">    <span class="string">&quot;bufio&quot;</span></span><br><span class="line">    <span class="string">&quot;fmt&quot;</span></span><br><span class="line">    <span class="string">&quot;os&quot;</span></span><br><span class="line">)</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">func</span> <span class="title">main</span><span class="params">()</span></span> &#123;</span><br><span class="line">    counts := <span class="built_in">make</span>(<span class="keyword">map</span>[<span class="keyword">string</span>]<span class="keyword">int</span>)</span><br><span class="line">    input := bufio.NewScanner(os.Stdin)</span><br><span class="line">    <span class="keyword">for</span> input.Scan() &#123;</span><br><span class="line">        counts[input.Text()]++</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// <span class="doctag">NOTE:</span> ignoring potential errors from input.Err()</span></span><br><span class="line">    <span class="keyword">for</span> line, n := <span class="keyword">range</span> counts &#123;</span><br><span class="line">        <span class="keyword">if</span> n &gt; <span class="number">1</span> &#123;</span><br><span class="line">            fmt.Printf(<span class="string">&quot;%d\t%s\n&quot;</span>, n, line)</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>正如<code>for</code>循环一样，<code>if</code>语句条件两边也不加括号，但是主体部分需要加。<code>if</code>语句的<code>else</code>部分是可选的，在<code>if</code>的条件为<code>false</code>时执行。</p><p><strong>map</strong>存储了键/值（key/value）的集合，对集合元素，提供常数时间的存、取或测试操作。键可以是任意类型，只要其值能用<code>==</code>运算符比较，最常见的例子是字符串；值则可以是任意类型。这个例子中的键是字符串，值是整数。内置函数<code>make</code>创建空<code>map</code>，此外，它还有别的作用。4.3节讨论<code>map</code>。</p><p>（译注：从功能和实现上说，<code>Go</code>的<code>map</code>类似于<code>Java</code>语言中的<code>HashMap</code>，Python语言中的<code>dict</code>，<code>Lua</code>语言中的<code>table</code>，通常使用<code>hash</code>实现。遗憾的是，对于该词的翻译并不统一，数学界术语为<code>映射</code>，而计算机界众说纷纭莫衷一是。为了防止对读者造成误解，保留不译。）</p><p>每次<code>dup</code>读取一行输入，该行被当做<code>map</code>，其对应的值递增。<code>counts[input.Text()]++</code>语句等价下面两句：</p><figure class="highlight go"><table><tr><td class="code"><pre><span class="line">line := input.Text()</span><br><span class="line">counts[line] = counts[line] + <span class="number">1</span></span><br></pre></td></tr></table></figure><p><code>map</code>中不含某个键时不用担心，首次读到新行时，等号右边的表达式<code>counts[line]</code>的值将被计算为其类型的零值，对于int`即0。</p><p>为了打印结果，我们使用了基于<code>range</code>的循环，并在<code>counts</code>这个<code>map</code>上迭代。跟之前类似，每次迭代得到两个结果，键和其在<code>map</code>中对应的值。<code>map</code>的迭代顺序并不确定，从实践来看，该顺序随机，每次运行都会变化。这种设计是有意为之的，因为能防止程序依赖特定遍历顺序，而这是无法保证的。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;入门&lt;/h1&gt;
&lt;p&gt;先扯一下go mod的问题。&lt;/p&gt;
&lt;p&gt;在1.13版本后引入了管理用的modules机制。&lt;/p&gt;
&lt;p&gt;执行：&lt;/p&gt;
&lt;figure class=&quot;highlight plaintext&quot;&gt;&lt;table&gt;&lt;tr&gt;&lt;td class=&quot;code</summary>
      
    
    
    
    <category term="study" scheme="https://www.jk404.cn/categories/study/"/>
    
    <category term="book" scheme="https://www.jk404.cn/categories/study/book/"/>
    
    <category term="The Go Programming Language" scheme="https://www.jk404.cn/categories/study/book/The-Go-Programming-Language/"/>
    
    
  </entry>
  
  <entry>
    <title>Gopl_01_preface</title>
    <link href="https://www.jk404.cn/study/book/The-Go-Programming-Language/Gopl-01-preface/"/>
    <id>https://www.jk404.cn/study/book/The-Go-Programming-Language/Gopl-01-preface/</id>
    <published>2022-10-24T17:31:40.000Z</published>
    <updated>2022-10-24T18:28:39.810Z</updated>
    
    <content type="html"><![CDATA[<h1>前言</h1><h2 id="起源">起源</h2><p>由下图可以粗略看出，许多早期的编程语言对Go语言的设计产生了重要影响。</p><img src="/study/book/The-Go-Programming-Language/Gopl-01-preface/figure_01.jpg" width="350" height="400/"><ul><li><p><strong>C语言</strong></p><p>Go语言有时候被描述为“类C语言”，其从C语言继承了相似的表达式语法、控制流结构、基础数据类型、调用参数传值、指针等很多思想，还有C语言一直所在意的编译后机器码的运行效率以及和现有操作系统的无缝适配。</p></li><li><p><strong>Pascal -&gt; Modula-2 -&gt; Oberon -&gt; Oberon-2</strong></p><p>其中一个有影响力的分支来自Niklaus Wirth所设计的Pascal语言。Modula-2语言激发了包的概念。然后Oberon语言摒弃了模块接口文件和模块实现文件之间的区别。第二代的Oberon-2语言直接影响了包的导入和声明的语法，还有Oberon语言的面向对象特性所提供的方法的声明语法等。</p></li><li><p><strong>CSP -&gt; Squeak -&gt; Newsqueak -&gt; Alef</strong></p><p>[CSP] - 来自于贝尔实验室的<em>Tony Hoare</em>于1978年发表的鲜为外界所知的关于并发研究的基础文献 <em>顺序通信进程</em>（ <em>communicating sequential processes</em>），缩写为<em>CSP</em>。在<em>CSP</em>中，程序是一组中间没有共享状态的平行运行的处理过程，它们之间使用管道进行通信和控制同步。不过<em>Tony Hoare</em>的<em>CSP</em>只是一个用于描述并发性基本概念的描述语言，并不是一个可以编写可执行程序的通用编程语言。</p><p>[Squeak] - <em>Rob Pike</em>和其他人开始不断尝试将CSP引入实际的编程语言中。他们第一次尝试引入CSP特性的编程语言叫Squeak（老鼠间交流的语言），是一个提供鼠标和键盘事件处理的编程语言，它的管道是静态创建的。</p><p>[Newsqueak] - 改进版的Newsqueak语言提供了类似C语言语句和表达式的语法和类似Pascal语言的推导语法。Newsqueak是一个带垃圾回收的纯函数式语言，它再次针对键盘、鼠标和窗口事件管理。但是在Newsqueak语言中管道是动态创建的，属于第一类值, 可以保存到变量中。</p><p>[Alef] - 在Plan9操作系统中，这些优秀的想法被吸收到了一个叫Alef的编程语言中。Alef试图将Newsqueak语言改造为系统编程语言，但是因为缺少垃圾回收机制而导致并发编程很痛苦。在Aelf之后还有一个叫Limbo的编程语言，Go语言从其中借鉴了很多特性。</p></li><li><p>其他特性</p><p>Go语言的其他的一些特性零散地来自于其他一些编程语言；</p><p>比如iota语法是从APL语言借鉴，词法作用域与嵌套函数来自于Scheme语言（和其他很多语言）。当然，我们也可以从Go中发现很多创新的设计。比如Go语言的切片为动态数组提供了有效的随机存取的性能，这可能会让人联想到链表的底层的共享机制。还有Go语言新发明的defer语句。</p></li></ul><h2 id="语言设计项目">语言设计项目</h2><p>所有的编程语言都反映了语言设计者对编程哲学的反思，通常包括之前的语言所暴露的一些不足地方的改进。Go项目是在Google公司维护超级复杂的几个软件系统遇到的一些问题的反思（但是这类问题绝不是Google公司所特有的）。</p><p>正如Rob Pike所说，“软件的复杂性是乘法级相关的”，通过增加一个部分的复杂性来修复问题通常将慢慢地增加其他部分的复杂性。通过增加功能、选项和配置是修复问题的最快的途径，但是这很容易让人忘记简洁的内涵，即从长远来看，简洁依然是好软件的关键因素。只有通过简洁的设计，才能让一个系统保持稳定、安全和持续的进化。</p><p>Go项目包括编程语言本身，附带了相关的工具和标准库，最后但并非代表不重要的是，关于简洁编程哲学的宣言。就事后诸葛的角度来看，Go语言的这些地方都做的还不错：拥有自动垃圾回收、一个包系统、函数作为一等公民、词法作用域、系统调用接口、只读的UTF8字符串等。但是Go语言本身只有很少的特性，也不太可能添加太多的特性。例如，它没有隐式的数值转换，没有构造函数和析构函数，没有运算符重载，没有默认参数，也没有继承，没有泛型，没有异常，没有宏，没有函数修饰，更没有线程局部存储。但是，语言本身是成熟和稳定的，而且承诺保证向后兼容：用之前的Go语言编写程序可以用新版本的Go语言编译器和标准库直接构建而不需要修改代码。</p><p>Go语言有足够的类型系统以避免动态语言中那些粗心的类型错误，但是，Go语言的类型系统相比传统的强类型语言又要简洁很多。虽然，有时候这会导致一个“无类型”的抽象类型概念，但是Go语言程序员并不需要像C++或Haskell程序员那样纠结于具体类型的安全属性。在实践中，Go语言简洁的类型系统给程序员带来了更多的安全性和更好的运行时性能</p><p>Go语言鼓励当代计算机系统设计的原则，特别是局部的重要性。它的内置数据类型和大多数的准库数据结构都经过精心设计而<strong>避免显式的初始化或隐式的构造函数</strong>，因为很少的内存分配和内存初始化代码被隐藏在库代码中了。Go语言的<strong>聚合类型（结构体和数组）可以直接操作它们的元素</strong>，只需要更少的存储空间、更少的内存写操作，而且指针操作比其他间接操作的语言也更有效率。<strong>由于现代计算机是一个并行的机器，Go语言提供了基于CSP的并发特性支持</strong>。Go语言的<strong>动态栈使得轻量级线程goroutine的初始栈可以很小</strong>，因此，创建一个goroutine的代价很小，创建百万级的goroutine完全是可行的。</p><p>Go语言的标准库（通常被称为语言自带的电池），提供了清晰的构建模块和公共接口，包含I/O操作、文本处理、图像、密码学、网络和分布式应用程序等，并支持许多标准化的文件格式和编解码协议。<strong>库和工具使用了大量的约定来减少额外的配置和解释，从而最终简化程序的逻辑</strong>，而且，每个Go程序结构都是如此的相似，因此，Go程序也很容易学习。使用Go语言自带工具构建Go语言项目只需要使用文件名和标识符名称, 一个偶尔的特殊注释来确定所有的库、可执行文件、测试、基准测试、例子、以及特定于平台的变量、项目的文档等；Go语言源代码本身就包含了<strong>构建规范</strong>。</p><h2 id="学习脉络">学习脉络</h2><p>第一章包含了本教程的基本结构，通过十几个程序介绍了用Go语言如何实现类似读写文件、文本格式化、创建图像、网络客户端和服务器通讯等日常工作。</p><p>第二章描述了Go语言程序的基本元素结构、变量、新类型定义、包和文件、以及作用域等概念。第三章讨论了数字、布尔值、字符串和常量，并演示了如何显示和处理Unicode字符。第四章描述了复合类型，从简单的数组、字典、切片到动态列表。第五章涵盖了函数，并讨论了错误处理、panic和recover，还有defer语句。</p><p>第一章到第五章是基础部分，主流命令式编程语言这部分都类似。个别之处，Go语言有自己特色的语法和风格，但是大多数程序员能很快适应。其余章节是Go语言特有的：方法、接口、并发、包、测试和反射等语言特性。</p><p>Go语言的面向对象机制与一般语言不同。它没有类层次结构，甚至可以说没有类；仅仅通过组合（而不是继承）简单的对象来构建复杂的对象。方法不仅可以定义在结构体上, 而且, 可以定义在任何用户自定义的类型上；并且, 具体类型和抽象类型（接口）之间的关系是隐式的，所以很多类型的设计者可能并不知道该类型到底实现了哪些接口。方法在第六章讨论，接口在第七章讨论。</p><p>第八章讨论了基于顺序通信进程(CSP)概念的并发编程，使用goroutines和channels处理并发编程。</p><p>第九章则讨论了传统的基于共享变量的并发编程。</p><p>第十章描述了包机制和包的组织结构。这一章还展示了如何有效地利用Go自带的工具，使用单个命令完成编译、测试、基准测试、代码格式化、文档以及其他诸多任务。</p><p>第十一章讨论了单元测试，Go语言的工具和标准库中集成了轻量级的测试功能，避免了强大但复杂的测试框架。测试库提供了一些基本构件，必要时可以用来构建复杂的测试构件。</p><p>第十二章讨论了反射，一种程序在运行期间审视自己的能力。反射是一个强大的编程工具，不过要谨慎地使用；这一章利用反射机制实现一些重要的Go语言库函数, 展示了反射的强大用法。</p><p>第十三章解释了底层编程的细节，在必要时，可以使用unsafe包绕过Go语言安全的类型系统。</p><h2 id="环境-v5">环境</h2><p>书中所有的代码都可以从 <a href="http://gopl.io/">http://gopl.io</a> 上的Git仓库下载。go get命令根据每个例子的导入路径智能地获取、构建并安装。只需要选择一个目录作为工作空间，然后将GOPATH环境变量设置为该路径。</p><p>必要时，Go语言工具会创建目录。例如：</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ <span class="built_in">export</span> GOPATH=<span class="variable">$HOME</span>/gobook    <span class="comment"># 选择工作目录</span></span><br><span class="line">$ go get gopl.io/ch1/helloworld <span class="comment"># 获取/编译/安装</span></span><br><span class="line">$ <span class="variable">$GOPATH</span>/bin/helloworld        <span class="comment"># 运行程序</span></span><br><span class="line">Hello, 世界                     <span class="comment"># 这是中文</span></span><br></pre></td></tr></table></figure><p>运行这些例子需要安装Go1.5以上的版本。</p><figure class="highlight bash"><table><tr><td class="code"><pre><span class="line">$ go version</span><br><span class="line">go version go1.5 linux/amd64</span><br></pre></td></tr></table></figure><p>如果使用其他的操作系统, 请参考 <a href="https://golang.org/doc/install">https://golang.org/doc/install</a> 提供的说明安装</p><h2 id="辅助文档">辅助文档</h2><p>最佳的帮助信息来自Go语言的官方网站，<a href="https://golang.org/">https://golang.org</a> ，它提供了完善的参考文档，包括编程语言规范和标准库等诸多权威的帮助信息。同时也包含了如何编写更地道的Go程序的基本教程，还有各种各样的在线文本资源和视频资源，它们是本书最有价值的补充。Go语言的官方博客 <a href="https://blog.golang.org/">https://blog.golang.org</a> 会不定期发布一些Go语言最好的实践文章，包括当前语言的发展状态、未来的计划、会议报告和Go语言相关的各种会议的主题等信息（译注： <a href="http://talks.golang.org/">http://talks.golang.org/</a> 包含了官方收录的各种报告的讲稿）。</p><p>在线访问的一个有价值的地方是可以从web页面运行Go语言的程序（而纸质书则没有这么便利了）。这个功能由来自 <a href="https://play.golang.org/">https://play.golang.org</a> 的 Go Playground 提供，并且可以方便地嵌入到其他页面中，例如 <a href="https://golang.org/">https://golang.org</a> 的主页，或 godoc 提供的文档页面中。</p><p>Playground可以简单的通过执行一个小程序来测试对语法、语义和对程序库的理解，类似其他很多语言提供的REPL即时运行的工具。同时它可以生成对应的url，非常适合共享Go语言代码片段，汇报bug或提供反馈意见等。</p><p>基于 Playground 构建的 Go Tour，<a href="https://tour.golang.org/">https://tour.golang.org</a> ，是一个系列的Go语言入门教程，它包含了诸多基本概念和结构相关的并可在线运行的互动小程序。</p><p>当然，Playground 和 Tour 也有一些限制，它们只能导入标准库，而且因为安全的原因对一些网络库做了限制。如果要在编译和运行时需要访问互联网，对于一些更复杂的实验，你可能需要在自己的电脑上构建并运行程序。幸运的是下载Go语言的过程很简单，从 <a href="https://golang.org/">https://golang.org</a> 下载安装包应该不超过几分钟（译注：感谢伟大的长城，让大陆的Gopher们都学会了自己打洞的基本生活技能，下载时间可能会因为洞的大小等因素从几分钟到几天或更久），然后就可以在自己电脑上编写和运行Go程序了。</p><p>Go语言是一个开源项目，你可以在 <a href="https://golang.org/pkg">https://golang.org/pkg</a> 阅读标准库中任意函数和类型的实现代码，和下载安装包的代码完全一致。这样，你可以知道很多函数是如何工作的， 通过挖掘找出一些答案的细节，或者仅仅是出于欣赏专业级Go代码。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;前言&lt;/h1&gt;
&lt;h2 id=&quot;起源&quot;&gt;起源&lt;/h2&gt;
&lt;p&gt;由下图可以粗略看出，许多早期的编程语言对Go语言的设计产生了重要影响。&lt;/p&gt;
&lt;img src=&quot;/study/book/The-Go-Programming-Language/Gopl-01-preface</summary>
      
    
    
    
    <category term="study" scheme="https://www.jk404.cn/categories/study/"/>
    
    <category term="book" scheme="https://www.jk404.cn/categories/study/book/"/>
    
    <category term="The Go Programming Language" scheme="https://www.jk404.cn/categories/study/book/The-Go-Programming-Language/"/>
    
    
  </entry>
  
  <entry>
    <title>data_structure_02_linear_list</title>
    <link href="https://www.jk404.cn/study/course/data-structure/data-structure-02-linear-list/"/>
    <id>https://www.jk404.cn/study/course/data-structure/data-structure-02-linear-list/</id>
    <published>2022-10-24T03:48:44.000Z</published>
    <updated>2022-11-08T12:31:08.833Z</updated>
    
    <content type="html"><![CDATA[<h1>线性表</h1><p>重点内容：</p><ul><li>线性表的定义和基本操作</li><li>线性表的实现：顺序存储、链式存储、线性表的应用</li></ul><p>知识框架：</p><p><img src="/study/course/data-structure/data-structure-02-linear-list/figure01.jpg" alt></p><h2 id="线性表的定义和基本操作">线性表的定义和基本操作</h2><h3 id="线性表的定义">线性表的定义</h3><p>线性表是具有相同数据类型的 $n \space (n \geq 0)$ 个数据元素的有限序列，其中 $n$ 为表长，为0是代表线性表为空表。若用 $L$ 命名线性表，则一般表示为：<br>$$<br>L=(a_1,a_2,…,a_i,a_{i+1},…,a_n)<br>$$<br>线性表逻辑特性为：</p><ul><li>$a_1$ 为<strong>唯一</strong>的”第一个“数据元素，又称为<strong>表头元素</strong> ；</li><li>$a_n$ 是<strong>唯一</strong>的”最后一个“数据元素，又称为<strong>表尾元素</strong>；</li><li><strong>除第一个元素外</strong>，每个元素<strong>有且仅有</strong>一个直接<strong>前驱</strong>；</li><li><strong>除最后一个元素外</strong>，每个元素<strong>有且仅有</strong>一个直接<strong>后继</strong>；</li></ul><p>注意：</p><ul><li><strong>线性表</strong>是一种<strong>逻辑结构</strong>，表示元素之间一对一的相邻关系；</li><li><strong>顺序表和链表</strong>强调的是<strong>存储结构</strong>，和线性表属于不同层面的概念；</li></ul><h3 id="线性表的基本操作">线性表的基本操作</h3><p>线性表的主要操作有：</p><ul><li><code>InitList(&amp;L)</code> - 初始化表，构造一个空的线性表</li><li><code>Length(L)</code> - 求表长，返回线性表<code>L</code>的长度，即其中元素的个数</li><li><code>LocateElem(L, e)</code> - 按值查找操作，在表L中查找具有给定值的元素</li><li><code>GetElem(L, i)</code> - 按位查找操作，获取表L中第i个位置的元素的值</li><li><code>ListInsert(&amp;L, i, e)</code> - 插入操作，在表L中第i个位置插入指定元素e</li><li><code>ListDelete(&amp;L, i, &amp;e)</code> - 删除操作，删除表L中第i个位置的元素，并用e返回删除元素的值</li><li><code>PrintList(L)</code> - 打印线性表，按前后顺序输出线性表L的所有元素值</li><li><code>Empty(L)</code> - 判空操作，若L为空表，则返回true，否则返回false</li><li><code>DestroyList(&amp;L)</code> - 销毁操作，销毁线性表，释放L所占用的内存空间</li></ul><p>注意：</p><ul><li>基本操作的实现取决于存储结构</li><li><code>&amp;</code>表示C++中表示引用调用，在C语言中采用指针可达到同样的效果</li></ul><h2 id="线性表的顺序表示">线性表的顺序表示</h2><h3 id="顺序表的定义">顺序表的定义</h3><p>线性表的顺序存储称为顺序表。</p><p>是用<strong>一组地址连续的存储单元</strong>依次存储线性表中的数据元素，从而使得<strong>逻辑上相邻</strong>的两个元素在<strong>物理位置上也相邻</strong>。即<strong>逻辑顺序与物理顺序相同</strong>。</p><p><strong>线性表的顺序存储结构是一种<em>随机存储</em>的存储结构</strong>。典型的就是高级程序设计语言中的<strong>数组</strong>。但是要注意的是，线性表元素的位序从1开始，而数组的元素下标从0开始。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// 静态分配</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MaxSize 50</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">SeqList</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="comment">/* data */</span></span><br><span class="line">    ElemType data[MaxSize];<span class="comment">// 顺序表数组</span></span><br><span class="line">    <span class="keyword">int</span> length;</span><br><span class="line">&#125;;</span><br><span class="line"></span><br><span class="line"><span class="comment">// 动态分配</span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> InitSize 100</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">SeqList</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="comment">/* data */</span></span><br><span class="line">    ElemType *data;<span class="comment">// 动态分配数组的指针</span></span><br><span class="line">    <span class="keyword">int</span> length, Maxsize;</span><br><span class="line">&#125;;</span><br></pre></td></tr></table></figure><p>顺序表的特点有：</p><ul><li>随机访问，通过首地址和元素序号可以在 $O(1)$ 的时间内找到指定元素；</li><li>存储密度高，每个结点只存储数据元素；</li><li>逻辑相邻的元素物理相邻，插入和删除操作需要移动大量元素；</li></ul><h3 id="顺序表上的操作">顺序表上的操作</h3><ul><li>插入</li><li>删除</li><li>按值查找</li></ul><p>以下列顺序表结构为例：</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> InitSize 100</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    ElemType *data;         <span class="comment">// 指针</span></span><br><span class="line">    <span class="keyword">int</span> length;             <span class="comment">// 表长</span></span><br><span class="line">    <span class="keyword">int</span> MaxSize = InitSize; <span class="comment">//表长的最大值</span></span><br><span class="line">&#125; SeqList;</span><br></pre></td></tr></table></figure><h4 id="插入">插入</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 按位插入指定数值 */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">ListInsert</span><span class="params">(SeqList &amp;SeqL, <span class="keyword">int</span> order, ElemType element)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (order &lt; <span class="number">1</span> || order &gt; SeqL.length + <span class="number">1</span>) <span class="comment">// 注意 order 和 index 的区别</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">if</span> (SeqL.length &gt;= SeqL.MaxSize) <span class="comment">// 如果表满了，则直接返回 false</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    将表元素向右挪动，如果从前往后遍历会覆盖掉值，如果声明temp变量来临时存储又会加大开销</span></span><br><span class="line"><span class="comment">    所以考虑从后往前遍历</span></span><br><span class="line"><span class="comment">    for (int j = order; j &lt;= SeqL.length; j++)</span></span><br><span class="line"><span class="comment">    &#123;</span></span><br><span class="line"><span class="comment">        SeqL.data[j] = SeqL.data[j - 1];</span></span><br><span class="line"><span class="comment">    &#125;</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> j = SeqL.length; j &gt;= order; j--)</span><br><span class="line">    &#123;</span><br><span class="line">        SeqL.data[j] = SeqL.data[j - <span class="number">1</span>];</span><br><span class="line">    &#125;</span><br><span class="line">    SeqL.data[order - <span class="number">1</span>] = element;</span><br><span class="line">    SeqL.length++;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对于插入操作而言：</p><ul><li><p>最好情况：在表尾（位序为<code>L.length+1</code>，下标为<code>L.length</code>）插入，不移动元素</p></li><li><p>最坏情况：在表头（位序为1，下标为0），需要移动<code>L.length</code>次</p></li><li><p>平均情况：假设 $p_i \space (p_i=\frac{1}{n+1})$ 是在第 $i$ （这里的 $i$ 是位序）个位置上插入一个结点的概率，则在长度为 $n$ 的线性表中插入一个结点时，$n-i+1$ 为需要挪动的次数，则所需要移动结点的平均次数为：<br>$$<br>\begin{equation}<br>\begin{split}<br>N<br>&amp;=\sum_{i=1}^{n+1}p_i(n-i+1)\<br>&amp;=\sum_{i=1}^{n+1}\frac{1}{n+1}(n-i+1)\<br>&amp;=\frac{1}{n+1}\sum_{i=1}^{n+1}（n-i+1)\<br>&amp;=\frac{1}{n+1} \times \frac{n(n+1)}{2}\<br>&amp;=\frac{n}{2}<br>\end{split}<br>\end{equation}<br>$$<br>即插入算法的平均时间复杂度为 $O(n)$ 。</p></li></ul><h4 id="删除">删除</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 删除指定位序的元素，并用变量 del_e 返回 */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">ListDelete</span><span class="params">(SeqList &amp;SeqL, <span class="keyword">int</span> order, ElemType &amp;del_e)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (order &lt; <span class="number">1</span> || order &gt; SeqL.length)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">if</span> (SeqL.length &lt;= <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    del_e = SeqL.data[order - <span class="number">1</span>];</span><br><span class="line">    <span class="comment">/* 将表元素向左挪动，从前向后遍历 */</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> j = order; j &lt; SeqL.length; j++)</span><br><span class="line">    &#123;</span><br><span class="line">        SeqL.data[order - <span class="number">1</span>] = SeqL.data[order];</span><br><span class="line">    &#125;</span><br><span class="line">    SeqL.length--;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对于删除操作而言：</p><ul><li><p>最好情况：删除表尾（位序为<code>L.length</code>，下标为<code>L.length-1</code>）元素，不移动元素</p></li><li><p>最坏情况：删除表头（位序为1，下标为0），需要移动<code>L.length-1</code>次</p></li><li><p>平均情况：假设 $p_i \space (p_i=\frac{1}{n})$ 是删除第 $i$ （这里的 $i$ 是位序）个位置的结点的概率，则在长度为 $n$ 的线性表中删除一个结点时，$n-i$ 为需要挪动的次数，则所需要移动结点的平均次数为：<br>$$<br>\begin{equation}<br>\begin{split}<br>N<br>&amp;=\sum_{i=1}^{n}p_i(n-i)\<br>&amp;=\sum_{i=1}^{n}\frac{1}{n}(n-i)\<br>&amp;=\frac{1}{n}\sum_{i=1}^{n}（n-i)\<br>&amp;=\frac{1}{n} \times \frac{n(n-1)}{2}\<br>&amp;=\frac{n-1}{2}<br>\end{split}<br>\end{equation}<br>$$<br>即删除算法的平均时间复杂度为 $O(n)$ 。</p></li></ul><h4 id="按值查找">按值查找</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 按值查找位序 */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">LocateByElem</span><span class="params">(SeqList SeqL, ElemType element)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> idx = <span class="number">0</span>; idx &lt; SeqL.length; idx++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (element = SeqL.data[idx])</span><br><span class="line">            <span class="keyword">return</span> idx + <span class="number">1</span>; <span class="comment">// 返回的位序为 [下标 + 1]</span></span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>; <span class="comment">// 由于位序不可能为 0 ，返回 0 则表示查找失败</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>对于按值查找操作而言：</p><ul><li><p>最好情况：查找的元素在表头，只需要执行条件语句一次，时间复杂度为 $O(1)$</p></li><li><p>最坏情况：查找的元素在表尾或不存在，需要执行条件语句<code>L.length</code>次，即 $n$ 次，时间复杂度为 $O(n)$</p></li><li><p>平均情况：假设 $p_i \space (p_i=\frac{1}{n})$ 是查找的元素在第 $i$ （这里的 $i$ 是位序）个位置的结点的概率，则在长度为 $n$ 的线性表中查找值为<code>element</code>的结点时，所需要执行条件语句的平均次数为：<br>$$<br>\begin{equation}<br>\begin{split}<br>N<br>&amp;=\sum_{i=1}^{n}p_i \times i\<br>&amp;=\sum_{i=1}^{n}\frac{1}{n} \times i\<br>&amp;=\frac{1}{n}\sum_{i=1}^{n}i\<br>&amp;=\frac{1}{n} \times \frac{n(n+1)}{2}\<br>&amp;=\frac{n+1}{2}<br>\end{split}<br>\end{equation}<br>$$<br>即线性表按值查找算法的平均时间复杂度为 $O(n)$ 。</p></li></ul><h2 id="课后题-v3">课后题</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;malloc.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">define</span> InitSize 100</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">int</span> ElemType;</span><br><span class="line">ElemType name;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="keyword">int</span> *data;              <span class="comment">// 指针</span></span><br><span class="line">    <span class="keyword">int</span> length;             <span class="comment">// 表长</span></span><br><span class="line">    <span class="keyword">int</span> MaxSize = InitSize; <span class="comment">//表长的最大值</span></span><br><span class="line">&#125; SeqList;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* tool 打印线性表 */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">PrintList</span><span class="params">(SeqList SeqL)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; SeqL.length; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>, SeqL.data[i]);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q1. 从顺序表中删除具有最小值得元素（假设唯一）并由函数返回被删除元素的值，空出的位置由最后一个元素填补，若顺序表为空，则显示出错信息并退出运行 */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">DeleteMin_Q1</span><span class="params">(SeqList &amp;L, ElemType &amp;element)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (L.length == <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    element = L.data[<span class="number">0</span>]; <span class="comment">// 初始化为表头元素</span></span><br><span class="line">    <span class="keyword">int</span> minPos = <span class="number">0</span>;      <span class="comment">// 记录线性表中最小元素的下标</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; L.length; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (L.data[i] &lt; element)</span><br><span class="line">        &#123;</span><br><span class="line">            element = L.data[i];</span><br><span class="line">            minPos = i;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    L.data[minPos] = L.data[L.length - <span class="number">1</span>];</span><br><span class="line">    L.length--;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q2. 设计高效算法，将顺序表L的所有元素逆置， 要求算法的空间复杂度为 O(1) */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">ReverseList_Q2</span><span class="params">(SeqList &amp;L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (L.length == <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">int</span> temp;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; L.length / <span class="number">2</span>; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        temp = L.data[i];</span><br><span class="line">        L.data[i] = L.data[L.length - <span class="number">1</span> - i]; <span class="comment">/* A + B = length-1 则 B = length-1-A */</span></span><br><span class="line">        L.data[L.length - <span class="number">1</span> - i] = temp;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q3. 对长度为 n 的顺序表 L 编写一个时间复杂度为 O(n) ，空间复杂度为 O(1) 的算法，该算法删除线性表中所有值为 x 的元素 */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">DeteleAll_Q3</span><span class="params">(SeqList &amp;L, ElemType x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (L.length == <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">int</span> frontxNum = <span class="number">0</span>; <span class="comment">/* 记录当前下标之前等于 x 的元素个数 */</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; L.length; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 目标元素，frontxNum+1 */</span></span><br><span class="line">        <span class="keyword">if</span> (L.data[i] == x)</span><br><span class="line">        &#123;</span><br><span class="line">            frontxNum++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">/* 非目标元素，当前元素下标前移 frontxNum 位*/</span></span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            L.data[i - frontxNum] = L.data[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/* 表长减去删除元素个数 */</span></span><br><span class="line">    L.length -= frontxNum;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q4. 从有序顺序表中删除其值在给定值 s 与 t 之间（要求 s &lt; t ）的所有元素，若 s 或 t 不合理或顺序表为空，则显示出错信息并退出运行 */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">DeleteBetweenST_Q4</span><span class="params">(SeqList &amp;L, ElemType s, ElemType t)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 注意题目是有序顺序表 */</span></span><br><span class="line">    <span class="comment">/* 先按要求排除不正常情况 */</span></span><br><span class="line">    <span class="keyword">if</span> (s &gt;= t || L.length == <span class="number">0</span>) <span class="comment">// 如果 s &gt;= t 或者为空表，则返回 false</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="keyword">if</span> (L.data[<span class="number">0</span>] &gt; t || L.data[L.length - <span class="number">1</span>] &lt; s) <span class="comment">// 如果有序顺序表中的元素不在[s,t]区间内，则直接返回 true</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> end, start;</span><br><span class="line">    <span class="keyword">for</span> (start = <span class="number">0</span>; start &lt; L.length &amp;&amp; L.data[start] &lt; s; start++) <span class="comment">// 得到 start，其为第一个被删除元素的下标</span></span><br><span class="line">        ;</span><br><span class="line">    <span class="keyword">for</span> (end = start; end &lt; L.length &amp;&amp; L.data[end] &lt;= t; end++) <span class="comment">// 得到 end，注意这里的 &lt;= ，end 为第一个需要前移的元素的下标</span></span><br><span class="line">        ;</span><br><span class="line">    <span class="comment">// end-start 为需要删除的元素个数，那么直接把后面的元素前移 end-start 位即可，当然 end 会一直遍历到顺序表尾部</span></span><br><span class="line">    <span class="keyword">for</span> (; end &lt; L.length; end++, start++)</span><br><span class="line">    &#123;</span><br><span class="line">        L.data[start] = L.data[end];</span><br><span class="line">    &#125;</span><br><span class="line">    L.length -= end - start;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q5. 从顺序表中删除其值在给定值 s 与 t 之间，包含 s，t（要求 s &lt; t）的所有元素，若 s 或 t 不合理或顺序表为空，则显示出错信息并退出运行 */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">DeleteBetweenST_Q5</span><span class="params">(SeqList &amp;L, ElemType s, ElemType t)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (s &gt;= t || L.length == <span class="number">0</span> || L.data[<span class="number">0</span>] &gt; t || L.data[L.length - <span class="number">1</span>] &lt; s)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> frontdelNum = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; L.length; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (L.data[i] &gt;= s &amp;&amp; L.data[i] &lt;= t)</span><br><span class="line">        &#123;</span><br><span class="line">            frontdelNum++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            L.data[i - frontdelNum] = L.data[i];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    L.length -= frontdelNum;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q6. 从有序顺序表删除所有其值重复的元素，使表中所有元素值均不同 */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">DeleteRepeat_Q6</span><span class="params">(SeqList &amp;L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (L.length == <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> i, j;</span><br><span class="line">    <span class="comment">/* 使用双指针 */</span></span><br><span class="line">    <span class="keyword">for</span> (i = <span class="number">0</span>, j = <span class="number">1</span>; j &lt; L.length; j++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 如果左指针元素和右指针元素相同，则右指针+1，继续比对</span></span><br><span class="line"><span class="comment">        if (L.data[i] == L.data[j])</span></span><br><span class="line"><span class="comment">            j++; */</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 如果碰到下一个不同的元素，则左指针+1，并且覆盖为新元素 */</span></span><br><span class="line">        <span class="keyword">if</span> (L.data[i] != L.data[j])</span><br><span class="line">        &#123;</span><br><span class="line">            i++;</span><br><span class="line">            L.data[i] = L.data[j];</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    L.length = i + <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q7. 将两个有序顺序表合并为一个新的有序顺序表，由函数返回结果顺序表 */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">CombineSeqL_Q7</span><span class="params">(SeqList L1, SeqList L2, SeqList &amp;L3)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 如果两个顺序表的长度之和超过了顺序表的最大长度，则返回 false */</span></span><br><span class="line">    <span class="keyword">if</span> (L1.length + L2.length &gt; L3.MaxSize)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (L1.length != <span class="number">0</span> &amp;&amp; L2.length != <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> i = <span class="number">0</span>, j = <span class="number">0</span>, k = <span class="number">0</span>;</span><br><span class="line">        <span class="keyword">while</span> (i &lt; L1.length &amp;&amp; j &lt; L2.length)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (L1.data[i] &lt;= L2.data[j])</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">/* L3.data[k++] = L1.data[i++]; */</span></span><br><span class="line">                L3.data[k] = L1.data[i];</span><br><span class="line">                k++;</span><br><span class="line">                i++;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">/* L3.data[k++] = L2.data[j++]; */</span></span><br><span class="line">                L3.data[k] = L2.data[j];</span><br><span class="line">                k++;</span><br><span class="line">                j++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">while</span> (i &lt; L1.length)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">/* L3.data[k++] = L1.data[i++]; */</span></span><br><span class="line">            L3.data[k] = L1.data[i];</span><br><span class="line">            k++;</span><br><span class="line">            i++;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">while</span> (j &lt; L2.length)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">/* L3.data[k++] = L2.data[j++]; */</span></span><br><span class="line">            L3.data[k] = L2.data[j];</span><br><span class="line">            k++;</span><br><span class="line">            j++;</span><br><span class="line">        &#125;</span><br><span class="line">        L3.length = k;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q8. 已知在一维数组A[m+n]中依次存放两个线性表(a1,a2,...,am)和(b1,b2,...bn)，试编写一个函数，将数组中两个顺序表的位置互换，即将(b1,b2,...bn)放在(a1,a2,...,am)前面 */</span></span><br><span class="line"><span class="comment">/*</span></span><br><span class="line"><span class="comment">    解法：</span></span><br><span class="line"><span class="comment">    1. 可以先把两个部分分别进行逆序，得到：(am,...,a2,a1) 和 (bn,...,b2,b1)，即为 (am,...,a2,a1,bn,...,b2,b1)</span></span><br><span class="line"><span class="comment">    2. 再整体逆序可以得到：(b1,b2,...,bn,a1,a2,...,am)，即为结果</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"><span class="comment">/* 将下标为start到end的元素逆序排列 */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">Reverse</span><span class="params">(SeqList &amp;L, <span class="keyword">int</span> start, <span class="keyword">int</span> end)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (start &gt; end || end &gt;= L.length)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 需要逆序元素个数的一半 */</span></span><br><span class="line">    <span class="keyword">int</span> mid = (end - start + <span class="number">1</span>) / <span class="number">2</span>;</span><br><span class="line">    <span class="comment">/* 逆序交换， i 和 start 同时增加，start作为下标，i 作为交换次数 */</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; mid; i++, start++)</span><br><span class="line">    &#123;</span><br><span class="line">        ElemType temp = L.data[start];</span><br><span class="line">        L.data[start] = L.data[end - i];</span><br><span class="line">        L.data[end - i] = temp;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">ReverseTwoPartList_Q8</span><span class="params">(SeqList &amp;L, <span class="keyword">int</span> m, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="built_in">Reverse</span>(L, <span class="number">0</span>, m - <span class="number">1</span>);</span><br><span class="line">    <span class="comment">// PrintList(L);</span></span><br><span class="line">    <span class="built_in">Reverse</span>(L, m, L.length - <span class="number">1</span>);</span><br><span class="line">    <span class="comment">// PrintList(L);</span></span><br><span class="line">    <span class="built_in">Reverse</span>(L, <span class="number">0</span>, L.length - <span class="number">1</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q9. 线性表(a1,a2,...an)中的元素有序递增且按顺序存储与计算机内，要求设计一个算法，完成用最少时间在表中查找数值 x 的元素，若找到，则将其与后继元素位置相交换，若查不到，则将其插入表中并使表中元素仍有序递增。 */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">FindXSwapOrInsert_Q9</span><span class="params">(SeqList &amp;L, ElemType x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (L.length == <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="comment">/* 首先要求用最短时间查询值为 x 的元素，这里使用二分法 */</span></span><br><span class="line">    <span class="keyword">int</span> low = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> high = L.length - <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">int</span> mid;</span><br><span class="line">    <span class="keyword">while</span> (low &lt;= high)</span><br><span class="line">    &#123;</span><br><span class="line">        mid = (low + high) / <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">if</span> (L.data[mid] == x)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (L.data[mid] &gt; x)</span><br><span class="line">        &#123;</span><br><span class="line">            high = mid - <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="comment">// L.data[mid] &lt; x</span></span><br><span class="line">        &#123;</span><br><span class="line">            low = mid + <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/* 若查找到元素，且该元素不是顺序表的最后一个 */</span></span><br><span class="line">    <span class="keyword">if</span> (L.data[mid] == x &amp;&amp; mid != L.length - <span class="number">1</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">// 交换</span></span><br><span class="line">        ElemType temp = L.data[mid];</span><br><span class="line">        L.data[mid] = L.data[mid + <span class="number">1</span>];</span><br><span class="line">        L.data[mid + <span class="number">1</span>] = temp;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 若没有查找到元素，则按顺序插入，在二分结束后，low 和 high 的指针相邻：high low*/</span></span><br><span class="line">    <span class="keyword">if</span> (L.length &lt;= L.MaxSize - <span class="number">1</span> &amp;&amp; low &gt; high)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> i;</span><br><span class="line">        <span class="comment">/* 由于相邻，则 L.data[high] &lt; x &lt; L.data[low] ，因此下标从 low （包括 low ）以后的元素后移 */</span></span><br><span class="line">        <span class="keyword">for</span> (i = L.length - <span class="number">1</span>; i &gt;= low; i--)</span><br><span class="line">        &#123;</span><br><span class="line">            L.data[i + <span class="number">1</span>] = L.data[i];</span><br><span class="line">        &#125;</span><br><span class="line">        L.data[i + <span class="number">1</span>] = x;</span><br><span class="line">        L.length++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"><span class="comment">/* Q10. 【2010】设将n(n&gt;1)个整数存放到一堆数组R中，设计一个在时间和空间两方面尽可能高效的算法，使得R中保存的序列循环左移p(0&lt;p&lt;n)个位置，即将R中数据由(X0,X1,...,Xn)变为(Xp,Xp+1,...,Xn-1,X0,X1,...,Xp-1)</span></span><br><span class="line"><span class="comment">要求：  1. 给出算法基本思想</span></span><br><span class="line"><span class="comment">    2. 根据设计思想采用C/C++来描述算法</span></span><br><span class="line"><span class="comment">    3. 说明空间复杂度和时间复杂度  */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">MoveList_Q10</span><span class="params">(SeqList &amp;L, <span class="keyword">int</span> p)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 对于循环左移 p 个位置而言，相当于将 p 个序列元素放置到了 n-p 个元素之后，即：</span></span><br><span class="line"><span class="comment">    由 &#123;a1,a2,...ap,b1,b2,...,bn-p&#125; 变成了 &#123;b1,b2,...,bn-p&#125;&#123;a1,a2,...ap&#125;</span></span><br><span class="line"><span class="comment">    相当于：</span></span><br><span class="line"><span class="comment">    &#123;a1,a2,...,ap&#125; 逆序后 &#123;ap,...,a2,a1&#125;</span></span><br><span class="line"><span class="comment">    &#123;b1,b2,...,bn-p&#125; 逆序后 &#123;bn-p,...,b2,b1&#125;</span></span><br><span class="line"><span class="comment">    拼接到一起后再逆序，得到 &#123;b1,b2,...,bn-p,a1,a2,...,ap&#125;</span></span><br><span class="line"><span class="comment">    */</span></span><br><span class="line">    <span class="keyword">if</span> (L.length == <span class="number">0</span> || p &lt;= <span class="number">0</span> || p &gt;= L.length)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    <span class="built_in">Reverse</span>(L, <span class="number">0</span>, p - <span class="number">1</span>);</span><br><span class="line">    <span class="built_in">Reverse</span>(L, p, L.length - <span class="number">1</span>);</span><br><span class="line">    <span class="built_in">Reverse</span>(L, <span class="number">0</span>, L.length - <span class="number">1</span>);</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="comment">/* 空间复杂度为：O(1)   时间复杂度为：O(n) */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q11. 一个长度为 L (L&gt;=1) 的升序序列 S ，处在第L/2（向上取整）个位置的数称为 S 的中位数。例如，若序列 S1 = (11, 13, 15, 17, 19)，则 S1 的中位数是 15，两个序列的中位数是含它们所有元素的升序序列的中位数。例如，若 S2 = (2,4,6,8,20)，则 S1 和 S2 的中位数是 11 .现在右两个等长升序序列，设计一个在时间空间都尽可能高效的算法，找出两个序列的中位数，要求:</span></span><br><span class="line"><span class="comment">1. 给出算法的基本设计思想</span></span><br><span class="line"><span class="comment">2. 代码描述并注释</span></span><br><span class="line"><span class="comment">3. 说明时间复杂度和空间复杂度 */</span></span><br><span class="line"><span class="function">ElemType <span class="title">FindMid_Q11</span><span class="params">(SeqList L1, SeqList L2)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 从两个序列的中间开始比较，向两边扩散，可以看作是一种二分法 */</span></span><br><span class="line">    <span class="keyword">int</span> start1 = <span class="number">0</span>, start2 = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">int</span> end1 = L1.length - <span class="number">1</span>, end2 = L2.length - <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">int</span> mid1, mid2;</span><br><span class="line">    <span class="keyword">while</span> (start1 != end1 || start2 != end2)</span><br><span class="line">    &#123;</span><br><span class="line">        mid1 = (start1 + end1) / <span class="number">2</span>;</span><br><span class="line">        mid2 = (start2 + end2) / <span class="number">2</span>;</span><br><span class="line">        <span class="keyword">if</span> (L1.data[mid1] == L2.data[mid2])</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">return</span> L1.data[mid1];</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (L1.data[mid1] &lt; L2.data[mid2])</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> ((start1 + end1) % <span class="number">2</span> == <span class="number">0</span>)</span><br><span class="line">            &#123;</span><br><span class="line">                start1 = mid1;</span><br><span class="line">                end2 = mid2;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">            &#123;</span><br><span class="line">                start1 = mid1 + <span class="number">1</span>;</span><br><span class="line">                end2 = mid2;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> ((start2 + end2 % <span class="number">2</span> == <span class="number">0</span>))</span><br><span class="line">            &#123;</span><br><span class="line">                end1 = mid1;</span><br><span class="line">                start2 = mid2;</span><br><span class="line">            &#125;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">            &#123;</span><br><span class="line">                end1 = mid1;</span><br><span class="line">                start2 = mid2;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> L1.data[start1] &lt; L2.data[start2] ? L1.data[start1] : L2.data[start2];</span><br><span class="line">    <span class="comment">/* 时间复杂度 O(log2n) 空间复杂度 O(1) */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q12. [2013] 已知一个整数序列 A=(a0,a1,...,an-1)，其中 0&lt;=ai&lt;n(0&lt;=i&lt;n)。若存在ap1=ap2=...=apm=x 且 m&gt;n/2(0&lt;=pk&lt;n, 1&lt;=k&lt;=m)，则称 x 为 A 的主元素，例如 A=(0,5,5,3,5,7,5,5)，则 5 为主元素；又如 A=(0,5,5,3,5,1,5,7)，则 A 中没有主元素(m=n/2)，假设 A 中的 n 个元素保存在一个一维数组中，设计一个尽可能高效的算法，招呼A的主元素，若存在主元素，则输出该元素，否则输出-1，要求：</span></span><br><span class="line"><span class="comment">1. 给出算法基本设计思路</span></span><br><span class="line"><span class="comment">2. 代码</span></span><br><span class="line"><span class="comment">3. 时间复杂度和空间复杂度 */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">GetMainElement_Q12</span><span class="params">(<span class="keyword">int</span> *arr, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 如果一个数组存在主元素，那么其至少有两个主元素一定相邻，从而使得对其的计数 count &gt;= 2 */</span></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    1. 候选主元素初始化为数组第一个元素</span></span><br><span class="line"><span class="comment">    2. 向后遍历数组，当元素与候选主元素相同时count+1，不同则count-1</span></span><br><span class="line"><span class="comment">    3. 当下一个元素不等于候选主元素且count=0时，则更新候选主元素为下一个元素，并继续遍历</span></span><br><span class="line"><span class="comment">    4. 遍历完成后，对候选主元素进行判断，统计出现次数是否满足要求 count &gt; n/2，满足则返回主元素， 否则返回-1*/</span></span><br><span class="line">    <span class="keyword">if</span> (arr)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> count = <span class="number">1</span>;</span><br><span class="line">        <span class="keyword">int</span> i, cur_element;</span><br><span class="line"></span><br><span class="line">        cur_element = arr[<span class="number">0</span>];</span><br><span class="line">        <span class="keyword">for</span> (i = <span class="number">1</span>; i &lt; n; i++)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">if</span> (arr[i] == cur_element)</span><br><span class="line">                count++;</span><br><span class="line">            <span class="keyword">else</span></span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">if</span> (count &gt; <span class="number">0</span>)</span><br><span class="line">                    count--;</span><br><span class="line">                <span class="keyword">else</span></span><br><span class="line">                &#123;</span><br><span class="line">                    cur_element = arr[i];</span><br><span class="line">                    count = <span class="number">1</span>;</span><br><span class="line">                &#125;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (count &gt; <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">for</span> (i = count = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="keyword">if</span> (arr[i] == cur_element)</span><br><span class="line">                    count++;</span><br><span class="line">            &#125;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">if</span> (count &gt; n / <span class="number">2</span>)</span><br><span class="line">            <span class="keyword">return</span> cur_element;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="number">-1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/* 时间复杂度为 O(n) 空间复杂度为 O(1) */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q13. [2018] 给定一个含 n(n&gt;=1) 个整数的数组，设计一个在时间上尽可能高效的算法，找出数组中未出现的最小正整数，例如&#123;-5,3,2,3&#125;中未出现的最小正整数是 1，&#123;1,2,3&#125;中未出现的最小正整数是 4，要求：</span></span><br><span class="line"><span class="comment">1. 给出算法基本设计思想</span></span><br><span class="line"><span class="comment">2. 代码</span></span><br><span class="line"><span class="comment">3. 时间复杂度和空间复杂度 */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">GetMinPositiveInt_Q13</span><span class="params">(<span class="keyword">int</span> *arr, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 返回的最小正整数一定在区间[1,n+1]内，因此arr[i]如果不在这个区间内，则不做操作，在这个区间内则用一个数组做记录 */</span></span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    1. 创建大小为 n 的数组 memory，并初始化为 0</span></span><br><span class="line"><span class="comment">    2. 遍历数组 arr，寻找在 [1,n] 范围内的值，并设置memory中对应下标的元素为 1</span></span><br><span class="line"><span class="comment">    3. 最后遍历数组 memory，寻找第一个没有被设置为 1 的元素下标（即没有出现过），由于只能返回 [1,n]，因此最后需要另外加上一种情况 n+1 */</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> *memory = (<span class="keyword">int</span> *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(<span class="keyword">int</span>) * n);</span><br><span class="line">    <span class="built_in">memset</span>(memory, <span class="number">0</span>, <span class="built_in"><span class="keyword">sizeof</span></span>(<span class="keyword">int</span>) * n);</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (arr[i] &gt;= <span class="number">1</span> &amp;&amp; arr[i] &lt;= n)</span><br><span class="line">        &#123;</span><br><span class="line">            memory[arr[i] - <span class="number">1</span>] = <span class="number">1</span>;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (memory[i] == <span class="number">0</span>)</span><br><span class="line">            <span class="keyword">return</span> i + <span class="number">1</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> n + <span class="number">1</span>;</span><br><span class="line">    <span class="comment">/* 时间复杂度为 O(n) 空间复杂度为 O(n) */</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q14. 定义三元组(a,b,c)（a,b,c均为正整数）的距离 D=|a-b|+|b-c|+|c-a|。给定3个非空整数集合 S1, S2 和 S3，按升序分别存储在3个数组中。设计一个尽可能高效的算法，计算并输出所有可能的三元组(a,b,c)(a∈S1, b∈S2, c∈S3)中的最小距离。</span></span><br><span class="line"><span class="comment">例如 S1=&#123;-1,0,9&#125; S2=&#123;-25,-10,10,11&#125; S3=&#123;2,9,17,30,41&#125;，则最小距离为2，相应的三元组为&#123;9,10,9&#125;</span></span><br><span class="line"><span class="comment">要求：</span></span><br><span class="line"><span class="comment">1. 给出算法的设计思想</span></span><br><span class="line"><span class="comment">2. 代码</span></span><br><span class="line"><span class="comment">3. 时间复杂度和空间复杂度</span></span><br><span class="line"><span class="comment">*/</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">abs_</span><span class="params">(<span class="keyword">int</span> num)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (num &lt; <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> -num;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> num;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">isMin</span><span class="params">(<span class="keyword">int</span> a, <span class="keyword">int</span> b, <span class="keyword">int</span> c)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (a &lt;= b &amp;&amp; a &lt;= c)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">        <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">GetMinDistanceTuple_Q14</span><span class="params">(<span class="keyword">int</span> *s1, <span class="keyword">int</span> m, <span class="keyword">int</span> *s2, <span class="keyword">int</span> n, <span class="keyword">int</span> *s3, <span class="keyword">int</span> p)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/*</span></span><br><span class="line"><span class="comment">    1. 由于 D=|a-b|+|b-c|+|c-a|&gt;=0，当 a=b=c 时，D最小</span></span><br><span class="line"><span class="comment">    2. 在一维坐标系上，a,b,c三点中，必定有一点在两点之间（可重合），假设取c最大值，a为最小值，那 D=2|c-a|，要使|c-a|最小，只需要让元组中最大值和最小值的差值最小即可</span></span><br><span class="line"><span class="comment">    3. 可以固定比较元组中的最大值 c ，之后每次更新元组中的最小值 a ，直到计算出最小距离 D*/</span></span><br><span class="line">    <span class="keyword">int</span> min_D = <span class="number">0x7fffffff</span>;</span><br><span class="line">    <span class="keyword">int</span> cur_D;</span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">0</span>, j = <span class="number">0</span>, k = <span class="number">0</span>;</span><br><span class="line">    <span class="keyword">while</span> (i &lt; m &amp;&amp; j &lt; n &amp;&amp; k &lt; p &amp;&amp; min_D &gt; <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        cur_D = <span class="built_in">abs_</span>(s1[i] - s2[j]) + <span class="built_in">abs_</span>(s2[j] - s3[k]) + <span class="built_in">abs_</span>(s3[k] - s1[i]);</span><br><span class="line">        <span class="keyword">if</span> (cur_D &lt; min_D)</span><br><span class="line">            min_D = cur_D;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (<span class="built_in">isMin</span>(s1[i], s2[j], s3[k]))</span><br><span class="line">            i++;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (<span class="built_in">isMin</span>(s2[j], s1[i], s3[k]))</span><br><span class="line">            j++;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            k++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> min_D;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">// SeqList L;</span></span><br><span class="line">    <span class="comment">// // printf(&quot;%p&quot;, L);</span></span><br><span class="line">    <span class="comment">// L.data = (ElemType *)malloc(sizeof(ElemType) * InitSize); // C动态分配</span></span><br><span class="line">    <span class="comment">// // L.data = new ElemType[InitSize]; // C++动态分配</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;L.length);</span></span><br><span class="line">    <span class="comment">// for (int i = 0; i &lt; L.length; i++)</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     scanf_s(&quot;%d&quot;, &amp;L.data[i]);</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">    <span class="comment">// printf(&quot;before: &quot;);</span></span><br><span class="line">    <span class="comment">// PrintList(L);</span></span><br><span class="line">    <span class="comment">// printf(&quot;after: &quot;);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q1 */</span></span><br><span class="line">    <span class="comment">// int element;</span></span><br><span class="line">    <span class="comment">// DeleteMin_Q1(L, element);</span></span><br><span class="line">    <span class="comment">// PrintList(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q2 */</span></span><br><span class="line">    <span class="comment">// ReverseList_Q2(L);</span></span><br><span class="line">    <span class="comment">// PrintList(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q3 */</span></span><br><span class="line">    <span class="comment">// DeteleAll_Q3(L, 4);</span></span><br><span class="line">    <span class="comment">// PrintList(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q4 */</span></span><br><span class="line">    <span class="comment">// DeleteBetweenST_Q4(L, 4, 10);</span></span><br><span class="line">    <span class="comment">// PrintList(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q5 */</span></span><br><span class="line">    <span class="comment">// DeleteBetweenST_Q5(L, 4, 8);</span></span><br><span class="line">    <span class="comment">// PrintList(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q6 */</span></span><br><span class="line">    <span class="comment">// DeleteRepeat_Q6(L);</span></span><br><span class="line">    <span class="comment">// PrintList(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q7 */</span></span><br><span class="line">    <span class="comment">// SeqList L1;</span></span><br><span class="line">    <span class="comment">// L1.data = (ElemType *)malloc(sizeof(ElemType) * InitSize);</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;L1.length);</span></span><br><span class="line">    <span class="comment">// for (int i = 0; i &lt; L1.length; i++)</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     scanf_s(&quot;%d&quot;, &amp;L1.data[i]);</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">    <span class="comment">// SeqList L2;</span></span><br><span class="line">    <span class="comment">// L2.data = (ElemType *)malloc(sizeof(ElemType) * InitSize);</span></span><br><span class="line">    <span class="comment">// CombineSeqL_Q7(L, L1, L2);</span></span><br><span class="line">    <span class="comment">// PrintList(L2);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q8 */</span></span><br><span class="line">    <span class="comment">// ReverseTwoPartList_Q8(L, 1, 2);</span></span><br><span class="line">    <span class="comment">// PrintList(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q9 */</span></span><br><span class="line">    <span class="comment">// FindXSwapOrInsert_Q9(L, 5);</span></span><br><span class="line">    <span class="comment">// PrintList(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q10 [2010] */</span></span><br><span class="line">    <span class="comment">// MoveList_Q10(L, 4);</span></span><br><span class="line">    <span class="comment">// PrintList(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q11 [2011] */</span></span><br><span class="line">    <span class="comment">// SeqList L1;</span></span><br><span class="line">    <span class="comment">// L1.data = (ElemType *)malloc(sizeof(ElemType) * InitSize);</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;L1.length);</span></span><br><span class="line">    <span class="comment">// for (int i = 0; i &lt; L1.length; i++)</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     scanf_s(&quot;%d&quot;, &amp;L1.data[i]);</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">    <span class="comment">// printf(&quot;%d&quot;, FindMid_Q11(L, L1));</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q12. [2013] */</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// int *arr = (int *)malloc(sizeof(int) * n);</span></span><br><span class="line">    <span class="comment">// // printf(&quot;%p\n&quot;, arr);</span></span><br><span class="line">    <span class="comment">// for (int i = 0; i &lt; n; i++)</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     scanf_s(&quot;%d&quot;, &amp;arr[i]);</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">    <span class="comment">// for (int i = 0; i &lt; n; i++)</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     printf(&quot;%d &quot;, arr[i]);</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">    <span class="comment">// printf(&quot;\n&quot;);</span></span><br><span class="line">    <span class="comment">// printf(&quot;%d&quot;, GetMainElement_Q12(arr, n));</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q13. [2018] */</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// int *arr = (int *)malloc(sizeof(int) * n);</span></span><br><span class="line">    <span class="comment">// for (int i = 0; i &lt; n; i++)</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     scanf_s(&quot;%d&quot;, &amp;arr[i]);</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">    <span class="comment">// printf(&quot;%d&quot;, GetMinPositiveInt_Q13(arr, n));</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q14. [2020] */</span></span><br><span class="line">    <span class="comment">// int m, n, p;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;m);</span></span><br><span class="line">    <span class="comment">// int *s1 = (int *)malloc(sizeof(int) * m);</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// int *s2 = (int *)malloc(sizeof(int) * n);</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;p);</span></span><br><span class="line">    <span class="comment">// int *s3 = (int *)malloc(sizeof(int) * p);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// for (int i = 0; i &lt; m; i++)</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     scanf_s(&quot;%d&quot;, &amp;s1[i]);</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">    <span class="comment">// for (int i = 0; i &lt; n; i++)</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     scanf_s(&quot;%d&quot;, &amp;s2[i]);</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">    <span class="comment">// for (int i = 0; i &lt; p; i++)</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     scanf_s(&quot;%d&quot;, &amp;s3[i]);</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">// printf(&quot;%d&quot;, GetMinDistanceTuple_Q14(s1, m, s2, n, s3, p));</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h2 id="线性表的链式表示">线性表的链式表示</h2><p>链式存储线性表时，<strong>不需要</strong>使用<strong>地址连续</strong>的存储单元，<strong>不要求逻辑上相邻的元素在物理位置上也相邻</strong>，通过“链”建立其数据元素之间的逻辑关系，插入和删除操作只需要修改指针，但是也会<strong>失去随机存储</strong>的优点。</p><h3 id="单链表的定义">单链表的定义</h3><p>线性表的链式存储又称为单链表，通过一组任意的存储单元来存储线性表中的数据单元，为了建立元素间的线性关系，对每个链表结点，除了存放元素自身的信息外，还存放一个指向其后继的指针。</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LNode</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="comment">/* data */</span></span><br><span class="line">    ElemType data;      <span class="comment">// 数据单元</span></span><br><span class="line">    <span class="class"><span class="keyword">struct</span> <span class="title">LNode</span> *<span class="title">next</span>;</span> <span class="comment">// 指向下一个 node 的指针</span></span><br><span class="line">&#125; LNode, *LinkList;     <span class="comment">// 可以表示单结点或是指向整个链表的指针</span></span><br></pre></td></tr></table></figure><p>需要注意的是：</p><ul><li>非随机存取的存储结构，不能直接找到表中某个特定结点，查找特定结点需要从表头开始遍历。</li><li>通常用头指针来标识一个单链表，头指针为NULL时表示一个空表。</li><li>在第一个结点之前通常附加一个头结点，数据域可以为NULL也可以是表长，其指针指向第一个元素。带头结点的优点：<ul><li>第一个元素结点的操作和表上其他位置的结点操作一致</li><li>无论链表是否为空，头指针都指向头结点的非空指针（空表中头结点的指针域为空）</li></ul></li></ul><h3 id="单链表上的操作">单链表上的操作</h3><h4 id="按顺序插入结点">按顺序插入结点</h4><ul><li><p>头插法</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">LinkList <span class="title">InsertListFromHead</span><span class="params">(LinkList &amp;L, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 创建头结点，并使其 next 指向 NULL */</span></span><br><span class="line">    L = (LinkList)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode)); <span class="comment">/* 注意这里不是 LinkList *，因为在struct的定义里已经被设置为了数组形式存储LNode */</span></span><br><span class="line">    L-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 创建要插入结点 */</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 获取数据域 */</span></span><br><span class="line">        <span class="keyword">int</span> x;</span><br><span class="line">        <span class="built_in">scanf_s</span>(<span class="string">&quot;%d&quot;</span>, &amp;x);</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 为新建结点创建空间 */</span></span><br><span class="line">        LNode *p = (LNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line">        p-&gt;data = x;       <span class="comment">/* 设置数据域 */</span></span><br><span class="line">        p-&gt;next = L-&gt;next; <span class="comment">/* 插入的结点指针指向头结点指针所指区域 */</span></span><br><span class="line">        L-&gt;next = p;       <span class="comment">/* 头结点指针指向插入的元素 */</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>头插法时，读入数据的顺序与生成链表中的元素顺序是相反的，每个结点插入时间为 $O(1)$ ，当表长为 $n$ 时，时间复杂度为 $O(n)$ 。</p><p>假设不存在头结点，则插入代码为：</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">LinkList <span class="title">InsertListFromHead</span><span class="params">(LinkList &amp;L, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;    </span><br><span class="line"><span class="comment">/* 假设没有头结点 */</span></span><br><span class="line">    <span class="keyword">int</span> x;</span><br><span class="line">    <span class="built_in">scanf_s</span>(<span class="string">&quot;%d&quot;</span>, &amp;x);</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 第一个元素的插入 */</span></span><br><span class="line">    L = (LinkList)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line">    L-&gt;data = x;</span><br><span class="line">    L-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 其余元素插入 */</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 获取数据域 */</span></span><br><span class="line">        <span class="keyword">int</span> x;</span><br><span class="line">        <span class="built_in">scanf_s</span>(<span class="string">&quot;%d&quot;</span>, &amp;x);</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 为插入结点创建空间 */</span></span><br><span class="line">        LNode *p = (LNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line">        p-&gt;data = x;</span><br><span class="line">        p-&gt;next = L-&gt;next;</span><br><span class="line">        L-&gt;next = p;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure></li><li><p>尾插法</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">LinkList <span class="title">InsertListFromTail</span><span class="params">(LinkList &amp;L, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 头结点 */</span></span><br><span class="line">    L = (LinkList)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line">    L-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 引入一个尾结点，初始化为与头结点相同的域 */</span></span><br><span class="line">    LNode *tail = L;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 获取数据域 */</span></span><br><span class="line">        <span class="keyword">int</span> x;</span><br><span class="line">        <span class="built_in">scanf_s</span>(<span class="string">&quot;%d&quot;</span>, &amp;x);</span><br><span class="line"></span><br><span class="line">        LNode *p = (LNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line">        p-&gt;data = x;</span><br><span class="line">        <span class="comment">/* 添加在 tail 后 */</span></span><br><span class="line">        p-&gt;next = tail-&gt;next;</span><br><span class="line">        tail-&gt;next = p;</span><br><span class="line">        <span class="comment">/* tail = p，使得下一个插入结点能够在 p 之后 */</span></span><br><span class="line">        tail = p;</span><br><span class="line">    &#125;</span><br><span class="line">    tail-&gt;next = <span class="literal">NULL</span>; <span class="comment">// 尾结点指针置空</span></span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>时间复杂度为 $O(n)$ 。</p></li></ul><h4 id="按位序查找值">按位序查找值</h4><p>返回位序对应的结点的数据域</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 按位序找到结点值 */</span></span><br><span class="line"><span class="function">LinkList <span class="title">GetDataByOrder</span><span class="params">(LinkList L, <span class="keyword">int</span> order)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 结点指针指向第一个元素 */</span></span><br><span class="line">    LNode *p = L-&gt;next;</span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">1</span>; <span class="comment">/* 计数用 */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 当位序为 0 的时候，返回头结点数据域 */</span></span><br><span class="line">    <span class="keyword">if</span> (order == <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> L;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (order &lt; <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 只要 p 不为 nullptr 且 还不到对应位序，当 i==order时，即为要找的结点，此时返回p-&gt;data即可 */</span></span><br><span class="line">    <span class="keyword">while</span> (p &amp;&amp; i &lt; order)</span><br><span class="line">    &#123;</span><br><span class="line">        p = p-&gt;next;</span><br><span class="line">        i++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> p;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="按值查找位序">按值查找位序</h4><p>按值查找位序，没有找到对应的值则返回0</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 按值查找结点位序 */</span></span><br><span class="line"><span class="function">LinkList <span class="title">GetOrderByData</span><span class="params">(LinkList L, ElemType data)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">1</span>;</span><br><span class="line">    LNode *p = L-&gt;next; <span class="comment">// 指针指向第一个元素</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (p)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (p-&gt;data == data)</span><br><span class="line">            <span class="keyword">return</span> p;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            p = p-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        i++;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="按位序插入结点">按位序插入结点</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 按位置插入结点 */</span></span><br><span class="line"><span class="function">LinkList <span class="title">InsertListByOrder</span><span class="params">(LinkList &amp;L, <span class="keyword">int</span> order, ElemType e)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">1</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 要插入的结点 */</span></span><br><span class="line">    LNode *newNode = (LNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 继承头指针 */</span></span><br><span class="line">    LNode *preNode = L;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (order &lt; <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (order == <span class="number">0</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">return</span> L;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 如果 order 超出了链表长度， preNode 会是最后一个结点，新插入的结点会被自动放在最后一个*/</span></span><br><span class="line">    <span class="comment">/* preNode-&gt;next 初始指向了第一个结点，在 while 循环结束后为需要插入结点的前驱结点 */</span></span><br><span class="line">    <span class="keyword">while</span> (preNode &amp;&amp; i &lt; order)</span><br><span class="line">    &#123;</span><br><span class="line">        preNode = preNode-&gt;next;</span><br><span class="line">        i++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* preNode 为 newNode 的前驱结点，将 newNode 链接进来 */</span></span><br><span class="line">    <span class="keyword">if</span> (preNode)</span><br><span class="line">    &#123;</span><br><span class="line">        newNode-&gt;next = preNode-&gt;next;</span><br><span class="line">        preNode-&gt;next = newNode;</span><br><span class="line">        <span class="comment">/* 设置 newNode 的数据域 */</span></span><br><span class="line">        newNode-&gt;data = e;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="按位序删除结点">按位序删除结点</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 按位序删除结点 */</span></span><br><span class="line"><span class="function">LinkList <span class="title">DeleteNodeByOrder</span><span class="params">(LinkList &amp;L, <span class="keyword">int</span> order)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (order &lt; <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">if</span> (order == <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> L;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">1</span>;          <span class="comment">// 计数</span></span><br><span class="line">    LNode *preNode = L; <span class="comment">/* 遍历指针 */</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (preNode &amp;&amp; i &lt; order)</span><br><span class="line">    &#123;</span><br><span class="line">        preNode = preNode-&gt;next;</span><br><span class="line">        i++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 要删除的结点 */</span></span><br><span class="line">    LNode *delNode = preNode-&gt;next;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 要删除的结点如果存在 */</span></span><br><span class="line">    <span class="keyword">if</span> (delNode)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 前驱结点的 next 指向删除结点的 next */</span></span><br><span class="line">        preNode-&gt;next = delNode-&gt;next;</span><br><span class="line">        <span class="built_in">free</span>(delNode); <span class="comment">/* 释放 */</span></span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br><span class="line"></span><br></pre></td></tr></table></figure><h4 id="获取表长">获取表长</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">GetLinkListLen</span><span class="params">(LinkList L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> len = <span class="number">0</span>; <span class="comment">/* 包含头指针的长度 */</span></span><br><span class="line">    LNode *p = L;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (p)</span><br><span class="line">    &#123;</span><br><span class="line">        p = p-&gt;next;</span><br><span class="line">        len++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> len - <span class="number">1</span>; <span class="comment">/* 返回不包含头指针的长度 */</span></span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="双链表的定义">双链表的定义</h3><p>单链表只有后继指针，只能向后遍历。访问<strong>后继结点</strong>的时间复杂度为 $O(1)$ ，访问<strong>前驱结点</strong>的时间复杂度为 $O(n)$ 。</p><p>双链表有前驱和后继指针，可以向前向后遍历。访问<strong>后继结点和前驱结点</strong>的时间复杂度都为 $O(1)$ 。</p><h3 id="双链表上的操作">双链表上的操作</h3><h4 id="按顺序插入结点-v2">按顺序插入结点</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 双链表 尾插法 */</span></span><br><span class="line"><span class="function">DLinklist <span class="title">InsertElemFromTail</span><span class="params">(DLinklist &amp;DL, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    DL = (DLinklist)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(DNode));</span><br><span class="line">    DL-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    DNode *tail = DL;</span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> x;</span><br><span class="line">        <span class="built_in">scanf_s</span>(<span class="string">&quot;%d&quot;</span>, &amp;x);</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 插入结点 p */</span></span><br><span class="line">        DNode *p = (DNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(DNode));</span><br><span class="line">        p-&gt;data = x; <span class="comment">/* 设置插入结点的数据域 */</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 一般插入需要管理四条相关的链子，这里因为是在表尾插入，NULL 没有 next 和 prior指针，省去了对其 prior 的修改 */</span></span><br><span class="line">        p-&gt;next = tail-&gt;next; <span class="comment">/* next 指向 tail-&gt;next */</span></span><br><span class="line">        p-&gt;prior = tail;      <span class="comment">/* prior 指向 tail */</span></span><br><span class="line">        tail-&gt;next = p;       <span class="comment">/* tail-&gt;next 指向 待插入结点 */</span></span><br><span class="line"></span><br><span class="line">        tail = p; <span class="comment">/* tail被赋值为插入结点 p */</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> DL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="按位序插入结点-v2">按位序插入结点</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 双链表 按位序插入结点 */</span></span><br><span class="line"><span class="function">DLinklist <span class="title">InsertElemByOrder</span><span class="params">(DLinklist &amp;DL, <span class="keyword">int</span> order, <span class="keyword">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (order &lt; <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">if</span> (order == <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> DL;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">1</span>;</span><br><span class="line">    DNode *preNode = DL;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 得到要插入位置的前驱结点 */</span></span><br><span class="line">    <span class="keyword">while</span> (preNode &amp;&amp; i &lt; order)</span><br><span class="line">    &#123;</span><br><span class="line">        preNode = preNode-&gt;next;</span><br><span class="line">        i++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    DNode *insNode = (DNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(DNode));</span><br><span class="line">    insNode-&gt;data = x;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 当preNode不是最后一个结点时，处理四条链子 */</span></span><br><span class="line">    <span class="keyword">if</span> (preNode-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        insNode-&gt;next = preNode-&gt;next;</span><br><span class="line">        preNode-&gt;next-&gt;prior = insNode; <span class="comment">/* 如果preNode是最后一个结点，则NULL-&gt;prior会报错，因为NULL不存在该指针 */</span></span><br><span class="line">        preNode-&gt;next = insNode;</span><br><span class="line">        insNode-&gt;prior = preNode;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 如果是最后一个结点，处理3条链子 */</span></span><br><span class="line">        insNode-&gt;next = preNode-&gt;next;</span><br><span class="line">        preNode-&gt;next = insNode;</span><br><span class="line">        insNode-&gt;prior = preNode;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> DL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h4 id="按位序删除结点-v2">按位序删除结点</h4><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="comment">/* 双链表 根据位序删除结点 */</span></span><br><span class="line"><span class="function">DLinklist <span class="title">DeleteDNodeByOrder</span><span class="params">(DLinklist &amp;DL, <span class="keyword">int</span> order)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">if</span> (order &lt; <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    <span class="keyword">if</span> (order == <span class="number">0</span>)</span><br><span class="line">        <span class="keyword">return</span> DL;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> i = <span class="number">1</span>; <span class="comment">/* 计数 */</span></span><br><span class="line">    DNode *preNode = DL;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 找到要删除结点的前驱结点 */</span></span><br><span class="line">    <span class="keyword">while</span> (preNode &amp;&amp; i &lt; order)</span><br><span class="line">    &#123;</span><br><span class="line">        preNode = preNode-&gt;next;</span><br><span class="line">        i++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 要删除的结点 */</span></span><br><span class="line">    DNode *delNode = preNode-&gt;next;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 如果delNode不是最后一个结点，调整两条“链子” */</span></span><br><span class="line">    <span class="keyword">if</span> (delNode-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        preNode-&gt;next = delNode-&gt;next;</span><br><span class="line">        delNode-&gt;next-&gt;prior = preNode;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 如果delNode是最后一个结点，NULL没有前驱指针，只用调整一条链子 */</span></span><br><span class="line">        preNode-&gt;next = delNode-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">free</span>(delNode);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> DL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><h3 id="循环链表">循环链表</h3><ul><li><p>循环单链表</p><ul><li>最后一个结点的next指针指向头结点。在<strong>任何一个位置</strong>插入和删除结点的<strong>操作</strong>都是<strong>等价</strong>的，因此<strong>不需要判断是否为表尾结点</strong>。</li><li>可以从任何一个位置开始遍历整个链表。</li><li>对于循环单链表而言，通常设置的是尾指针，假设尾指针为<code>r</code>，则<code>r-&gt;next</code>即为头指针。如果只有头指针，则对表尾操作需要 $O(n)$ 的时间，因为需要遍历过去，但是如果是尾指针，则对表头表尾操作都只需要 $O(1)$ 的时间。</li></ul><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="function">Linklist <span class="title">InsertDataFromTail</span><span class="params">(Linklist &amp;CL, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    CL = (Linklist)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode) * n);</span><br><span class="line">    CL-&gt;next = CL; <span class="comment">/* 指向自己 */</span></span><br><span class="line"></span><br><span class="line">    LNode *tail = CL;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> x;</span><br><span class="line">        <span class="built_in">scanf_s</span>(<span class="string">&quot;%d&quot;</span>, &amp;x);</span><br><span class="line"></span><br><span class="line">        LNode *insNode = (LNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line">        insNode-&gt;data = x;</span><br><span class="line"></span><br><span class="line">        insNode-&gt;next = tail-&gt;next;</span><br><span class="line">        tail-&gt;next = insNode;</span><br><span class="line">        tail = insNode;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> CL;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>循环单链表的尾插法；</p></li><li><p>循环双链表</p><ul><li>头结点的prior需要指向尾结点，尾结点的next需要指向头结点。</li></ul><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line">CL = (DLinklist)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(DNode) * n);        </span><br><span class="line">CL-&gt;next = CL;</span><br><span class="line">CL-&gt;prior = CL;</span><br></pre></td></tr></table></figure></li></ul><h3 id="静态链表">静态链表</h3><p>借助数组来描述线性表的链式存储。</p><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">define</span> MaxSize 50</span></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span>&#123;</span></span><br><span class="line">ElemType data;</span><br><span class="line">    <span class="keyword">int</span> next;<span class="comment">// 下一个元素的数组下标</span></span><br><span class="line">&#125; SLinklist[Maxsize];</span><br></pre></td></tr></table></figure><h2 id="顺序表和链表的比较">顺序表和链表的比较</h2><h3 id="性质比较">性质比较</h3><ul><li>存取方式<ul><li>顺序表：可以顺序存放，<strong>随机存取</strong>，仅需<strong>一次</strong>访问</li><li>链表：从表头依次存取，需要<strong>依次</strong>访问</li></ul></li><li>逻辑结构与物理结构<ul><li>顺序表：<strong>逻辑相邻</strong>的元素<strong>物理相邻</strong></li><li>链表：<strong>逻辑相邻</strong>的元素<strong>不需要物理相邻</strong></li></ul></li><li>查找、插入和删除操作<ul><li>查找：<ul><li>按值查找：<ul><li>顺序表：<strong>无序</strong>顺序表时间复杂度为 $O(n)$ ，有序顺序表采取<strong>折半法</strong>时间复杂度为 $O(log_2n)$ 。</li><li>链表：时间复杂度为 $O(n)$ 。</li></ul></li><li>按位置查找：<ul><li>顺序表：时间复杂度为 $O(1)$ 。</li><li>链表：时间复杂度为 $O(n)$ 。</li></ul></li></ul></li><li>插入和删除：<ul><li>顺序表：平均需要移动半个表长的元素，所以最坏时间复杂度为：$O(n)$ 。</li><li>链表：时间复杂度为 $O(1)$ 。</li></ul></li></ul></li><li>空间分配<ul><li>顺序表<ul><li>静态分配：满了不能扩充，分配多了浪费空间</li><li>动态分配：可以扩充，但是需要移动大量元素，如果没有连续空间分配将失败</li></ul></li><li>链表：只要有内存空间即可灵活分配。</li></ul></li></ul><h3 id="如何选取">如何选取</h3><ul><li>基于存储考虑：难以估计长度和存储规模的时候，不要用顺序表，但是链式存储存储密度低</li><li>基于运算考虑：插入删除频繁时用链表，访问频繁时用顺序表</li><li>基于环境考虑：顺序表容易实现，链表操作基于指针。</li></ul><h2 id="课后题-v4">课后题</h2><figure class="highlight cpp"><table><tr><td class="code"><pre><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;stdio.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;malloc.h&gt;</span></span></span><br><span class="line"><span class="meta">#<span class="meta-keyword">include</span> <span class="meta-string">&lt;string.h&gt;</span></span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="keyword">int</span> ElemType;</span><br><span class="line"><span class="comment">// typedef char ElemType;</span></span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LNode</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="comment">/* data */</span></span><br><span class="line">    ElemType data;</span><br><span class="line">    LNode *next;</span><br><span class="line">&#125; LNode, *Linklist;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">DNode</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    <span class="comment">/* data */</span></span><br><span class="line">    ElemType data;</span><br><span class="line">    DNode *next;</span><br><span class="line">    DNode *prior;</span><br><span class="line">&#125; DNode, *DLinklist;</span><br><span class="line"></span><br><span class="line"><span class="keyword">typedef</span> <span class="class"><span class="keyword">struct</span> <span class="title">LNode_Q20</span></span></span><br><span class="line"><span class="class">&#123;</span></span><br><span class="line">    ElemType data;</span><br><span class="line">    <span class="keyword">int</span> freq;</span><br><span class="line">    LNode_Q20 *next;</span><br><span class="line">    LNode_Q20 *prior;</span><br><span class="line">&#125; LNode_Q20, *Linklist_Q20;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 打印不带头结点的单链表 */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">PrintLinklistWithoutHead</span><span class="params">(Linklist L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (L)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>, L-&gt;data);</span><br><span class="line">        L = L-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 打印带头结点的单链表 */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">PrintLinklist</span><span class="params">(Linklist L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (L-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        L = L-&gt;next;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>, L-&gt;data);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 打印带头结点的循环双链表 */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">PrintCDLinklist</span><span class="params">(DLinklist CDL)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    DNode *curNode = CDL;</span><br><span class="line">    <span class="keyword">while</span> (curNode-&gt;next != CDL)</span><br><span class="line">    &#123;</span><br><span class="line">        curNode = curNode-&gt;next;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>, curNode-&gt;data);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">// for (int i = 0; i &lt; n; i++)</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     CDL = CDL-&gt;next;</span></span><br><span class="line">    <span class="comment">//     printf(&quot;%d &quot;, CDL-&gt;data);</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 打印带头结点的循环单链表 */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">PrintCLinklist</span><span class="params">(Linklist L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    LNode *curNode = L;</span><br><span class="line">    <span class="keyword">while</span> (curNode-&gt;next != L)</span><br><span class="line">    &#123;</span><br><span class="line">        curNode = curNode-&gt;next;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>, curNode-&gt;data);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 打印带有 Freq 域的双链表 */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">PrintFreqDLinklist</span><span class="params">(Linklist_Q20 FDL)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">while</span> (FDL-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        FDL = FDL-&gt;next;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>, FDL-&gt;data);</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d\n&quot;</span>, FDL-&gt;freq);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;\n&quot;</span>);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 计算带头结点的单链表的表长 */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">GetLinklistLength</span><span class="params">(Linklist L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="keyword">int</span> len = <span class="number">0</span>;</span><br><span class="line">    LNode *curNode = L;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (curNode)</span><br><span class="line">    &#123;</span><br><span class="line">        curNode = curNode-&gt;next;</span><br><span class="line">        len++;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 最后一个NULL也会计入 */</span></span><br><span class="line">    <span class="keyword">return</span> len - <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 创建表长为 n 的不带头结点的单链表 */</span></span><br><span class="line"><span class="function">Linklist <span class="title">InsertDataFromTailwithoutHead</span><span class="params">(Linklist &amp;L, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    L = (Linklist)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode) * n);</span><br><span class="line">    L-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 处理第一个元素 */</span></span><br><span class="line">    <span class="keyword">int</span> x;</span><br><span class="line">    <span class="built_in">scanf_s</span>(<span class="string">&quot;%d&quot;</span>, &amp;x);</span><br><span class="line">    L-&gt;data = x;</span><br><span class="line"></span><br><span class="line">    LNode *tail = L;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 处理剩下的 n-1 个元素 */</span></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">1</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">scanf_s</span>(<span class="string">&quot;%d&quot;</span>, &amp;x);</span><br><span class="line"></span><br><span class="line">        LNode *p = (LNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line">        p-&gt;data = x;</span><br><span class="line"></span><br><span class="line">        p-&gt;next = tail-&gt;next;</span><br><span class="line">        tail-&gt;next = p;</span><br><span class="line">        tail = p;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 创建表长为 n 的带头结点的单链表 */</span></span><br><span class="line"><span class="function">Linklist <span class="title">InsertDataFromTail</span><span class="params">(Linklist &amp;L, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    L = (Linklist)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode) * n);</span><br><span class="line">    L-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    LNode *tail = L;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        ElemType x;</span><br><span class="line">        <span class="built_in">scanf_s</span>(<span class="string">&quot;%d&quot;</span>, &amp;x);</span><br><span class="line">        <span class="comment">// getchar();</span></span><br><span class="line"></span><br><span class="line">        LNode *p = (LNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line">        p-&gt;data = x;</span><br><span class="line"></span><br><span class="line">        p-&gt;next = tail-&gt;next;</span><br><span class="line">        tail-&gt;next = p;</span><br><span class="line">        tail = p;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 创建表长为 n 的带头结点的循环双链表 */</span></span><br><span class="line"><span class="function">DLinklist <span class="title">InsertDataFromTail_Circle</span><span class="params">(DLinklist &amp;CDL, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    CDL = (DLinklist)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(DNode) * n);</span><br><span class="line">    CDL-&gt;next = CDL;</span><br><span class="line">    CDL-&gt;prior = CDL;</span><br><span class="line">    DNode *tail = CDL;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> x;</span><br><span class="line">        <span class="built_in">scanf_s</span>(<span class="string">&quot;%d&quot;</span>, &amp;x);</span><br><span class="line"></span><br><span class="line">        DNode *insNode = (DNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(insNode));</span><br><span class="line">        insNode-&gt;data = x;</span><br><span class="line"></span><br><span class="line">        insNode-&gt;next = tail-&gt;next;</span><br><span class="line">        insNode-&gt;prior = tail;</span><br><span class="line">        tail-&gt;next = insNode;</span><br><span class="line">        tail = insNode;</span><br><span class="line">    &#125;</span><br><span class="line">    CDL-&gt;prior = tail;</span><br><span class="line">    <span class="keyword">return</span> CDL;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 创建表长为 n 的带头结点的循环单链表 */</span></span><br><span class="line"><span class="function">Linklist <span class="title">InsertDataFromTail_Circle</span><span class="params">(Linklist &amp;CL, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    CL = (Linklist)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode) * n);</span><br><span class="line">    CL-&gt;next = CL;</span><br><span class="line">    LNode *tail = CL;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> x;</span><br><span class="line">        <span class="built_in">scanf_s</span>(<span class="string">&quot;%d&quot;</span>, &amp;x);</span><br><span class="line"></span><br><span class="line">        LNode *insNode = (LNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line">        insNode-&gt;data = x;</span><br><span class="line"></span><br><span class="line">        insNode-&gt;next = tail-&gt;next;</span><br><span class="line">        tail-&gt;next = insNode;</span><br><span class="line">        tail = insNode;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> CL;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* 创建表长为 n 的带头结点的双链表，且链表带有访问频度域 freq */</span></span><br><span class="line"><span class="function">Linklist_Q20 <span class="title">InsertDataFromTailwithFreq</span><span class="params">(Linklist_Q20 &amp;FDL, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    FDL = (Linklist_Q20)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode_Q20) * n);</span><br><span class="line">    FDL-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">    FDL-&gt;prior = <span class="literal">NULL</span>;</span><br><span class="line">    FDL-&gt;freq = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">    LNode_Q20 *tail = FDL;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">for</span> (<span class="keyword">int</span> i = <span class="number">0</span>; i &lt; n; i++)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">int</span> x;</span><br><span class="line">        <span class="built_in">scanf_s</span>(<span class="string">&quot;%d&quot;</span>, &amp;x);</span><br><span class="line"></span><br><span class="line">        LNode_Q20 *insNode = (LNode_Q20 *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode_Q20));</span><br><span class="line">        insNode-&gt;data = x;</span><br><span class="line">        insNode-&gt;freq = <span class="number">0</span>;</span><br><span class="line"></span><br><span class="line">        insNode-&gt;next = tail-&gt;next;</span><br><span class="line">        insNode-&gt;prior = tail;</span><br><span class="line">        tail-&gt;next = insNode;</span><br><span class="line">        tail = insNode;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> FDL;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q1. 设计一个递归算法，删除不带头结点的单链表 L 中所有值为 x 的结点 */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">DeleteAllxNodeWithoutHead_Q1</span><span class="params">(Linklist &amp;L, <span class="keyword">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    LNode *p;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (L == <span class="literal">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">if</span> (L-&gt;data == x)</span><br><span class="line">    &#123;</span><br><span class="line">        p = L;                              <span class="comment">/* 结点 p 继承 data=x 的结点 */</span></span><br><span class="line">        L = L-&gt;next;                        <span class="comment">/* 将当前结点覆盖为下一结点 */</span></span><br><span class="line">        <span class="built_in">free</span>(p);                            <span class="comment">/* 释放 data=x 的结点 */</span></span><br><span class="line">        <span class="built_in">DeleteAllxNodeWithoutHead_Q1</span>(L, x); <span class="comment">/* 递归调用 */</span></span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">DeleteAllxNodeWithoutHead_Q1</span>(L-&gt;next, x); <span class="comment">/* 如果当前结点的data!=x，则判断下一个结点 L-&gt;next */</span></span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q2.在带头结点的单链表L中，删除所有值为x的结点，并释放空间  */</span></span><br><span class="line"><span class="comment">/* 解法一：引入一个 LNode *preNode 指针来指向每次判断结点的前驱结点，若 p-&gt;data == x，则借助 preNode 删除该结点，否则就同步向后移动一位 */</span></span><br><span class="line"><span class="comment">/* 解法二：p 遍历结点，当 p-&gt;data != x时，将该结点链接到 L 的尾部 */</span></span><br><span class="line"><span class="comment">/* 解法三：递归判断每一个结点，p-&gt;data == x 则在原表 L 上将该结点断链，否则递归判断下一个结点 */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">DeleteAllxNode_Q2</span><span class="params">(Linklist &amp;L, <span class="keyword">int</span> x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    LNode *p;</span><br><span class="line">    <span class="keyword">if</span> (L == <span class="literal">NULL</span>)</span><br><span class="line">        <span class="keyword">return</span>;</span><br><span class="line">    <span class="keyword">if</span> (L-&gt;data == x)</span><br><span class="line">    &#123;</span><br><span class="line">        p = L;</span><br><span class="line">        L = L-&gt;next;</span><br><span class="line">        <span class="built_in">free</span>(p);</span><br><span class="line">        <span class="built_in">DeleteAllxNode_Q2</span>(L, x);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">DeleteAllxNode_Q2</span>(L-&gt;next, x);</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q3. 设 L 为带头结点的单链表，从尾到头反向输出每个结点的值 */</span></span><br><span class="line"><span class="comment">/* 解法一:遍历链表，每个结点都用头插法插入到 L 中即可实现逆序 */</span></span><br><span class="line"><span class="comment">/* 解法二：栈 */</span></span><br><span class="line"><span class="function">Linklist <span class="title">ReverseLinklist_Q3</span><span class="params">(Linklist &amp;L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    LNode *p = L-&gt;next; <span class="comment">/* 工作指针 */</span></span><br><span class="line">    L-&gt;next = <span class="literal">NULL</span>;     <span class="comment">/* 用 p 继承L-&gt;next后，将L-&gt;next置空，方便进行头插法 */</span></span><br><span class="line">    LNode *auxiliary;   <span class="comment">/* 辅助指针，为工作指针的后继，防止断链 */</span></span><br><span class="line">    <span class="comment">/* 第一个元素不用动，将后面的元素用头插法插入head结点后 */</span></span><br><span class="line">    <span class="keyword">while</span> (p)</span><br><span class="line">    &#123;</span><br><span class="line">        auxiliary = p-&gt;next; <span class="comment">/* 保存p-&gt;next，防止先前的完整链断掉 */</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 进行头插法 */</span></span><br><span class="line">        p-&gt;next = L-&gt;next;</span><br><span class="line">        L-&gt;next = p;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 将 p 恢复为先前完整链的下一个结点 */</span></span><br><span class="line">        p = auxiliary;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q4. 在带头结点的单链表 L 中删除一个最小值结点，最小值结点唯一。 */</span></span><br><span class="line"><span class="function">Linklist <span class="title">DeleteMinNode_Q4</span><span class="params">(Linklist &amp;L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 碰到需要删除结点的，一般需要引入一个指向删除结点的前驱结点的指针 minPre，在这里还需要使用 minNode 来指向链表中的最小值结点，并用工作指针 curNode 进行遍历，每次访问结点 curNode-&gt;data 将和 minNode-&gt;data 进行比较，如果minNode-&gt;data &gt; curNode-&gt;data ，则对 minNode 进行替换，结束遍历后，使用 preNode 对结点进行删除 */</span></span><br><span class="line"></span><br><span class="line">    LNode *preNode = L;             <span class="comment">/* 工作指针的前驱结点 */</span></span><br><span class="line">    LNode *curNode = preNode-&gt;next; <span class="comment">/* 工作指针，用于遍历链表 */</span></span><br><span class="line">    LNode *minNode = L-&gt;next;       <span class="comment">/* minNode 指向第一个元素 */</span></span><br><span class="line">    LNode *minPre = preNode;        <span class="comment">/* minNode 的前驱，用于删除 minNode */</span></span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (curNode)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 如果结点的 data 比 所保存的最小值更小，则更新 */</span></span><br><span class="line">        <span class="keyword">if</span> (curNode-&gt;data &lt; minNode-&gt;data)</span><br><span class="line">        &#123;</span><br><span class="line">            minNode = curNode; <span class="comment">/* 将 minNode 指向当前结点 */</span></span><br><span class="line">            minPre = preNode;  <span class="comment">/* 将 minPre 指向当前结点的前驱 */</span></span><br><span class="line">        &#125;</span><br><span class="line">        preNode = curNode; <span class="comment">/* 继续遍历 */</span></span><br><span class="line">        curNode = curNode-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 遍历完后， minNode 指向最小值结点，minPre 指向其前驱结点，执行删除操作即可 */</span></span><br><span class="line">    minPre-&gt;next = minNode-&gt;next;</span><br><span class="line">    <span class="built_in">free</span>(minNode);</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q5. 将带头结点的单链表就地（即空间复杂度为O(1)）逆置 */</span></span><br><span class="line"><span class="function">Linklist <span class="title">ReverseLinklist_Q5</span><span class="params">(Linklist &amp;L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 这一题和第三题一致 */</span></span><br><span class="line"></span><br><span class="line">    LNode *p = L-&gt;next; <span class="comment">/* p 指向第一个元素 */</span></span><br><span class="line">    L-&gt;next = <span class="literal">NULL</span>;     <span class="comment">/* p 继承了 L-&gt;next 后，将 L-&gt;next 置空，方便头插法 */</span></span><br><span class="line">    LNode *auxiliary;   <span class="comment">/* 防止断链 */</span></span><br><span class="line">    <span class="keyword">while</span> (p)</span><br><span class="line">    &#123;</span><br><span class="line">        auxiliary = p-&gt;next;</span><br><span class="line"></span><br><span class="line">        p-&gt;next = L-&gt;next;</span><br><span class="line">        L-&gt;next = p;</span><br><span class="line"></span><br><span class="line">        p = auxiliary;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q6. 使带头结点的单链表 L 内元素 data 域有序递增 */</span></span><br><span class="line"><span class="function">Linklist <span class="title">OrderlyIncreasing_Q6</span><span class="params">(Linklist &amp;L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 有两种解法：</span></span><br><span class="line"><span class="comment">    1. 将 L 拆分成两个链表，第一个链表用于存放最终结果，初始化为只含有一个结点的链表，遍历第二个链表（剩下的结点），按照大小在合适的位置进行插入。显然这种方法的时间复杂度为 O(n^2)</span></span><br><span class="line"><span class="comment">    2. 先将链表元素数据存放到数组进行排序，排序完成后再插入数组中，这种方法的时间复杂度可以达到 O(nlog2n) */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 解法一 */</span></span><br><span class="line">    LNode *curNode = L-&gt;next;         <span class="comment">/* 指向第一个元素 */</span></span><br><span class="line">    LNode *preNode;                   <span class="comment">/* curNode 的前驱结点 */</span></span><br><span class="line">    LNode *auxiliary = curNode-&gt;next; <span class="comment">/* curNode 的后继结点 */</span></span><br><span class="line"></span><br><span class="line">    curNode-&gt;next = <span class="literal">NULL</span>; <span class="comment">/* 第一个链表只有 curNode 一个结点 */</span></span><br><span class="line"></span><br><span class="line">    curNode = auxiliary;</span><br><span class="line">    <span class="keyword">while</span> (curNode)</span><br><span class="line">    &#123;</span><br><span class="line">        auxiliary = curNode-&gt;next;</span><br><span class="line">        preNode = L;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 如果最终链表当前结点的 data 小于 比较链表当前结点的 data ，则向后继续遍历，直到找到要插入位置，得到其前驱 preNode */</span></span><br><span class="line">        <span class="keyword">while</span> (preNode-&gt;next &amp;&amp; preNode-&gt;next-&gt;data &lt; curNode-&gt;data)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">/* code */</span></span><br><span class="line">            preNode = preNode-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 插入 */</span></span><br><span class="line">        curNode-&gt;next = preNode-&gt;next;</span><br><span class="line">        preNode-&gt;next = curNode;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* curNode = curNode-&gt;next ，比较链表向后遍历 */</span></span><br><span class="line">        curNode = auxiliary;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">// todo 解法二</span></span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q7. 带头结点的单链表中，所有元素结点的数据值无序，删除表中所有介于给定的两个值之间的元素 */</span></span><br><span class="line"><span class="function">Linklist <span class="title">DeleteNodeBetweenAB_Q7</span><span class="params">(Linklist &amp;L, <span class="keyword">int</span> a, <span class="keyword">int</span> b)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    LNode *preNode = L;</span><br><span class="line">    LNode *curNode = preNode-&gt;next;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (curNode)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (curNode-&gt;data &gt;= a &amp;&amp; curNode-&gt;data &lt;= b)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">/* 如果需要删除当前结点 */</span></span><br><span class="line">            preNode-&gt;next = curNode-&gt;next;</span><br><span class="line">            <span class="built_in">free</span>(curNode);</span><br><span class="line">            curNode = preNode-&gt;next; <span class="comment">/* 使得 curNode 保持为 preNode 的后继结点 */</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">/* 前驱指针指向当前结点，当前结点指向下一结点 */</span></span><br><span class="line">            preNode = curNode;</span><br><span class="line">            curNode = preNode-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q8. 找出两个链表中的公共结点 */</span></span><br><span class="line"><span class="function">Linklist <span class="title">FindPublicNode_Q8</span><span class="params">(Linklist L1, Linklist L2)</span></span></span><br><span class="line"><span class="function"></span>&#123; <span class="comment">/*</span></span><br><span class="line"><span class="comment">    公共结点的意思并不是 data 域相等就可以了，而是指向了同一个结点，而两个链表如果有公共结点，在公共结点后两个链表不可能在有分叉，因为同一个结点不可能有两个不一样的 next 指针，所以两个带有公共结点的链表的拓扑结构为 Y 型。</span></span><br><span class="line"><span class="comment">    由于两个链表长度不一定一致，所以要计算表长差值，这样才能同步找到第一个公共结点。第一个公共结点最好出现在较短链表的第一个结点，最差出现在最后一个结点。</span></span><br><span class="line"><span class="comment"> */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 先计算表长 */</span></span><br><span class="line">    <span class="keyword">int</span> gap;</span><br><span class="line">    <span class="keyword">int</span> len1 = <span class="built_in">GetLinklistLength</span>(L1);</span><br><span class="line">    <span class="keyword">int</span> len2 = <span class="built_in">GetLinklistLength</span>(L2);</span><br><span class="line">    <span class="comment">// printf(&quot;%d\n&quot;, len1);</span></span><br><span class="line">    <span class="comment">// printf(&quot;%d\n&quot;, len2);</span></span><br><span class="line">    Linklist Longerlist, Shorterlist;</span><br><span class="line">    <span class="keyword">if</span> (len1 &gt; len2)</span><br><span class="line">    &#123;</span><br><span class="line">        Longerlist = L1-&gt;next;</span><br><span class="line">        Shorterlist = L2-&gt;next;</span><br><span class="line">        gap = len1 - len2;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        Longerlist = L2-&gt;next;</span><br><span class="line">        Shorterlist = L1-&gt;next;</span><br><span class="line">        gap = len2 - len1;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 把较长的链表调整到和较短表相同的位置 */</span></span><br><span class="line">    <span class="keyword">while</span> (gap--)</span><br><span class="line">    &#123;</span><br><span class="line">        Longerlist = Longerlist-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 比较判断 */</span></span><br><span class="line">    <span class="keyword">while</span> (Shorterlist &amp;&amp; Longerlist)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (Shorterlist == Longerlist)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="keyword">return</span> Shorterlist;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            Shorterlist = Shorterlist-&gt;next;</span><br><span class="line">            Longerlist = Longerlist-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q9. 带头结点的单链表，不借助数组辅助空间，按递增次序输出单链表各个结点的 data 域 。并释放结点所占的存储空间 */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">OutputOrderlyIncreasing_Q9</span><span class="params">(Linklist &amp;L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    LNode *orderNode;</span><br><span class="line">    LNode *curNode = L-&gt;next;         <span class="comment">/* 实际上是第一个结点 */</span></span><br><span class="line">    LNode *auxiliary = curNode-&gt;next; <span class="comment">/* 实际上是第二个结点 或 NULL */</span></span><br><span class="line"></span><br><span class="line">    curNode-&gt;next = <span class="literal">NULL</span>; <span class="comment">/* 断成两条链，一条排序链（带头结点），一条比较链（无头结点） */</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* curNode 回到比较链，第二个结点 */</span></span><br><span class="line">    curNode = auxiliary;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 排序 */</span></span><br><span class="line">    <span class="keyword">while</span> (curNode)</span><br><span class="line">    &#123;</span><br><span class="line">        auxiliary = curNode-&gt;next; <span class="comment">/* 防止断链 */</span></span><br><span class="line">        orderNode = L;             <span class="comment">/* 排序链的头结点 */</span></span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 固定比较链上的结点，遍历排序链。如果排序链上的数据小于比较链上的数据 */</span></span><br><span class="line">        <span class="keyword">while</span> (orderNode-&gt;next &amp;&amp; orderNode-&gt;next-&gt;data &lt; curNode-&gt;data)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">/* 遍历寻找，直到找到合适的插入位置 */</span></span><br><span class="line">            orderNode = orderNode-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 找到合适位置插入 */</span></span><br><span class="line">        curNode-&gt;next = orderNode-&gt;next;</span><br><span class="line">        orderNode-&gt;next = curNode;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 排序链下一个结点 */</span></span><br><span class="line">        curNode = auxiliary;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 排序完成后，输出并释放空间 */</span></span><br><span class="line">    curNode = L-&gt;next;</span><br><span class="line">    <span class="keyword">while</span> (curNode)</span><br><span class="line">    &#123;</span><br><span class="line">        auxiliary = curNode-&gt;next;</span><br><span class="line"></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>, curNode-&gt;data);</span><br><span class="line">        <span class="built_in">free</span>(curNode);</span><br><span class="line">        curNode = auxiliary;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q10. 带头结点单链表 A 分解为两个带头结点的单链表 A 和 B ，使得 A 表中含有原表序号为奇数的元素，而 B 表中含有原表中序号为偶数的元素，保持其顺序不变 */</span></span><br><span class="line"><span class="function">Linklist <span class="title">SeperateOddEven_Q10</span><span class="params">(Linklist &amp;L1)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 定义两个指针，preNode指向原链表 L1 的头指针，curNode为其后的指针，curNode 永远指向奇数结点，preNode 永远指向偶数结点 */</span></span><br><span class="line">    LNode *preNode = L1;</span><br><span class="line">    LNode *curNode = preNode-&gt;next;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 新链的头结点，指向 curNode，如果是空表 head-&gt;next 将指向 NULL */</span></span><br><span class="line">    LNode *head = (LNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line">    head-&gt;next = curNode;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 只要 curNode 存在 */</span></span><br><span class="line">    <span class="keyword">while</span> (curNode)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 如果 curNode-&gt;next 存在 */</span></span><br><span class="line">        <span class="keyword">if</span> (curNode-&gt;next)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">/* 前驱结点的 next 越过 curNode 指向 curNode-&gt;next */</span></span><br><span class="line">            preNode-&gt;next = curNode-&gt;next;</span><br><span class="line"></span><br><span class="line">            <span class="comment">/* 对 curNode 的 next 指针进行更行，越过 preNode-&gt;next 指向了 下下个结点，下下个结点可能是 NULL，那么两条链最后都会指向 NULL，因此不用再处理 */</span></span><br><span class="line">            curNode-&gt;next = curNode-&gt;next-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">/* 如果 curNode-&gt;next 不存在， 说明 curNode 已经是最后一个结点 或者 已经是 NULL 了，那么 curNode-&gt;next-&gt;next 是不存在的，访问会 Segment Fault */</span></span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">/* 经过下列分析，可以得出，当curNode-&gt;next不存在时，可以直接置空 preNode-&gt;next 即可 */</span></span><br><span class="line">            preNode-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">            <span class="comment">// /* 如果 curNode 是最后一个结点，那么其前驱结点 preNode 的 next 指针需要单独处理，指向 NULL */</span></span><br><span class="line">            <span class="comment">// if (curNode)</span></span><br><span class="line">            <span class="comment">//     preNode-&gt;next = curNode-&gt;next;</span></span><br><span class="line"></span><br><span class="line">            <span class="comment">// /* 如果 curNode 是 NULL，那么也需要处理 preNode */</span></span><br><span class="line">            <span class="comment">// else</span></span><br><span class="line">            <span class="comment">//     preNode-&gt;next = NULL;</span></span><br><span class="line">            <span class="keyword">return</span> head;</span><br><span class="line">        &#125;</span><br><span class="line">        curNode = curNode-&gt;next;</span><br><span class="line">        preNode = preNode-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> head;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q11. 设 C=&#123;a1,b1,a2,b2,...,an,bn&#125;为线性表，次啊用嗲头结点的 hc 单链表存放，设计就地算法，将其拆为两个线性表，使得 A=&#123;a1,a2,a3,...,an&#125;，B=&#123;b1,b2,b3,...,bn&#125; */</span></span><br><span class="line"><span class="function">Linklist <span class="title">SeperateOddEven_Q11</span><span class="params">(Linklist &amp;L1)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 偶数表的头结点兼工作指针 */</span></span><br><span class="line">    LNode *preNode = L1;</span><br><span class="line">    <span class="comment">/* 奇数表的第一个结点兼工作指针 */</span></span><br><span class="line">    LNode *curNode = preNode-&gt;next;</span><br><span class="line">    <span class="comment">/* 奇数表的头结点 */</span></span><br><span class="line">    LNode *head = (LNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line">    head-&gt;next = curNode;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 如果 curNode 存在，根据循环内的操作分析两种情况：</span></span><br><span class="line"><span class="comment">    1. curNode 有后继结点，curNode-&gt;next 存在，curNode-&gt;next-&gt;next 可能为 NULL</span></span><br><span class="line"><span class="comment">    2. curNode 没有后继结点，为表尾结点，curNode-&gt;next = NULL ，那么 curNode-&gt;next-&gt;next 不存在，也是NULL */</span></span><br><span class="line">    <span class="keyword">while</span> (curNode)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 1. 情况1：curNode 存在后继结点 */</span></span><br><span class="line">        <span class="keyword">if</span> (curNode-&gt;next)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">/* 通过奇数结点的 next 指针，将偶数结点插入到偶数表尾部 */</span></span><br><span class="line">            preNode-&gt;next = curNode-&gt;next;</span><br><span class="line"></span><br><span class="line">            <span class="comment">/* 越过偶数结点，next 指针指向下一个偶数结点，根据分析：curNode-&gt;next-&gt;next 可能为 NULL ，即 curNode-&gt;next 可能指向空指针 */</span></span><br><span class="line">            curNode-&gt;next = curNode-&gt;next-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">/* 2. 情况2：curNode 没有后继结点，为表尾结点 */</span></span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">/* 只需要处理偶数表的尾指针指向 NULL 即可 */</span></span><br><span class="line">            preNode-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">            <span class="keyword">return</span> head;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 各表向后遍历 */</span></span><br><span class="line">        preNode = preNode-&gt;next;</span><br><span class="line">        curNode = curNode-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> head;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q12. 在递增有序的线性表中，有数值相同的元素存在，若存储方式为单链表，设计算法去掉数值相同的元素，并保持有序状态 */</span></span><br><span class="line"><span class="function">Linklist <span class="title">DeleteSameNode_Q12</span><span class="params">(Linklist &amp;L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 由于是递增有序的线性表，因此 data 域相等的结点一定相邻，可以设置 curNode 和它的前驱结点 preNode ， preNode 指向每一个值不同的结点，curNode向后遍历，直到碰到值不一样的结点，再挪动 preNode 和 curNode */</span></span><br><span class="line">    LNode *preNode = L-&gt;next;</span><br><span class="line">    LNode *curNode = preNode-&gt;next;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (curNode)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 如果 curNode 存在（因为 curNode 可能会为 NULL），并且两结点 data 域相同 */</span></span><br><span class="line">        <span class="keyword">while</span> (curNode &amp;&amp; preNode-&gt;data == curNode-&gt;data)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">/* curNode-&gt;next 可能是 NULL，即 curNode可能被赋值为 NULL */</span></span><br><span class="line">            curNode = curNode-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 更新 */</span></span><br><span class="line">        preNode-&gt;next = curNode;</span><br><span class="line">        preNode = curNode;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 如果 curNode 存在，且 curNode-&gt;next 也存在， 则向后更新 */</span></span><br><span class="line">        <span class="keyword">if</span> (curNode &amp;&amp; curNode-&gt;next)</span><br><span class="line">        &#123;</span><br><span class="line">            curNode = curNode-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q13. 递增有序的两个单链表，归并为一个递减有序的单链表，要求利用原来两个单链表的结点存放归并后的单链表 */</span></span><br><span class="line"><span class="function">Linklist <span class="title">CombineOrderlyDecreasing_Q13</span><span class="params">(Linklist &amp;L1, Linklist &amp;L2)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    LNode *curNode1 = L1-&gt;next;</span><br><span class="line">    LNode *auxiliary1 = curNode1-&gt;next;</span><br><span class="line"></span><br><span class="line">    LNode *curNode2 = L2-&gt;next;</span><br><span class="line">    LNode *auxiliary2 = curNode2-&gt;next;</span><br><span class="line"></span><br><span class="line">    LNode *head = (LNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line">    head-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 遍历完较短的表，将 data 域较小的结点用头插法插入 head-&gt;next */</span></span><br><span class="line">    <span class="keyword">while</span> (curNode1 &amp;&amp; curNode2)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 如果要用 curNode-&gt;data，那么要注意 curNode 不能为 NULL */</span></span><br><span class="line">        <span class="keyword">if</span> (curNode1-&gt;data &lt; curNode2-&gt;data)</span><br><span class="line">        &#123;</span><br><span class="line">            curNode1-&gt;next = head-&gt;next;</span><br><span class="line">            head-&gt;next = curNode1;</span><br><span class="line">            curNode1 = auxiliary1;</span><br><span class="line">            <span class="keyword">if</span> (curNode1)</span><br><span class="line">                auxiliary1 = curNode1-&gt;next;</span><br><span class="line">            <span class="comment">/* 执行结束后，auxiliary1 可能是 NULL，即curNode1 为链尾结点 */</span></span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            curNode2-&gt;next = head-&gt;next;</span><br><span class="line">            head-&gt;next = curNode2;</span><br><span class="line">            curNode2 = auxiliary2;</span><br><span class="line">            <span class="keyword">if</span> (curNode2)</span><br><span class="line">                auxiliary2 = curNode2-&gt;next;</span><br><span class="line">            <span class="comment">/* 执行结束后，auxiliary2 可能是 NULL，即curNode2 为链尾结点 */</span></span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 将较长链表剩下的结点用头插法插入链表 */</span></span><br><span class="line">    <span class="keyword">while</span> (curNode1)</span><br><span class="line">    &#123;</span><br><span class="line">        curNode1-&gt;next = head-&gt;next;</span><br><span class="line">        head-&gt;next = curNode1;</span><br><span class="line">        curNode1 = auxiliary1;</span><br><span class="line">        <span class="keyword">if</span> (curNode1)</span><br><span class="line">            auxiliary1 = curNode1-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">while</span> (curNode2)</span><br><span class="line">    &#123;</span><br><span class="line">        curNode2-&gt;next = head-&gt;next;</span><br><span class="line">        head-&gt;next = curNode2;</span><br><span class="line">        curNode2 = auxiliary2;</span><br><span class="line">        <span class="keyword">if</span> (curNode2)</span><br><span class="line">            auxiliary2 = curNode2-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> head;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q14. A, B 两个带头结点的单链表有序递增，设计算法：从 A 和 B 中的公共元素产生单链表 C，要求不破坏 A、B 的结点 */</span></span><br><span class="line"><span class="function">Linklist <span class="title">FindOrderlyPublicNode_Q14</span><span class="params">(Linklist L1, Linklist L2)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 注意观察题目，题目说明的是公共元素而不是公共结点，即 data 域相同时，将新建data域相等的结点，并用尾插法插入到新的链表中 */</span></span><br><span class="line">    LNode *curNode1 = L1-&gt;next;</span><br><span class="line">    LNode *curNode2 = L2-&gt;next;</span><br><span class="line"></span><br><span class="line">    LNode *head = (LNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line">    head-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">    LNode *tail = head;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (curNode1 &amp;&amp; curNode2)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (curNode1-&gt;data == curNode2-&gt;data)</span><br><span class="line">        &#123;</span><br><span class="line">            LNode *insNode = (LNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line">            insNode-&gt;data = curNode1-&gt;data;</span><br><span class="line">            insNode-&gt;next = tail-&gt;next;</span><br><span class="line">            tail-&gt;next = insNode;</span><br><span class="line">            tail = insNode;</span><br><span class="line"></span><br><span class="line">            curNode1 = curNode1-&gt;next;</span><br><span class="line">            curNode2 = curNode2-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (curNode1-&gt;data &lt; curNode2-&gt;data)</span><br><span class="line">            curNode1 = curNode1-&gt;next;</span><br><span class="line">        <span class="keyword">else</span> <span class="comment">// (curNode1-&gt;data &gt; curNode2-&gt;data)</span></span><br><span class="line">            curNode2 = curNode2-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> head;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q15. 链表 A、B 分别代表两个集合，元素递增有序，求 A 和 B 的交集，存放在 A 链表中 */</span></span><br><span class="line"><span class="function">Linklist <span class="title">FindOrderlyPublicNode_Q15</span><span class="params">(Linklist &amp;L1, Linklist &amp;L2)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 两个链表有序递增，交集需要满足：</span></span><br><span class="line"><span class="comment">    1. 位序相等</span></span><br><span class="line"><span class="comment">    2. data 域相等</span></span><br><span class="line"><span class="comment">     */</span></span><br><span class="line">    LNode *curNode1 = L1-&gt;next;</span><br><span class="line">    LNode *curNode2 = L2-&gt;next;</span><br><span class="line">    LNode *tail = L1;</span><br><span class="line">    LNode *delNode = (LNode *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(LNode));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (curNode1 &amp;&amp; curNode2)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 如果两个结点的 data 域相等，将结点插入 L1 */</span></span><br><span class="line">        <span class="keyword">if</span> (curNode1-&gt;data == curNode2-&gt;data)</span><br><span class="line">        &#123;</span><br><span class="line">            tail-&gt;next = curNode1;</span><br><span class="line">            tail = curNode1;</span><br><span class="line">            curNode1 = curNode1-&gt;next;</span><br><span class="line"></span><br><span class="line">            <span class="comment">/* 释放在 L2 的结点 */</span></span><br><span class="line">            delNode = curNode2;</span><br><span class="line">            curNode2 = curNode2-&gt;next;</span><br><span class="line">            <span class="built_in">free</span>(delNode);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">/* 如果 curNode1-&gt;data 小于 curNode2-&gt;data，curNode1 向后遍历，并且删掉不等于交集结点的结点 */</span></span><br><span class="line">        <span class="keyword">else</span> <span class="keyword">if</span> (curNode1-&gt;data &lt; curNode2-&gt;data)</span><br><span class="line">        &#123;</span><br><span class="line">            delNode = curNode1;</span><br><span class="line">            curNode1 = curNode1-&gt;next;</span><br><span class="line">            <span class="built_in">free</span>(delNode);</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">/* curNode2 同理 */</span></span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            delNode = curNode2;</span><br><span class="line">            curNode2 = curNode2-&gt;next;</span><br><span class="line">            <span class="built_in">free</span>(delNode);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/* 遍历完后，较长链剩下的结点释放掉 */</span></span><br><span class="line">    <span class="keyword">while</span> (curNode1)</span><br><span class="line">    &#123;</span><br><span class="line">        delNode = curNode1;</span><br><span class="line">        curNode1 = curNode1-&gt;next;</span><br><span class="line">        <span class="built_in">free</span>(delNode);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">while</span> (curNode2)</span><br><span class="line">    &#123;</span><br><span class="line">        delNode = curNode2;</span><br><span class="line">        curNode2 = curNode2-&gt;next;</span><br><span class="line">        <span class="built_in">free</span>(delNode);</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 链尾置空 */</span></span><br><span class="line">    tail-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">    <span class="built_in">free</span>(L2);</span><br><span class="line">    <span class="keyword">return</span> L1;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q16. 两个整数序列 A=a1,a2,...am 和 B=b1,b2,...,bn 已经存入两个单链表中，设计算法判断 B 是否是 A 的连续子序列 */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">isContinuousSubSequence_Q16</span><span class="params">(Linklist L1, Linklist L2)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 连续子序列，要注意判断的是 B ，所以应该从A上的每一个结点向后检索是否能和 B 匹配，如果 B 是 A 的连续子序列，则 B 一定会被遍历完，指针指向 NULL */</span></span><br><span class="line">    LNode *curNode1 = L1;</span><br><span class="line">    LNode *compareNode = curNode1;</span><br><span class="line">    LNode *curNode2 = L2;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (curNode1 &amp;&amp; curNode2)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* 两链表数据相等，同时向后遍历比较 */</span></span><br><span class="line">        <span class="keyword">if</span> (curNode1-&gt;data == curNode2-&gt;data)</span><br><span class="line">        &#123;</span><br><span class="line">            curNode1 = curNode1-&gt;next;</span><br><span class="line">            curNode2 = curNode2-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">/* 不相等，则 curNode2 重新从 L2 开头开始遍历，curNode1 则回到 L1 上一次开始比较的结点的下一个结点 */</span></span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            compareNode = compareNode-&gt;next;</span><br><span class="line">            curNode1 = compareNode;</span><br><span class="line">            curNode2 = L2;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 如果 curNode2 遍历完 L2，说明 B 是 A 的连续子序列 */</span></span><br><span class="line">        <span class="keyword">if</span> (curNode2 == <span class="literal">NULL</span>)</span><br><span class="line">            <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q17. 设计一个算法用于判断带头结点的循环双链表是否对称 */</span></span><br><span class="line"><span class="function"><span class="keyword">bool</span> <span class="title">isCDLinklistSymmetry_Q17</span><span class="params">(DLinklist CDL)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    DNode *nextNode = CDL-&gt;next;</span><br><span class="line">    DNode *priorNode = CDL-&gt;prior;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 遍历时会有如下情况：</span></span><br><span class="line"><span class="comment">    1. 当链表结点数量为奇数时，两个指针最终会指向同一个结点，并且一定是对称的</span></span><br><span class="line"><span class="comment">    2. 当链表结点数量为偶数时，两个指针最终会相邻，相邻时如果也是相等的，则也是对称的</span></span><br><span class="line"><span class="comment">    3. 其余为非终点或起点的普遍状况：如果 nextNode 和 priorNode 没有指向同一个结点，并且它们不相邻 */</span></span><br><span class="line">    <span class="keyword">while</span> (nextNode != priorNode &amp;&amp; priorNode-&gt;next != nextNode)</span><br><span class="line">    <span class="comment">/* 这里要注意 必须是 priorNode-&gt;next != nextNode ，不能是 nextNode-&gt;next != priorNode，否则当链表结点个数为偶数个时，最后相邻结点没有进行比较*/</span></span><br><span class="line">    &#123;</span><br><span class="line"></span><br><span class="line">        <span class="keyword">if</span> (nextNode-&gt;data == priorNode-&gt;data)</span><br><span class="line">        &#123;</span><br><span class="line">            nextNode = nextNode-&gt;next;</span><br><span class="line">            priorNode = priorNode-&gt;prior;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="keyword">return</span> <span class="literal">false</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/* 出了 while 循环后，一定是对称的 */</span></span><br><span class="line">    <span class="keyword">return</span> <span class="literal">true</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q18. 两个循环单链表，链表头指针分别为 h1 和 h2，将链表 h2 链接到链表 h1 之后，要求连接后的链表仍然保持循环链表的形式 */</span></span><br><span class="line"><span class="function">Linklist <span class="title">CombineCircleLinklist_Q18</span><span class="params">(Linklist &amp;CL1, Linklist &amp;CL2)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    LNode *h1 = CL1;</span><br><span class="line">    LNode *curNode1 = h1;</span><br><span class="line">    LNode *h2 = CL2;</span><br><span class="line">    LNode *curNode2 = h2;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 得到指向 CL1 尾结点的指针 */</span></span><br><span class="line">    <span class="keyword">while</span> (curNode1-&gt;next != h1)</span><br><span class="line">    &#123;</span><br><span class="line">        curNode1 = curNode1-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/* 链接上 h2 */</span></span><br><span class="line">    curNode1-&gt;next = h2;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 得到指向 CL2 尾结点的指针 */</span></span><br><span class="line">    <span class="keyword">while</span> (curNode2-&gt;next != h2)</span><br><span class="line">    &#123;</span><br><span class="line">        curNode2 = curNode2-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="comment">/* 链接回 h1 */</span></span><br><span class="line">    curNode2-&gt;next = h1;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> CL1;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q19. 带头结点的循环单链表，结点值都为正整数，反复找出单链表中结点值最小的结点并输出，然后将该结点删除，直到表空，最后删除表头结点 */</span></span><br><span class="line"><span class="function"><span class="keyword">void</span> <span class="title">DeleteMinNodeCircle_Q19</span><span class="params">(Linklist &amp;CL)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    LNode *minNode;</span><br><span class="line">    LNode *preMinNode;</span><br><span class="line">    LNode *preNode;</span><br><span class="line">    LNode *curNode;</span><br><span class="line">    <span class="comment">/* 如果不是空表 */</span></span><br><span class="line">    <span class="keyword">while</span> (CL-&gt;next != CL)</span><br><span class="line">    &#123;</span><br><span class="line">        preNode = CL;</span><br><span class="line">        curNode = CL-&gt;next;</span><br><span class="line">        minNode = curNode;</span><br><span class="line">        preMinNode = preNode;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 遍历一轮 */</span></span><br><span class="line">        <span class="keyword">while</span> (curNode != CL)</span><br><span class="line">        &#123;</span><br><span class="line">            <span class="comment">/* 如果当前结点的 data 域比 minNode 的 data 域更小 */</span></span><br><span class="line">            <span class="keyword">if</span> (curNode-&gt;data &lt; minNode-&gt;data)</span><br><span class="line">            &#123;</span><br><span class="line">                <span class="comment">/* 指针指向对应位置 */</span></span><br><span class="line">                minNode = curNode;</span><br><span class="line">                preMinNode = preNode;</span><br><span class="line">            &#125;</span><br><span class="line"></span><br><span class="line">            <span class="comment">/* preNode 和 curNode 向前进一位 */</span></span><br><span class="line">            preNode = curNode;</span><br><span class="line">            curNode = curNode-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 遍历一轮后，minNode 是指向最小结点的指针 */</span></span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;%d &quot;</span>, minNode-&gt;data);</span><br><span class="line">        <span class="comment">/* 脱链 */</span></span><br><span class="line">        preMinNode-&gt;next = minNode-&gt;next;</span><br><span class="line">        <span class="comment">/* 清空 */</span></span><br><span class="line">        <span class="built_in">free</span>(minNode);</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">free</span>(CL);</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q20. 头指针为 L 的带有头结点的双链表，但是多一个访问频度域 freq。在链表被启用前，所有结点的 freq 初始化为 0 ，每当在链表中进行一次 Locate(L, x)的运算时，其域增加 1 ，并使次链表中结点保持按访问频度递减顺序排列，同时最近访问的结点排在频度相同的结点前面。编写符合要求的 Locate(L, x) 算法，返回所找到的结点指针 */</span></span><br><span class="line"><span class="function">Linklist_Q20 <span class="title">Locate_Q20</span><span class="params">(Linklist_Q20 &amp;FDL, ElemType x)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    LNode_Q20 *curNode = FDL-&gt;next;</span><br><span class="line">    LNode_Q20 *preNode = FDL;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 找到等于 x 的 curNode ，如果不存在 x ，则 curNode 指向 NULL */</span></span><br><span class="line">    <span class="keyword">while</span> (curNode &amp;&amp; curNode-&gt;data != x)</span><br><span class="line">        curNode = curNode-&gt;next;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 如果链表中没有 data 域等于 x 的结点 */</span></span><br><span class="line">    <span class="keyword">if</span> (!curNode)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="built_in">printf</span>(<span class="string">&quot;x is not exist.\n&quot;</span>);</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* data=x 的结点的 freq 域++ */</span></span><br><span class="line">        curNode-&gt;freq++;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 将 curNode 结点从链表中取下来，此时还能找到 curNode 的前驱和后继，但是 curNode 已经不在整体链表上 */</span></span><br><span class="line">        <span class="keyword">if</span> (curNode-&gt;next != <span class="literal">NULL</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            curNode-&gt;next-&gt;prior = curNode-&gt;prior;</span><br><span class="line">        &#125;</span><br><span class="line">        curNode-&gt;prior-&gt;next = curNode-&gt;next;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 按要求找到要插入位置的前驱结点 */</span></span><br><span class="line">        <span class="keyword">while</span> (preNode-&gt;next != <span class="literal">NULL</span> &amp;&amp; preNode-&gt;next-&gt;freq &gt; curNode-&gt;freq)</span><br><span class="line">        &#123;</span><br><span class="line">            preNode = preNode-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line"></span><br><span class="line">        <span class="comment">/* 插入 */</span></span><br><span class="line">        curNode-&gt;next = preNode-&gt;next;</span><br><span class="line">        curNode-&gt;prior = preNode;</span><br><span class="line">        <span class="comment">/* 如果 preNode-&gt;next 存在，则链接其前驱结点 */</span></span><br><span class="line">        <span class="keyword">if</span> (preNode-&gt;next)</span><br><span class="line">            preNode-&gt;next-&gt;prior = curNode;</span><br><span class="line">        preNode-&gt;next = curNode;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> FDL;</span><br><span class="line">    <span class="comment">// return curNode;</span></span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q21. [2009] 带表头的单链表，不改变链表前提下，尽可能高效地查找链表中倒数第 k 个位置上的结点（k为正整数）。若查找成功，算法输出结点的 data 域，并返回 1 ，否则返回 0 。 */</span></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">FindReciprocalK_Q21</span><span class="params">(Linklist L, <span class="keyword">int</span> k)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    LNode *auxiliary = L-&gt;next;</span><br><span class="line">    LNode *curNode = L-&gt;next;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (k--)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (auxiliary)</span><br><span class="line">            auxiliary = auxiliary-&gt;next;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">            <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">while</span> (auxiliary)</span><br><span class="line">    &#123;</span><br><span class="line">        auxiliary = auxiliary-&gt;next;</span><br><span class="line">        curNode = curNode-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="built_in">printf</span>(<span class="string">&quot;%d&quot;</span>, curNode-&gt;data);</span><br><span class="line">    <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q22. [2012] 用带头结点的单链表保存单词，当两个单词有相同的后缀时，可共享相同得到后缀存储空间。设计一个时间上尽可能高效的算法，找出由两个链表共同后缀的开始位置。 */</span></span><br><span class="line"><span class="function">LNode *<span class="title">FindPublicNode_Q22</span><span class="params">(Linklist L1, Linklist L2)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 可以知道，可知道如果两个链表有公共结点，则后缀都是一样的，差别就是需要同步遍历，因此要先让较长的链表先前后遍历，直到可以和较短链表同时向后遍历 */</span></span><br><span class="line">    LNode *longerNode, *shorterNode;</span><br><span class="line">    LNode *tempNode;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">int</span> gap, len1, len2;</span><br><span class="line">    len1 = <span class="built_in">GetLinklistLength</span>(L1);</span><br><span class="line">    len2 = <span class="built_in">GetLinklistLength</span>(L2);</span><br><span class="line">    <span class="keyword">if</span> (len1 &lt; len2)</span><br><span class="line">    &#123;</span><br><span class="line">        longerNode = L2-&gt;next;</span><br><span class="line">        shorterNode = L1-&gt;next;</span><br><span class="line">        gap = len2 - len1;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">else</span></span><br><span class="line">    &#123;</span><br><span class="line">        longerNode = L1-&gt;next;</span><br><span class="line">        shorterNode = L2-&gt;next;</span><br><span class="line">        gap = len1 - len2;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">while</span> (gap--)</span><br><span class="line">    &#123;</span><br><span class="line">        longerNode = longerNode-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">while</span> (longerNode)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="keyword">if</span> (longerNode == shorterNode)</span><br><span class="line">            <span class="keyword">return</span> longerNode;</span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            longerNode = longerNode-&gt;next;</span><br><span class="line">            shorterNode = shorterNode-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q23. [2015] 单链表保存 m 个整数，结点的结构为[data][link]，且|data|&lt;=n（n为正整数），设计一个时间复杂度尽可能高效的算法，对于链表中data的绝对值相等的结点，仅保留第一次出现的结点，而删除其余绝对值相等的结点 */</span></span><br><span class="line"><span class="function">Linklist <span class="title">DeteleABSSame_Q23</span><span class="params">(Linklist &amp;L, <span class="keyword">int</span> n)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    LNode *delNode;</span><br><span class="line">    LNode *curNode = L;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 开辟一个大小为 n+1 的动态一维数组,并置零 */</span></span><br><span class="line">    <span class="keyword">int</span> *q = (<span class="keyword">int</span> *)<span class="built_in">malloc</span>(<span class="built_in"><span class="keyword">sizeof</span></span>(<span class="keyword">int</span>) * (n + <span class="number">1</span>));</span><br><span class="line">    <span class="built_in">memset</span>(q, <span class="number">0</span>, <span class="built_in"><span class="keyword">sizeof</span></span>(<span class="keyword">int</span>) * (n + <span class="number">1</span>));</span><br><span class="line">    <span class="keyword">int</span> idx;</span><br><span class="line"></span><br><span class="line">    <span class="keyword">while</span> (curNode-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        <span class="comment">/* idx为data域的数值的绝对值 */</span></span><br><span class="line">        idx = curNode-&gt;next-&gt;data &gt; <span class="number">0</span> ? curNode-&gt;next-&gt;data : -curNode-&gt;next-&gt;data;</span><br><span class="line">        <span class="comment">/* 如果没有记录,表示该数值第一次出现,则置1,向后遍历.这里应当注意q+idx的指向 */</span></span><br><span class="line">        <span class="keyword">if</span> (*(q + idx) == <span class="number">0</span>)</span><br><span class="line">        &#123;</span><br><span class="line">            *(q + idx) = <span class="number">1</span>;</span><br><span class="line">            curNode = curNode-&gt;next;</span><br><span class="line">        &#125;</span><br><span class="line">        <span class="comment">/* 如果有记录,则删除当前结点 */</span></span><br><span class="line">        <span class="keyword">else</span></span><br><span class="line">        &#123;</span><br><span class="line">            delNode = curNode-&gt;next;</span><br><span class="line">            curNode-&gt;next = delNode-&gt;next;</span><br><span class="line">            <span class="built_in">free</span>(delNode);</span><br><span class="line">        &#125;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="built_in">free</span>(q);</span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q24. 判断一个链表是否有环,如果有,找出入口点并返回 */</span></span><br><span class="line"><span class="function">LNode *<span class="title">FindCircleNode_Q24</span><span class="params">(Linklist L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* 设置两个指针,快指针和慢指针,快指针一次走两步,慢指针一次走一步.假设 head 到达环入口点的距离是 a , 两只指针在环中相遇点距离入口点的距离是 b ,相遇时快指针比慢指针多走了 n 圈,环长为 r. 则有 2 * dis1 = dis2, 即为 2(a+b) = a+nr+b, 可以得到 a=nr-b , 因此当两个指针分别从 head 和 相遇点同步向后遍历,即可获得入口点 */</span></span><br><span class="line">    LNode *fastNode = L;</span><br><span class="line">    LNode *slowNode = L;</span><br><span class="line">    <span class="keyword">while</span> (fastNode-&gt;next &amp;&amp; slowNode)</span><br><span class="line">    &#123;</span><br><span class="line">        slowNode = slowNode-&gt;next;</span><br><span class="line">        fastNode = fastNode-&gt;next-&gt;next;</span><br><span class="line">        <span class="keyword">if</span> (slowNode == fastNode)</span><br><span class="line">            <span class="keyword">break</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">if</span> (slowNode == <span class="literal">NULL</span> || fastNode-&gt;next == <span class="literal">NULL</span>)</span><br><span class="line">    &#123;</span><br><span class="line">        fastNode-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line">        <span class="keyword">return</span> <span class="literal">NULL</span>;</span><br><span class="line">    &#125;</span><br><span class="line">    LNode *p1 = L;</span><br><span class="line">    LNode *p2 = slowNode;</span><br><span class="line">    <span class="keyword">while</span> (p1 != p2)</span><br><span class="line">    &#123;</span><br><span class="line">        p1 = p1-&gt;next;</span><br><span class="line">        p2 = p2-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> p1;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="comment">/* Q25. [2019] 线性表L=(a1,a2,a3,...,an-1,an),采用带头结点的单链表保存,设计空间复杂度O(1)且时间上尽可能高效的算法,重新排列L中的各结点,得到L&#x27;=(a1,an,a2,an-1,...) */</span></span><br><span class="line"><span class="function">Linklist <span class="title">SortLinklist_Q25</span><span class="params">(Linklist &amp;L)</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    LNode *p, *q, *r, *s;</span><br><span class="line">    p = q = L;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* q进2,p进1,寻找中间结点p */</span></span><br><span class="line">    <span class="keyword">while</span> (q-&gt;next)</span><br><span class="line">    &#123;</span><br><span class="line">        p = p-&gt;next;</span><br><span class="line">        q = q-&gt;next;</span><br><span class="line">        <span class="keyword">if</span> (q-&gt;next)</span><br><span class="line">            q = q-&gt;next;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* q 为后半段链表的首结点 */</span></span><br><span class="line">    q = p-&gt;next;</span><br><span class="line">    p-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 后半段链表逆置 */</span></span><br><span class="line">    <span class="keyword">while</span> (q)</span><br><span class="line">    &#123;</span><br><span class="line">        r = q-&gt;next;</span><br><span class="line">        q-&gt;next = p-&gt;next;</span><br><span class="line">        p-&gt;next = q;</span><br><span class="line">        q = r;</span><br><span class="line">    &#125;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* s 指向前半段链表的首结点 */</span></span><br><span class="line">    s = L-&gt;next;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* q 指向后半段链表的首结点 */</span></span><br><span class="line">    q = p-&gt;next;</span><br><span class="line">    p-&gt;next = <span class="literal">NULL</span>;</span><br><span class="line"></span><br><span class="line">    <span class="comment">/* 后半段插入指定位置 */</span></span><br><span class="line">    <span class="keyword">while</span> (q)</span><br><span class="line">    &#123;</span><br><span class="line">        r = q-&gt;next;       <span class="comment">/* r 指向后半段链表的下个结点 */</span></span><br><span class="line">        q-&gt;next = s-&gt;next; <span class="comment">/* q所指结点插入s所指结点后 */</span></span><br><span class="line">        s-&gt;next = q;</span><br><span class="line">        s = q-&gt;next; <span class="comment">/* s 指向前半段的下一个插入点 */</span></span><br><span class="line">        q = r;</span><br><span class="line">    &#125;</span><br><span class="line">    <span class="keyword">return</span> L;</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">main</span><span class="params">()</span></span></span><br><span class="line"><span class="function"></span>&#123;</span><br><span class="line">    <span class="comment">/* Q1. */</span></span><br><span class="line">    <span class="comment">// Linklist L;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L = InsertDataFromTailwithoutHead(L, n);</span></span><br><span class="line">    <span class="comment">// DeleteAllxNodeWithoutHead_Q1(L, 3);</span></span><br><span class="line">    <span class="comment">// PrintLinklistWithoutHead(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q2. */</span></span><br><span class="line">    <span class="comment">// Linklist L;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L = InsertDataFromTail(L, n);</span></span><br><span class="line">    <span class="comment">// DeleteAllxNode_Q2(L, 4);</span></span><br><span class="line">    <span class="comment">// PrintLinklist(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q3. */</span></span><br><span class="line">    <span class="comment">// Linklist L;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L = InsertDataFromTail(L, n);</span></span><br><span class="line">    <span class="comment">// L = ReverseLinklist_Q3(L);</span></span><br><span class="line">    <span class="comment">// PrintLinklist(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q4. */</span></span><br><span class="line">    <span class="comment">// Linklist L;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L = InsertDataFromTail(L, n);</span></span><br><span class="line">    <span class="comment">// L = DeleteMinNode_Q4(L);</span></span><br><span class="line">    <span class="comment">// PrintLinklist(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q5. */</span></span><br><span class="line">    <span class="comment">// Linklist L;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L = InsertDataFromTail(L, n);</span></span><br><span class="line">    <span class="comment">// L = ReverseLinklist_Q5(L);</span></span><br><span class="line">    <span class="comment">// PrintLinklist(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q6. */</span></span><br><span class="line">    <span class="comment">// Linklist L;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L = InsertDataFromTail(L, n);</span></span><br><span class="line">    <span class="comment">// L = OrderlyIncreasing_Q6(L);</span></span><br><span class="line">    <span class="comment">// PrintLinklist(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q7. */</span></span><br><span class="line">    <span class="comment">// Linklist L;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L = InsertDataFromTail(L, n);</span></span><br><span class="line">    <span class="comment">// L = DeleteNodeBetweenAB_Q7(L, 4, 9);</span></span><br><span class="line">    <span class="comment">// PrintLinklist(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q8. */</span></span><br><span class="line">    <span class="comment">// Linklist L1;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L1 = InsertDataFromTail(L1, n);</span></span><br><span class="line">    <span class="comment">// Linklist L2;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L2 = (Linklist)malloc(sizeof(LNode) * n);</span></span><br><span class="line">    <span class="comment">// L2-&gt;next = NULL;</span></span><br><span class="line">    <span class="comment">// LNode *tail = L2;</span></span><br><span class="line">    <span class="comment">// for (int i = 0; i &lt; n; i++)</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     int x;</span></span><br><span class="line">    <span class="comment">//     scanf_s(&quot;%d&quot;, &amp;x);</span></span><br><span class="line">    <span class="comment">//     LNode *p = (LNode *)malloc(sizeof(LNode));</span></span><br><span class="line">    <span class="comment">//     p-&gt;data = x;</span></span><br><span class="line">    <span class="comment">//     p-&gt;next = tail-&gt;next;</span></span><br><span class="line">    <span class="comment">//     tail-&gt;next = p;</span></span><br><span class="line">    <span class="comment">//     tail = p;</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">    <span class="comment">// LNode *p = (LNode *)malloc(sizeof(LNode));</span></span><br><span class="line">    <span class="comment">// p = L1-&gt;next-&gt;next-&gt;next-&gt;next;</span></span><br><span class="line">    <span class="comment">// tail-&gt;next = p;</span></span><br><span class="line">    <span class="comment">// tail = p;</span></span><br><span class="line">    <span class="comment">//</span></span><br><span class="line">    <span class="comment">// 5</span></span><br><span class="line">    <span class="comment">// 1 2 3 4 5</span></span><br><span class="line">    <span class="comment">// 5</span></span><br><span class="line">    <span class="comment">// 7 8 9 10 11</span></span><br><span class="line">    <span class="comment">// linklist:</span></span><br><span class="line">    <span class="comment">// L1: 1 2 3 4 5</span></span><br><span class="line">    <span class="comment">// L2: 7 8 9 10 11 4 5</span></span><br><span class="line">    <span class="comment">// Public: 4 5</span></span><br><span class="line">    <span class="comment">//</span></span><br><span class="line">    <span class="comment">// PrintLinklistWithoutHead(FindPublicNode_Q8(L1, L2)); /* 显然是不带头结点的单链表*/</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q9. */</span></span><br><span class="line">    <span class="comment">// Linklist L;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L = InsertDataFromTail(L, n);</span></span><br><span class="line">    <span class="comment">// OutputOrderlyIncreasing_Q9(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q10. */</span></span><br><span class="line">    <span class="comment">// Linklist L1, L2;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L1 = InsertDataFromTail(L1, n);</span></span><br><span class="line">    <span class="comment">// L2 = SeperateOddEven_Q10(L1);</span></span><br><span class="line">    <span class="comment">// PrintLinklist(L1);</span></span><br><span class="line">    <span class="comment">// PrintLinklist(L2);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q11. */</span></span><br><span class="line">    <span class="comment">// Linklist L1, L2;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L1 = InsertDataFromTail(L1, n);</span></span><br><span class="line">    <span class="comment">// L2 = SeperateOddEven_Q11(L1);</span></span><br><span class="line">    <span class="comment">// PrintLinklist(L1);</span></span><br><span class="line">    <span class="comment">// PrintLinklist(L2);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q12. */</span></span><br><span class="line">    <span class="comment">// Linklist L;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L = InsertDataFromTail(L, n);</span></span><br><span class="line">    <span class="comment">// L = DeleteSameNode_Q12(L);</span></span><br><span class="line">    <span class="comment">// PrintLinklist(L);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q13. */</span></span><br><span class="line">    <span class="comment">// Linklist L1;</span></span><br><span class="line">    <span class="comment">// int n1;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n1);</span></span><br><span class="line">    <span class="comment">// L1 = InsertDataFromTail(L1, n1);</span></span><br><span class="line">    <span class="comment">// Linklist L2;</span></span><br><span class="line">    <span class="comment">// int n2;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n2);</span></span><br><span class="line">    <span class="comment">// L2 = InsertDataFromTail(L2, n2);</span></span><br><span class="line">    <span class="comment">// PrintLinklist(CombineOrderlyDecreasing_Q13(L1, L2));</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q14. */</span></span><br><span class="line">    <span class="comment">// Linklist L1;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L1 = InsertDataFromTail(L1, n);</span></span><br><span class="line">    <span class="comment">// Linklist L2;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L2 = InsertDataFromTail(L2, n);</span></span><br><span class="line">    <span class="comment">// PrintLinklist(FindOrderlyPublicNode_Q14(L1, L2));</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q15. */</span></span><br><span class="line">    <span class="comment">// Linklist L1;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L1 = InsertDataFromTail(L1, n);</span></span><br><span class="line">    <span class="comment">// Linklist L2;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L2 = InsertDataFromTail(L2, n);</span></span><br><span class="line">    <span class="comment">// L1 = FindOrderlyPublicNode_Q15(L1, L2);</span></span><br><span class="line">    <span class="comment">// PrintLinklist(L1);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q16. */</span></span><br><span class="line">    <span class="comment">// Linklist L1;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L1 = InsertDataFromTail(L1, n);</span></span><br><span class="line">    <span class="comment">// Linklist L2;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L2 = InsertDataFromTail(L2, n);</span></span><br><span class="line">    <span class="comment">// if (isContinuousSubSequence_Q16(L1, L2))</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     printf(&quot;B is the continuous subsequence of A.&quot;);</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">    <span class="comment">// else</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     printf(&quot;B is not the continuous subsequence of A.&quot;);</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q17. */</span></span><br><span class="line">    <span class="comment">// DLinklist CDL1;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// InsertDataFromTail_Circle(CDL1, n);</span></span><br><span class="line">    <span class="comment">// PrintCDLinklist(CDL1);</span></span><br><span class="line">    <span class="comment">// if (isCDLinklistSymmetry_Q17(CDL1))</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     printf(&quot;Symmetry CDL\n&quot;);</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">    <span class="comment">// else</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     printf(&quot;NOT a Symmetry CDL\n&quot;);</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q18. */</span></span><br><span class="line">    <span class="comment">// Linklist CL1;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// CL1 = InsertDataFromTail_Circle(CL1, n);</span></span><br><span class="line">    <span class="comment">// Linklist CL2;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// CL2 = InsertDataFromTail_Circle(CL2, n);</span></span><br><span class="line">    <span class="comment">// CombineCircleLinklist_Q18(CL1, CL2);</span></span><br><span class="line">    <span class="comment">// PrintCLinklist(CL1);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q19. */</span></span><br><span class="line">    <span class="comment">// Linklist CL1;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// CL1 = InsertDataFromTail_Circle(CL1, n);</span></span><br><span class="line">    <span class="comment">// DeleteMinNodeCircle_Q19(CL1);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q20. */</span></span><br><span class="line">    <span class="comment">// Linklist_Q20 FDL;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// InsertDataFromTailwithFreq(FDL, n);</span></span><br><span class="line">    <span class="comment">// PrintFreqDLinklist(FDL);</span></span><br><span class="line">    <span class="comment">// for (int i = 0; i &lt; 10; i++)</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     Locate_Q20(FDL, 4);</span></span><br><span class="line">    <span class="comment">//     Locate_Q20(FDL, 2);</span></span><br><span class="line">    <span class="comment">//     Locate_Q20(FDL, 3);</span></span><br><span class="line">    <span class="comment">//     Locate_Q20(FDL, 8);</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">    <span class="comment">// PrintFreqDLinklist(FDL);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q21. */</span></span><br><span class="line">    <span class="comment">// Linklist L1;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L1 = InsertDataFromTail(L1, n);</span></span><br><span class="line">    <span class="comment">// FindReciprocalK_Q21(L1, 3);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q22. */</span></span><br><span class="line">    <span class="comment">// Linklist L1;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L1 = InsertDataFromTail(L1, n);</span></span><br><span class="line">    <span class="comment">// Linklist L2;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L2 = (Linklist)malloc(sizeof(LNode) * n);</span></span><br><span class="line">    <span class="comment">// L2-&gt;next = NULL;</span></span><br><span class="line">    <span class="comment">// LNode *tail = L2;</span></span><br><span class="line">    <span class="comment">// for (int i = 0; i &lt; n; i++)</span></span><br><span class="line">    <span class="comment">// &#123;</span></span><br><span class="line">    <span class="comment">//     int x;</span></span><br><span class="line">    <span class="comment">//     scanf_s(&quot;%d&quot;, &amp;x);</span></span><br><span class="line">    <span class="comment">//     LNode *p = (LNode *)malloc(sizeof(LNode));</span></span><br><span class="line">    <span class="comment">//     p-&gt;data = x;</span></span><br><span class="line">    <span class="comment">//     p-&gt;next = tail-&gt;next;</span></span><br><span class="line">    <span class="comment">//     tail-&gt;next = p;</span></span><br><span class="line">    <span class="comment">//     tail = p;</span></span><br><span class="line">    <span class="comment">// &#125;</span></span><br><span class="line">    <span class="comment">// LNode *p = (LNode *)malloc(sizeof(LNode));</span></span><br><span class="line">    <span class="comment">// p = L1-&gt;next-&gt;next-&gt;next-&gt;next;</span></span><br><span class="line">    <span class="comment">// tail-&gt;next = p;</span></span><br><span class="line">    <span class="comment">// tail = p;</span></span><br><span class="line">    <span class="comment">// // 5</span></span><br><span class="line">    <span class="comment">// // 1 2 3 4 5</span></span><br><span class="line">    <span class="comment">// // 5</span></span><br><span class="line">    <span class="comment">// // 7 8 9 10 11</span></span><br><span class="line">    <span class="comment">// // linklist:</span></span><br><span class="line">    <span class="comment">// // L1: 1 2 3 4 5</span></span><br><span class="line">    <span class="comment">// // L2: 7 8 9 10 11 4 5</span></span><br><span class="line">    <span class="comment">// // Public: 4 5</span></span><br><span class="line">    <span class="comment">// PrintLinklistWithoutHead(FindPublicNode_Q22(L1, L2)); /* 显然是不带头结点的单链表*/</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q23. */</span></span><br><span class="line">    <span class="comment">// Linklist L1;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L1 = InsertDataFromTail(L1, n);</span></span><br><span class="line">    <span class="comment">// L1 = DeteleABSSame_Q23(L1, 10);</span></span><br><span class="line">    <span class="comment">// PrintLinklist(L1);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q24. */</span></span><br><span class="line">    <span class="comment">// Linklist L1;</span></span><br><span class="line">    <span class="comment">// int n;</span></span><br><span class="line">    <span class="comment">// scanf_s(&quot;%d&quot;, &amp;n);</span></span><br><span class="line">    <span class="comment">// L1 = InsertDataFromTail(L1, n);</span></span><br><span class="line">    <span class="comment">// printf(&quot;%d&quot;, FindCircleNode_Q24(L1)-&gt;data);</span></span><br><span class="line"></span><br><span class="line">    <span class="comment">/* Q25. */</span></span><br><span class="line">    Linklist L1;</span><br><span class="line">    <span class="keyword">int</span> n;</span><br><span class="line">    <span class="built_in">scanf_s</span>(<span class="string">&quot;%d&quot;</span>, &amp;n);</span><br><span class="line">    L1 = <span class="built_in">InsertDataFromTail</span>(L1, n);</span><br><span class="line">    <span class="built_in">PrintLinklist</span>(<span class="built_in">SortLinklist_Q25</span>(L1));</span><br><span class="line"></span><br><span class="line">    <span class="keyword">return</span> <span class="number">0</span>;</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;线性表&lt;/h1&gt;
&lt;p&gt;重点内容：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;线性表的定义和基本操作&lt;/li&gt;
&lt;li&gt;线性表的实现：顺序存储、链式存储、线性表的应用&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;知识框架：&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/study/course/data-stru</summary>
      
    
    
    
    <category term="study" scheme="https://www.jk404.cn/categories/study/"/>
    
    <category term="course" scheme="https://www.jk404.cn/categories/study/course/"/>
    
    <category term="data structure" scheme="https://www.jk404.cn/categories/study/course/data-structure/"/>
    
    
  </entry>
  
  <entry>
    <title>data_sturcture_01_introduction</title>
    <link href="https://www.jk404.cn/study/course/data-structure/data-sturcture-01-introduction/"/>
    <id>https://www.jk404.cn/study/course/data-structure/data-sturcture-01-introduction/</id>
    <published>2022-10-20T04:16:10.000Z</published>
    <updated>2022-11-03T05:53:02.270Z</updated>
    
    <content type="html"><![CDATA[<h1>绪论</h1><p>这一章粗略过一下，讲一下重点即可。</p><p>知识框架图</p><p><img src="/study/course/data-structure/data-sturcture-01-introduction/figure_01.jpg" alt></p><h2 id="数据结构的一些概念">数据结构的一些概念</h2><p>【数据】- 信息载体；</p><p>【数据项】- 构成数据元素的不可分割的最小单位；</p><p>【数据元素】- 数据的基本单位，可由若干数据项组成；</p><p>【数据对象】- 具有相同性质的数据元素的集合，是数据的子集；</p><p>【数据类型】- 一个值的集合和定义在此集合上的一组操作的总称，包括类型有：1.原子类型 2.结构类型 3.抽象数据类型</p><p>【数据结构】- 相互之间存在一种或多种特定关系的数据元素的集合。在任何问题中，数据元素都不是孤立存在的，它们之间存在某种关系，这种数据元素相互之间的关系称为<code>结构(structure)</code>。数据结构包括三方面的内容：<code>1.逻辑结构</code> <code>2.存储结构</code> <code>3.数据的运算</code>。</p><p><em>数据的<strong>逻辑结构和存储结构是密不可分的</strong>，一个算法的<strong>设计</strong>取决于所选定的<strong>逻辑结构</strong>，而算法的<strong>实现</strong>依赖于所采用的<strong>存储结构</strong></em></p><h3 id="数据结构三要素">数据结构三要素</h3><p>上面提到了数据结构的三要素，分别是：</p><ul><li>逻辑结构</li><li>存储结构</li><li>数据的运算</li></ul><h4 id="逻辑结构">逻辑结构</h4><p>数据元素之间的逻辑关系，从逻辑关系上描述数据。它与数据的存储无关，是独立于计算机的。</p><p>逻辑结构分为<code>线性结构</code>和<code>非线性结构</code>。</p><p><img src="/study/course/data-structure/data-sturcture-01-introduction/figure_02.jpg" alt></p><h4 id="存储结构">存储结构</h4><p>存储结构是数据结构在计算机中的表示（映像），也称为物理结构。其包括数据元素的表示和关系的表示。存储方式主要有：</p><ul><li><p>顺序存储：把<strong>逻辑上相邻</strong>的元素存储在<strong>物理位置相邻</strong>的存储单元中，<strong>元素之间的关系由存储单元的邻接关系来体现</strong>。</p><ul><li>优点：可以实现<strong>随机存取</strong>，每个元素占用<strong>最少的存储空间</strong>；</li><li>缺点：<strong>只能使用相邻的一整块存储单元</strong>，可能产生较多外部<strong>碎片</strong>；</li></ul></li><li><p>链式存储：<strong>逻辑相邻</strong>的元素在<strong>物理位置上不需要相邻</strong>，借助<strong>指示元素存储地址的指针</strong>来表示元素之间的逻辑关系。</p><ul><li>优点：不会出现碎片，<strong>充分利用了所有存储单元</strong>；</li><li>缺点：每个元素因<strong>存储指针而占用额外的存储空间</strong>，而且只能实现<strong>顺序存取</strong>；</li></ul></li><li><p>索引存储：在存储元素信息的同时，建立<strong>附加的索引表</strong>，索引表的每一项称为索引项，一般形式是（关键字，地址）。</p><ul><li>优点：<strong>检索速度快</strong>；</li><li>缺点：附加的<strong>索引表占用而外空间</strong>，<strong>增加</strong>和<strong>删除</strong>也需要<strong>修改索引</strong>；</li></ul></li><li><p>散列存储：根据元素的<strong>关键字</strong>直接计算处该元素的<strong>存储地址</strong>，哈希(Hash)存储。</p><ul><li>优点：检索、增加和删除结点操作都很<strong>快</strong>；</li><li>缺点：如果<strong>散列函数较差</strong>，可能出现元素<strong>存储单元冲突</strong>；</li></ul></li></ul><h4 id="数据的运算">数据的运算</h4><p>包括：</p><ul><li>运算的定义：针对逻辑结构，说明运算的功能；</li><li>运算的实现：针对存储结构，指出运算的具体操作步骤；</li></ul><h2 id="算法和算法评价">算法和算法评价</h2><h3 id="基本概念">基本概念</h3><p>五个重要性：</p><ul><li>有穷性</li><li>确定性</li><li>可行性</li><li>输入</li><li>输出</li></ul><p>好的算法还要考虑以下目标：</p><ul><li>正确性</li><li>可读性</li><li>健壮性</li><li>效率与低存储量需求</li></ul><h3 id="算法效率的度量">算法效率的度量</h3><ul><li>时间复杂度</li><li>空间复杂度</li></ul><h4 id="时间复杂度">时间复杂度</h4><p>一个语句的<strong>频度</strong> 指的是 该语句在算法中<strong>被重复执行的次数</strong>。</p><p>算法中所有语句的频度之和记为 $T{(n)}$ ，它是该算法问题规模 $n$ 的函数。</p><p>时间复杂度主要分析 $T{(n)}$ 的数量级。基本运算的频度 $f{(n)}$（最深层循环内的语句的频度）与 $T{(n)}$ 有相同数量级。取 $f{(n)}$ 中随 $n$ 增长最快的项，将其系数置为 $1$ 作为时间复杂度的度量。例如， $f{(n)}=an<sup>3+bn</sup>2+cn$ 的复杂度为$O{(n^3)}$ 。</p><p>记为：<br>$$<br>T{(n)}=O(f{(n)})<br>$$<br>$O$ 的含义是 $T(n)$ 的数量级，严格的数学定义是：若 $T(n)$ 和 $f(n)$ 是定义在正整数集合上的两个函数，则存在正常数 $C$ 和 $n_{0}$，使得当 $n \geq n_0$ 时，都满足 $0 \leq T(n) \leq Cf(n)$。</p><p>时间复杂度还取决于输入数据的性质，如：</p><p>对于数组<code>arr[0...n-1]</code>，查找给定值<code>k</code>的算法大致为：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">i=n-1;</span><br><span class="line">while(i&gt;=0&amp;&amp;(arr[i]!=k))</span><br><span class="line">i--;</span><br><span class="line">return i;</span><br></pre></td></tr></table></figure><p>存在两种情况：</p><ul><li>若arr中没有等于<code>k</code>的元素，则语句<code>i--;</code>的频度是 $f(n)=n$ ；</li><li>若arr中最后一个元素就等于<code>k</code>，则语句<code>i--;</code>的频度是 $0$ ;</li></ul><p><strong>一般总是考虑在最坏情况下的时间复杂度</strong>，以保证算法的运行时间不会比之更长。</p><p>在分析一个程序的时间复杂度时，有以下两个规则：</p><ul><li><p>加法规则<br>$$<br>T(n)=T_1(n)+T_2(n)=O(f(n))+O(g(n))=O(max(f(n),g(n)))<br>$$</p><p>即：当两个频度的语句处于同一层时，该层的时间复杂度等于频度更大的语句的频度。</p></li><li><p>乘法规则<br>$$<br>T(n)=T_1(n) \times T_2(n)=O(f(n)) \times O(g(n))=O(f(n) \times g(n))<br>$$<br>即：当两个频度的语句进行嵌套时，其整体的时间复杂度等于嵌套各层的语句频度相乘。</p></li></ul><h5 id="时间复杂度比较">时间复杂度比较</h5><p>$O(1)&lt;O(log_2n)&lt;O(n)&lt;O(nlog_2n)&lt;O(n<sup>2)&lt;O(n</sup>3)&lt;O(2<sup>n)&lt;O(n!)&lt;O(n</sup>n)$</p><h4 id="空间复杂度">空间复杂度</h4><p>$S(n)$ 定义为该算法所耗费的存储空间，是问题规模 $n$ 的函数，记为：<br>$$<br>S(n)=O(g(n))<br>$$<br>一个程序在执行时除需要存储空间来存放<strong>本身所用的指令</strong>、<strong>常数</strong>、<strong>变量</strong>和<strong>输入数据</strong>外，还需要一些对数据进行操作的<strong>工作单元</strong>和存储一些为实现<strong>计算所需信息的辅助空间</strong>。若输入数据所占空间只取决于问题本身，和算法无关，则<strong>只需分析除输入和程序之外的额外空间</strong>。</p><p>算法<strong>原地工作</strong>指的是算法所需的<strong>辅助空间为常量</strong>，即 $O(1)$ 。</p><h2 id="归纳总结">归纳总结</h2><p>分析程序的时间复杂度是重点。</p><ul><li><p>循环主题中的变量<strong>参与</strong>循环条件的判断</p><p>此类型的题目应该找出主体语句中于 $T(n)$ 成正比的循环变量，将之代入条件中进行计算。例如：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// sample 1</span></span><br><span class="line"><span class="keyword">int</span> i=<span class="number">1</span>;</span><br><span class="line"><span class="keyword">while</span>(i&lt;=n)</span><br><span class="line">i=i*<span class="number">2</span></span><br></pre></td></tr></table></figure><p>在这个例子中，<code>i*2</code> 的次数正是主体语句执行次数 $t$ ，因此执行 $t$ 次后，有 $2^t \leq n$ ，取对数后可以得到 $t \leq log_2n$ ，即为 $T(n)=O(log_2n)$ 。</p><p>又例如：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="comment">// sample 2</span></span><br><span class="line"><span class="keyword">int</span> y=<span class="number">5</span>;</span><br><span class="line"><span class="keyword">while</span>((y+<span class="number">1</span>)*(y+<span class="number">1</span>)&lt;n)</span><br><span class="line">    y=y+<span class="number">1</span>;</span><br></pre></td></tr></table></figure><p>在这个例子中，<code>y+1</code> 的次数恰好与 $T(n)$ 成正比。</p><p>记 $t$ 为该程序执行次数。</p><ol><li>执行 $t$ 次后，先计算循环体里的语句：<br>$$<br>y=y_0+t=5+t<br>$$</li></ol></li></ul><ol start="2"><li><p>然后将 $y$ 代入循环条件得到：<br>$$<br>(t+5+1) \times (t+5+1) &lt; n<br>$$<br>即为：<br>$$<br>t&lt;\sqrt n -6<br>$$</p></li><li><p>即 $T(n)=O(\sqrt n)$</p></li></ol><ul><li><p>循环主题中的变量<strong>不参与</strong>循环条件的判断</p><p>采用 <strong>数学归纳法</strong> 或 <strong>直接累计循环次数</strong>。</p><p><strong>多层循环时，从内到外分析，忽略单步语句、条件判断语句，只关注主体语句的执行次数</strong>。</p><p>又可分为递归程序和非递归程序：</p><ul><li><p>递归程序是用公式递推，例子如下：</p><figure class="highlight c"><table><tr><td class="code"><pre><span class="line"><span class="function"><span class="keyword">int</span> <span class="title">fact</span><span class="params">(<span class="keyword">int</span> n)</span></span>&#123;</span><br><span class="line"><span class="keyword">if</span>(n&lt;=<span class="number">1</span>)</span><br><span class="line">        <span class="keyword">return</span> <span class="number">1</span>;</span><br><span class="line">    <span class="keyword">return</span> n * fact(n<span class="number">-1</span>);</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>递推为：</p><p>$$<br>\begin{equation*}<br>\begin{split}<br>T(n)<br>&amp; =1+T(n-1)\<br>&amp; =1+1+T(n-2)\<br>&amp; =\space …\<br>&amp; =n-1+T(1)\<br>&amp; =n<br>\end{split}<br>\end{equation*}<br>$$<br>最终可以得到 $T(n)=O(n)$</p></li><li><p>非递归程序可以直接累计次数。</p></li></ul></li></ul>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;绪论&lt;/h1&gt;
&lt;p&gt;这一章粗略过一下，讲一下重点即可。&lt;/p&gt;
&lt;p&gt;知识框架图&lt;/p&gt;
&lt;p&gt;&lt;img src=&quot;/study/course/data-structure/data-sturcture-01-introduction/figure_01.jpg&quot; al</summary>
      
    
    
    
    <category term="study" scheme="https://www.jk404.cn/categories/study/"/>
    
    <category term="course" scheme="https://www.jk404.cn/categories/study/course/"/>
    
    <category term="data structure" scheme="https://www.jk404.cn/categories/study/course/data-structure/"/>
    
    
  </entry>
  
  <entry>
    <title>pyside2_04_beautify</title>
    <link href="https://www.jk404.cn/program-language/python/pyside2/pyside2-04-beautify/"/>
    <id>https://www.jk404.cn/program-language/python/pyside2/pyside2-04-beautify/</id>
    <published>2022-10-18T10:18:29.000Z</published>
    <updated>2022-10-18T10:34:15.830Z</updated>
    
    <content type="html"><![CDATA[<h1>概要</h1><p>人们对一个友好的用户界面的要求，还需要配色来点缀，才可以使得它完美无缺。几乎在每个控件的属性中，都可以设置颜色和文字大小，这也是配色的主要表现形式。Qt作为一个强大的用户界面绘制工具，它支持QSS脚本来辅助控制窗口控件属性，兼容网页格式控制的CSS脚本，即其脚本的格式和属性。</p><h1>要点</h1><p>具体的参数我就不必讲解了，自行查阅资料学习，相信大家看过我的例子程序就很容易理解。文件名为[名称].qss， 文件语法参考CSS。控制某个控件，简写格式如下：</p><figure class="highlight plaintext"><table><tr><td class="code"><pre><span class="line">[控件类型]#[控件名称],[...]</span><br><span class="line">&#123;</span><br><span class="line">    [属性]:[值]</span><br><span class="line">    ...</span><br><span class="line">&#125;</span><br><span class="line"></span><br><span class="line"># 例：</span><br><span class="line">QStatusBar</span><br><span class="line">&#123;</span><br><span class="line">    color: #DCDCDC;         # 文字颜色</span><br><span class="line">    background: #484848;    # 背景颜色</span><br><span class="line">&#125;</span><br></pre></td></tr></table></figure><p>控件名称可以缺省，缺省时表示这一类控件都设置属性。多个控件可以用逗号分隔，然后统一设置属性。 当某个控件是QWidget的继承类时，可以使用QWidget#[名称]来替代（例如Label）。具体细则请网络查找QSS， 有非常详细的介绍。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;概要&lt;/h1&gt;
&lt;p&gt;人们对一个友好的用户界面的要求，还需要配色来点缀，才可以使得它完美无缺。几乎在每个控件的属性中，都可以设置颜色和文字大小，这也是配色的主要表现形式。Qt作为一个强大的用户界面绘制工具，它支持QSS脚本来辅助控制窗口控件属性，兼容网页格式控制的CSS脚</summary>
      
    
    
    
    <category term="program language" scheme="https://www.jk404.cn/categories/program-language/"/>
    
    <category term="python" scheme="https://www.jk404.cn/categories/program-language/python/"/>
    
    <category term="pyside2" scheme="https://www.jk404.cn/categories/program-language/python/pyside2/"/>
    
    
  </entry>
  
  <entry>
    <title>pyside2_03_layout</title>
    <link href="https://www.jk404.cn/program-language/python/pyside2/pyside2-03-layout/"/>
    <id>https://www.jk404.cn/program-language/python/pyside2/pyside2-03-layout/</id>
    <published>2022-10-18T10:02:58.000Z</published>
    <updated>2022-10-18T10:17:43.727Z</updated>
    
    <content type="html"><![CDATA[<h1>概要</h1><p>QT提供了很多界面布局，最简单的是水平和垂直。为什么要使用布局？有些人可能遇到过， 窗口绘制好后，运行时偶然拖动窗口或控件位置大小，会破坏整个布局的美观。在Qt中使用布局功能， 能很好的控制控件的大小和位置随窗孔变化而变化。</p><h1>绘制窗口</h1><p>添加MainWindow，放置一个Vertical Layout，并拖入label和pushButton。组件会在layout框架中自动排列好，给label设置属性，在filter里搜索alignment，将水平和垂直都设置为Center。</p>]]></content>
    
    
      
      
    <summary type="html">&lt;h1&gt;概要&lt;/h1&gt;
&lt;p&gt;QT提供了很多界面布局，最简单的是水平和垂直。为什么要使用布局？有些人可能遇到过， 窗口绘制好后，运行时偶然拖动窗口或控件位置大小，会破坏整个布局的美观。在Qt中使用布局功能， 能很好的控制控件的大小和位置随窗孔变化而变化。&lt;/p&gt;
&lt;h1&gt;绘制窗口</summary>
      
    
    
    
    <category term="program language" scheme="https://www.jk404.cn/categories/program-language/"/>
    
    <category term="python" scheme="https://www.jk404.cn/categories/program-language/python/"/>
    
    <category term="pyside2" scheme="https://www.jk404.cn/categories/program-language/python/pyside2/"/>
    
    
  </entry>
  
</feed>
